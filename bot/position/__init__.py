#!/usr/bin/env python3
"""
bot.position — Position lifecycle FSM package (Phase 4).

Contains PositionState FSM + PositionPhase enum (state.py) and
PositionManager (manager.py). Imports cleanly without moomoo-api installed.
"""
