#!/usr/bin/env python3
"""
bot.execution — Order execution package (Phase 4).

Contains ExecutionEngine (engine.py) and FillEvent (events.py).
Imports cleanly without moomoo-api installed (deferred imports pattern).
"""
