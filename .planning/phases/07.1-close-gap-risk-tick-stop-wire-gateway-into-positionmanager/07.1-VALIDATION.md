---
phase: 07.1
slug: close-gap-risk-tick-stop-wire-gateway-into-positionmanager
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-07
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Reconstructed from artifacts (State B) — phase executed 2026-07-06, no VALIDATION.md existed.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.x (pytest-asyncio 1.4, strict mode) |
| **Config file** | none — pytest auto-discovery from repo root |
| **Quick run command** | `python3 -m pytest tests/test_main_wiring.py` |
| **Full suite command** | `python3 -m pytest tests/ -q` |
| **Estimated runtime** | ~0.4s (wiring test) · ~full suite 589 passed / 1 skipped |

---

## Sampling Rate

- **After every task commit:** Run `python3 -m pytest tests/test_main_wiring.py`
- **After every plan wave:** Run `python3 -m pytest tests/ -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~1 second (targeted test)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.1-01-01 | 01 | 1 | RISK-TICK-STOP (crit 1: `_gateway` identity) | T-07.1-01 | `position_manager._gateway` is the shared SIMULATE `MoomooGateway` (not None) — no second/REAL gateway path | unit / regression | `python3 -m pytest tests/test_main_wiring.py::test_main_wires_gateway_into_position_manager` | ✅ | ✅ green |
| 07.1-01-02 | 01 | 1 | RISK-TICK-STOP (crit 2: D-02 dispatch) | T-07.1-01 | `arm_stop_protection()` reaches `gateway.subscribe_quote` on shipped `use_broker_stop_orders=false` instead of no-op'ing; dispatch goes through the existing path, not a duplicate order path | unit / regression | `python3 -m pytest tests/test_main_wiring.py::test_armed_stop_dispatches_through_real_construction` | ✅ | ✅ green |
| 07.1-01-03 | 01 | 1 | RISK-TICK-STOP (crit 3: no regression) | T-07.1-02 | Full suite green; `arm_stop_protection`, `place_stop_order`, `subscribe_quote`, bar-close FSM (D-03) unchanged — the additive kwarg introduces no new raise into the trade loop | suite | `python3 -m pytest tests/ -q` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. The regression guard
`tests/test_main_wiring.py` was created in Wave 1 (RED-first) as part of the fix.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

*Note: end-to-end SIMULATE Stop-Market probe against live OpenD (the broader RISK-TICK-STOP
D-01 broker path) is tracked at the milestone/UAT level, not this wiring phase — this phase's
scope is the constructor wiring + D-02 shipped-config dispatch, both automated above.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — no gaps)
- [x] No watch-mode flags
- [x] Feedback latency < 1s
- [x] `nyquist_compliant: true` set in frontmatter

## Validation Audit 2026-07-07

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

RISK-TICK-STOP fully covered by `tests/test_main_wiring.py` (2 tests, crit 1 + crit 2)
plus the full suite (crit 3). RED-before / GREEN-after confirmed at execution time.
No auditor spawn required — no gaps.

**Approval:** approved 2026-07-07
