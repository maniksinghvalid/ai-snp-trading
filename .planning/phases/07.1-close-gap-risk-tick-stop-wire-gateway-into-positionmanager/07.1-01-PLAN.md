---
phase: 07.1-close-gap-risk-tick-stop-wire-gateway-into-positionmanager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/main.py
  - tests/test_main_wiring.py
autonomous: true
requirements:
  - RISK-TICK-STOP
must_haves:
  truths:
    - "After bot/main.py's real construction sequence runs, position_manager._gateway is the same MoomooGateway instance passed to the other engines (not None) — RISK-TICK-STOP crit 1."
    - "Calling arm_stop_protection() on the real-construction position_manager dispatches to gateway.subscribe_quote (D-02, shipped use_broker_stop_orders=false) instead of silently no-op'ing — RISK-TICK-STOP crit 2."
    - "The regression test fails against the unmodified bot/main.py (proves it would have caught Gap 1) and passes after the one-line fix — RISK-TICK-STOP crit 2."
    - "The full test suite stays green; arm_stop_protection, place_stop_order, subscribe_quote, and the bar-close stop backstop (D-03) are unchanged — RISK-TICK-STOP crit 3."
  artifacts:
    - path: "tests/test_main_wiring.py"
      provides: "Regression test that drives bot/main.py's real PositionManager construction path and asserts the gateway wiring holds"
      contains: "arm_stop_protection"
      min_lines: 40
    - path: "bot/main.py"
      provides: "PositionManager construction with gateway wired in"
      contains: "gateway=gateway"
  key_links:
    - from: "bot/main.py"
      to: "PositionManager"
      via: "gateway=gateway keyword argument in the PositionManager(...) call"
      pattern: "gateway=gateway"
    - from: "tests/test_main_wiring.py"
      to: "bot.main.main"
      via: "invokes the real main() construction with I/O seams patched, captures the real PositionManager"
      pattern: "\\.main\\(\\)"
---

<objective>
Close v1.0-MILESTONE-AUDIT Gap 1: `bot/main.py` never passes `gateway=` into its
`PositionManager(...)` construction (lines ~100-111), so `PositionManager._gateway`
is `None` on every live/paper run and `arm_stop_protection()` (manager.py:265-266)
silently no-ops — RISK-TICK-STOP's broker Stop-Market path (D-01) and quote-tick
fallback (D-02) are dead code in production. Only the bar-close stop backstop (D-03)
is active.

The fix is one line (`gateway=gateway,` in the existing call). The real deliverable
is a regression test that drives the ACTUAL `bot/main.py` construction path (not a
hand-built `PositionManager(gateway=mock)` — that is the very pattern that let this
ship broken and pass Phase 07 verification). RED-first: the test must fail against
current `bot/main.py`, proving it would have caught Gap 1, then pass after the fix.

Purpose: make RISK-TICK-STOP real on the live path and install a guard so this class
of bug — correct class, wrong wiring — cannot silently regress again.
Output: `tests/test_main_wiring.py` (new regression test) + one added kwarg in `bot/main.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-close-gap-risk-tick-stop-wire-gateway-into-positionmanager/07.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@bot/main.py
@bot/position/manager.py
</context>

<artifacts_produced>
## Artifacts this phase produces

New symbols:
- `tests/test_main_wiring.py` — new test module. Contains the regression test
  function(s), e.g. `test_main_wires_gateway_into_position_manager` (asserts
  `position_manager._gateway` identity) and `test_armed_stop_dispatches_through_real_construction`
  (asserts `arm_stop_protection` calls `gateway.subscribe_quote`), plus a small
  in-test capture wrapper around `bot.main.PositionManager`.

Modified (no new symbol):
- `bot/main.py` — the existing `PositionManager(...)` call gains one keyword
  argument (`gateway=gateway`). No new function, class, or export.

Nothing else is created. `arm_stop_protection`, `place_stop_order`,
`subscribe_quote`, the D-02 quote-tick callback, and the bar-close FSM (D-03) are
untouched (read-only reference).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write the RED regression test driving bot/main.py's real construction path</name>
  <files>tests/test_main_wiring.py</files>
  <read_first>
    - bot/main.py — the full main() construction sequence being asserted; note `gateway` is a local at line 75 and the `PositionManager(...)` call at lines ~100-111 that currently omits it; note the final `asyncio.run(bot.run())` at line 141 is the only I/O trigger.
    - bot/position/manager.py lines 122-160 (`__init__` — the `gateway=None` kwarg stored as `self._gateway`) and lines 246-290 (`arm_stop_protection` — the `if self._gateway is None: return` no-op at 265-266, the D-02 `subscribe_quote` path at 270-290, the D-01 `place_stop_order` path at 292-315).
    - tests/position/test_manager.py — the `_make_pos` PositionState factory at line 43 (reuse this construction pattern) and the existing `TestArmStopProtection` class at ~line 2324 (the INSUFFICIENT hand-built `PositionManager(gateway=mock)` pattern — do NOT duplicate it; the new test must drive the real bot/main.py path instead).
    - rules.json line 60 — `"use_broker_stop_orders": false`, which means main()'s loaded cfg routes `arm_stop_protection` down the D-02 `subscribe_quote` path (not `place_stop_order`).
    - tests/conftest.py — existing fixtures / test conventions to reuse.
  </read_first>
  <action>
    Create tests/test_main_wiring.py. Import `bot.main` as a module and drive its
    real `main()` with only the three I/O seams patched (monkeypatch on the
    `bot.main` module namespace):
    (1) `bot.main.MoomooGateway` -> a factory returning a single shared `AsyncMock`
        instance (async `subscribe_quote` and `place_stop_order`), so the same mock
        object is what `gateway` binds to and what `position_manager._gateway` should
        end up being (identity check);
    (2) `bot.main.StateStore` -> `MagicMock` (its `.open()` returns a mock; no DB is
        touched by the D-02 arm path);
    (3) `bot.main.asyncio.run` -> a no-op that calls `.close()` on the passed
        coroutine so `bot.run()` never actually runs and no "coroutine was never
        awaited" warning is raised.
    Capture the REAL `PositionManager` instance by wrapping `bot.main.PositionManager`:
    the wrapper calls the original constructor, records the returned instance and the
    `gateway` kwarg it received, then returns the instance. `get_gateway_config()` and
    the real `rules.json` load may run unpatched (harmless — result of the former is
    discarded by the patched MoomooGateway factory).
    Write two assertions:
    (a) `captured_pm._gateway is mock_gateway` (identity — the wiring assertion,
        RISK-TICK-STOP crit 1);
    (b) build a `PositionState` via the `_make_pos` pattern (`PositionPhase.ACTIVE`,
        a set `trail_stop`, `code="US.AAPL"`), run
        `asyncio.run(captured_pm.arm_stop_protection(pos))`, and assert
        `mock_gateway.subscribe_quote` was awaited/called (shipped
        use_broker_stop_orders=false -> D-02 path — RISK-TICK-STOP crit 2). Add a
        comment that if the shipped flag ever flips to true, the asserted dispatch
        becomes `place_stop_order`.
    Do NOT modify bot/main.py in this task. The test MUST fail now (RED): against the
    unmodified bot/main.py, `captured_pm._gateway` is `None` (kwarg absent) so
    assertion (a) fails, and `arm_stop_protection` returns immediately at the
    `self._gateway is None` guard so `subscribe_quote` is never called and assertion
    (b) fails.
  </action>
  <verify>
    <automated>python -m pytest tests/test_main_wiring.py; test $? -ne 0</automated>
  </verify>
  <done>
    tests/test_main_wiring.py exists, imports cleanly, and `python -m pytest
    tests/test_main_wiring.py` FAILS (non-zero exit) against the unmodified
    bot/main.py — with failures pointing at `position_manager._gateway is None` and
    `subscribe_quote` never called. RED confirmed: the test would have caught Gap 1.
    bot/main.py is unchanged.
  </done>
  <acceptance_criteria>
    - File tests/test_main_wiring.py exists and collects/imports without error.
    - The test patches `bot.main.MoomooGateway` (shared AsyncMock), `bot.main.StateStore` (MagicMock), and `bot.main.asyncio.run` (coroutine-closing no-op), and wraps `bot.main.PositionManager` to capture the real instance — it does NOT construct `PositionManager(gateway=...)` by hand.
    - The test invokes `bot.main.main()` and asserts `captured_pm._gateway is <the shared mock gateway>` (identity) AND that `asyncio.run(captured_pm.arm_stop_protection(pos))` calls `mock_gateway.subscribe_quote`.
    - `python -m pytest tests/test_main_wiring.py` exits non-zero (RED) against the current unmodified bot/main.py.
    - `git diff --name-only` shows only tests/test_main_wiring.py added; bot/main.py NOT modified.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply the one-line gateway wiring fix and confirm GREEN + full suite</name>
  <files>bot/main.py</files>
  <read_first>
    - bot/main.py — the `PositionManager(...)` call at lines ~100-111 (currently passes store, engine, cfg, strategy, on_entry_alert, on_exit_alert) and the `gateway` local defined at line 75. This is an additive one-kwarg change.
    - tests/test_main_wiring.py — the RED regression test from Task 1, which must flip to GREEN after this change.
    - .planning/phases/07.1-close-gap-risk-tick-stop-wire-gateway-into-positionmanager/07.1-CONTEXT.md — the Fix shape / Non-goals block: do NOT touch arm_stop_protection, place_stop_order, subscribe_quote, the quote-tick callback, or the bar-close FSM (D-03); do NOT introduce a redundant `_gateway` late-bind in bot/service/bot.py.
  </read_first>
  <action>
    In bot/main.py, inside the existing `PositionManager(...)` call (lines ~100-111),
    add one new keyword argument `gateway=gateway` alongside the existing
    store/engine/cfg/strategy/on_entry_alert/on_exit_alert kwargs. The `gateway` local
    is already in scope (constructed at line 75, the same MoomooGateway instance
    already passed to ExecutionEngine, SignalEngine, RiskEngine, and TradingBot). This
    is the only production change: additive, one kwarg. Do not construct a second
    gateway, do not add a late-bind in bot/service/bot.py, and do not modify any
    manager.py logic. Then run the regression test (now GREEN) and the full suite.
  </action>
  <verify>
    <automated>python -m pytest tests/test_main_wiring.py -x && python -m pytest tests/ -q</automated>
  </verify>
  <done>
    `grep -n "gateway=gateway" bot/main.py` returns the line inside the
    PositionManager(...) call. tests/test_main_wiring.py PASSES. The full suite
    (`python -m pytest tests/ -q`) passes with zero failures. `git diff bot/main.py`
    shows exactly one added kwarg line and no other production change.
  </done>
  <acceptance_criteria>
    - `grep -c "gateway=gateway" bot/main.py` returns at least 1, and the match sits inside the `PositionManager(...)` call (not elsewhere).
    - `python -m pytest tests/test_main_wiring.py -x` PASSES (GREEN) — both the `_gateway` identity assertion and the `subscribe_quote` dispatch assertion hold.
    - `python -m pytest tests/ -q` passes with zero failures.
    - `git diff bot/main.py` shows only the single added `gateway=gateway` kwarg line; no edits to bot/position/manager.py or bot/service/bot.py.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot -> MoomooGateway -> OpenD -> paper (SIMULATE) account | Protective stop orders / quote subscriptions cross here. The gateway being wired in is the single SIMULATE-only `MoomooGateway` already used by ExecutionEngine/SignalEngine/RiskEngine — not a new connection. |
| test harness -> bot.main.main() | Test patches only I/O seams (MoomooGateway, StateStore, asyncio.run); production wiring code runs for real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Tampering | bot/main.py PositionManager wiring | mitigate | Fix is purely additive — passes the SAME already-constructed gateway local (main.py:75) that the other engines receive; no second gateway, no new order path. Regression test asserts dispatch goes through the existing `subscribe_quote`/`place_stop_order`, not a duplicate. No risk of duplicate/unintended stop orders from a new path. |
| T-07.1-02 | Denial of Service | arm_stop_protection in trade loop | accept | `arm_stop_protection` is NOT modified. Its existing `try/except Exception` around `subscribe_quote` (manager.py:284) and `place_stop_order` (manager.py:309) is preserved, so a broker/subscription failure is still swallowed and the bar-close FSM (D-03) remains the backstop. The fix only sets `self._gateway`; it cannot introduce a new raise into the loop. |
| T-07.1-03 | Elevation of Privilege | paper-safety guard (SAFE-01) | mitigate | The wired `gateway` is the identical single `MoomooGateway(get_gateway_config())` instance already guarded by the SAFE-01 paper guard and TrdEnv=SIMULATE lock; no new/REAL connection and no TrdEnv change is introduced. Verified by reading bot/main.py: exactly one MoomooGateway is constructed (line 75). |
| T-07.1-SC | Tampering | package installs (npm/pip/cargo) | accept (N/A) | No package installs in this phase — test-only + one-line source edit. No new dependency, no legitimacy gate needed. |
</threat_model>

<verification>
- `python -m pytest tests/test_main_wiring.py -x` passes (GREEN) after the fix and fails (RED) before it.
- `python -m pytest tests/ -q` full suite green.
- `grep -n "gateway=gateway" bot/main.py` confirms the kwarg is present in the PositionManager(...) call.
- `git diff` shows only two touched files: bot/main.py (one added kwarg line) and tests/test_main_wiring.py (new file). No change to bot/position/manager.py or bot/service/bot.py.
</verification>

<success_criteria>
- RISK-TICK-STOP crit 1: `position_manager._gateway is not None` after the real bot/main.py construction sequence (asserted by identity in the regression test).
- RISK-TICK-STOP crit 2: a regression test drives the real bot/main.py construction path (not a hand-built PositionManager) and asserts `arm_stop_protection()` calls `gateway.subscribe_quote` (shipped D-02 config) rather than no-op'ing; it fails against the pre-fix code and passes after.
- RISK-TICK-STOP crit 3: full suite green; bar-close stop backstop (D-03), arm_stop_protection, place_stop_order, and subscribe_quote are unchanged.
</success_criteria>

<output>
Create `.planning/phases/07.1-close-gap-risk-tick-stop-wire-gateway-into-positionmanager/07.1-01-SUMMARY.md` when done
</output>
