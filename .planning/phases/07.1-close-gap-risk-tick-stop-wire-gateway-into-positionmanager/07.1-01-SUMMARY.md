---
phase: 07.1-close-gap-risk-tick-stop-wire-gateway-into-positionmanager
plan: 01
subsystem: testing
tags: [risk, position-manager, gateway, wiring, regression-test, tdd]

requires:
  - phase: 07-strategy-optimization
    provides: PositionManager.arm_stop_protection (D-01/D-02) and gateway=None kwarg
provides:
  - "bot/main.py passes gateway= into PositionManager so RISK-TICK-STOP D-01/D-02 stop paths are live in production"
  - "tests/test_main_wiring.py — regression guard driving the real bot.main.main() construction path"
affects: [live-trade-loop, risk-stop-management, milestone-audit-gap-closure]

tech-stack:
  added: []
  patterns:
    - "Wiring regression tests drive the real bot.main.main() construction with only I/O seams (MoomooGateway, StateStore, asyncio.run) patched, capturing the real instance — never hand-built PositionManager(gateway=mock)"

key-files:
  created:
    - tests/test_main_wiring.py
  modified:
    - bot/main.py

key-decisions:
  - "Constructor-kwarg fix (gateway=gateway in PositionManager(...)) chosen over a bot/service/bot.py late-bind — gateway is already in scope at construction (main.py:75), so no late-bind excuse applies"
  - "Regression test asserts through the real main() path, not a hand-built PositionManager — the hand-built pattern is exactly what let Gap 1 ship and pass Phase 07 verification"

patterns-established:
  - "Production-wiring guard: patch only I/O seams and assert the real construction wires dependencies (identity + dispatch), so correct-class/wrong-wiring bugs cannot silently regress"

requirements-completed: [RISK-TICK-STOP]

duration: 6min
completed: 2026-07-06
---

# Phase 07.1 Plan 01: Wire gateway into PositionManager (RISK-TICK-STOP) Summary

**One-line `gateway=gateway` fix in bot/main.py revives the dead RISK-TICK-STOP stop paths, guarded by a regression test that drives the real main() construction and fails against the pre-fix code.**

## Performance

- **Duration:** ~6 min
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments
- Closed v1.0-MILESTONE-AUDIT Gap 1: `PositionManager._gateway` is now the shared `MoomooGateway` instance on every live/paper run instead of `None`, so `arm_stop_protection()` dispatches D-01/D-02 stop management instead of silently no-op'ing.
- Added `tests/test_main_wiring.py`, a regression guard that drives the actual `bot.main.main()` construction sequence (patching only the MoomooGateway, StateStore, and asyncio.run I/O seams) and asserts both the `_gateway` identity (crit 1) and that `arm_stop_protection()` reaches `gateway.subscribe_quote` on the shipped `use_broker_stop_orders=false` path (crit 2).
- Confirmed RED-before / GREEN-after: the test fails against unmodified `bot/main.py` (proving it would have caught Gap 1) and passes after the one-kwarg fix.

## Task Commits

1. **Task 1: Write RED regression test driving bot/main.py's real construction path** - `5cc61eb` (test)
2. **Task 2: Apply the one-line gateway wiring fix + confirm GREEN + full suite** - `a8b2418` (fix)

## Files Created/Modified
- `tests/test_main_wiring.py` - Regression test driving real `bot.main.main()` construction; asserts `_gateway` identity and `arm_stop_protection` -> `gateway.subscribe_quote` dispatch.
- `bot/main.py` - Added `gateway=gateway` kwarg to the existing `PositionManager(...)` call (the `gateway` local from line 75, already shared with the other engines).

## Decisions Made
- Used the direct constructor-kwarg fix; no `_gateway` late-bind added in `bot/service/bot.py` (gateway is available at construction time, so the late-bind precedent does not apply here).
- Test constructs `PositionState` with the required `position_id` field (discovered as a required dataclass field during Task 1) rather than reusing `_make_pos` from `tests/position/test_manager.py`, keeping the wiring test self-contained.

## Deviations from Plan
None - plan executed exactly as written. `arm_stop_protection`, `place_stop_order`, `subscribe_quote`, and the bar-close FSM (D-03) were left untouched, as specified.

## Issues Encountered
- First RED run of Task 1's crit-2 assertion errored on a missing required `position_id` argument to `PositionState` rather than on the wiring assertion. Added `position_id="POS-TEST-001"` so the test fails for the intended wiring reason (`subscribe_quote` never called). RED then confirmed for the correct cause before committing.

## Next Phase Readiness
- RISK-TICK-STOP is now live on the production path and guarded against silent regression. Remaining v1.0-MILESTONE-AUDIT items (Tier 2/Tier 3 tech debt, Phase 6 backtester gaps) are separately tracked and out of scope here.

## Self-Check: PASSED

- `tests/test_main_wiring.py` — FOUND
- `07.1-01-SUMMARY.md` — FOUND
- `gateway=gateway` in `bot/main.py` — FOUND
- Commit `5cc61eb` (RED test) — FOUND
- Commit `a8b2418` (fix) — FOUND
- Full suite: 589 passed, 1 skipped

---
*Phase: 07.1-close-gap-risk-tick-stop-wire-gateway-into-positionmanager*
*Completed: 2026-07-06*
