---
phase: 02-premarket-scanner
plan: 02
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - bot/state/migrations.py
  - bot/scanner/scanner.py
  - tests/scanner/test_scanner.py
  - tests/state/test_migrations.py
autonomous: true
requirements: [SCAN-02, SCAN-03, SCAN-05, SCAN-08]
user_setup: []

must_haves:
  truths:
    - "Migration 0002 adds five nullable columns (prior_day_high, prior_close, sma200, rvol_baseline, scan_pass) to daily_scan and bumps user_version to 2; existing migration 0001 is untouched"
    - "run_daily_scan computes SMA200 and the 14-day RVOL baseline (date < scan_date, no look-ahead), calls TrendJoinLong.passes_daily_filters per symbol, and excludes symbols with insufficient prior history"
    - "Passing candidates are ranked by gap % descending and capped at exactly 20 before persistence (SCAN-08)"
    - "Persistence uses INSERT ... ON CONFLICT(scan_date, code) DO UPDATE so re-running the same scan day does not duplicate rows (SCAN-05)"
  artifacts:
    - path: "bot/state/migrations.py"
      provides: "_MIGRATION_0002, CURRENT_VERSION=2"
      contains: "_MIGRATION_0002"
    - path: "bot/scanner/scanner.py"
      provides: "run_daily_scan, _evaluate_symbol, _persist_watchlist"
      exports: ["run_daily_scan"]
      min_lines: 80
  key_links:
    - from: "bot/scanner/scanner.py"
      to: "TrendJoinLong.passes_daily_filters"
      via: "per-symbol daily filter call"
      pattern: "passes_daily_filters"
    - from: "bot/scanner/scanner.py"
      to: "indicators.sma / indicators.rvol"
      via: "SMA200 + 14-day RVOL baseline computation"
      pattern: "sma\\(|rvol\\("
    - from: "bot/scanner/scanner.py"
      to: "daily_scan (SQLite)"
      via: "INSERT ON CONFLICT DO UPDATE upsert"
      pattern: "ON CONFLICT"
---

<objective>
Add migration 0002 (the D-08 rich-context columns on daily_scan) and build the
`run_daily_scan` orchestrator: compose the Plan 02-01 leaf modules
(universe/fetcher/calendar) with the Phase 1 strategy primitives (sma, rvol,
TrendJoinLong.passes_daily_filters) to produce a top-20 gap-ranked watchlist and
persist it idempotently. This plan delivers the premarket scan path end-to-end
EXCEPT the broker subscription and the intraday re-scan merge (Plan 02-03).

Purpose: This is the core scan logic — config-driven (no literals), no-look-ahead
RVOL, top-20 cap, idempotent upsert. Phase 3 reads the persisted rich columns
rather than recomputing (D-08).
Output: bot/state/migrations.py (0002), bot/scanner/scanner.py (run_daily_scan).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-premarket-scanner/02-CONTEXT.md
@.planning/phases/02-premarket-scanner/02-RESEARCH.md
@.planning/phases/02-premarket-scanner/02-PATTERNS.md
@.planning/phases/02-premarket-scanner/02-VALIDATION.md
@.planning/phases/02-premarket-scanner/02-01-SUMMARY.md
@bot/state/migrations.py
@bot/state/store.py
@bot/strategy/trend_join_long.py
@bot/strategy/indicators.py
@bot/config/loader.py
@bot/safety/et_helpers.py
@tests/conftest.py
@tests/state/test_migrations.py
---
# Interfaces this plan consumes (exact, from Phase 1 + Plan 02-01):
# - load_strategy_config(path="rules.json") -> StrategyConfig
#     cfg.min_price_usd, cfg.d3_min_gap_pct, cfg.rvol_lookback_days (=14)
# - TrendJoinLong(cfg).passes_daily_filters(code: str, daily_data: pd.DataFrame, sma200: float) -> bool
#     daily_data needs >=2 rows, lowercase columns: open, high, low, close (iloc[-2]=prior, iloc[-1]=today)
# - indicators.sma(series: pd.Series, period: int) -> Optional[float]   # NaN if < period
# - indicators.rvol(volume_frame: pd.DataFrame[date,volume], signal_date, lookback_days, today_volume) -> Optional[float]
#     denominator strictly date < signal_date (no look-ahead already enforced)
# - bot/scanner/universe.fetch_sp500_symbols(cache_dir="data") -> list   # yfinance-format
# - bot/scanner/universe.yfinance_to_moomoo(yf_symbol) -> "US.<sym>"
# - bot/scanner/fetcher.download_daily_bars(yf_symbols, threads=5) -> (data, failed_set); raises ScanDegradationError
# - bot/scanner/fetcher.get_ticker_frame(data, symbol) -> Optional[pd.DataFrame]   # lowercase cols or None
# - bot/scanner/calendar.is_trading_day(dt: date) -> bool
# - StateStore().open().conn  -> sqlite3.Connection ; daily_scan UNIQUE(scan_date, code)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0002 — extend daily_scan with rich context columns (D-08)</name>
  <files>bot/state/migrations.py, tests/state/test_migrations.py</files>
  <read_first>
    - bot/state/migrations.py (existing _MIGRATION_0001, MIGRATIONS list, CURRENT_VERSION, run_migrations runner — APPEND only, do not edit 0001)
    - .planning/phases/02-premarket-scanner/02-PATTERNS.md §"bot/state/migrations.py" (exact _MIGRATION_0002 SQL block, MIGRATIONS append, CURRENT_VERSION=2)
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Research Area 5" (ALTER TABLE ADD COLUMN, nullable-only constraint, user_version gate)
    - tests/state/test_migrations.py (TestMigrationFreshDb, TestMigrationIdempotency, in_memory_conn fixture analogs)
  </read_first>
  <behavior>
    - test_migration_0002_adds_columns: after run_migrations on a fresh in-memory DB, daily_scan has columns prior_day_high, prior_close, sma200, rvol_baseline, scan_pass (via PRAGMA table_info).
    - test_user_version_is_2_after_migration: PRAGMA user_version == 2 after run_migrations.
    - test_migration_idempotent_v2: calling run_migrations twice is a no-op (no duplicate-column error; version stays 2).
    - test_upgrade_from_v1_db: a DB pre-migrated to user_version=1 (0001 only) gains the 5 new columns when run_migrations is called (ordered upgrade applies only 0002).
  </behavior>
  <action>
    Append a `_MIGRATION_0002` string after `_MIGRATION_0001` containing exactly five
    `ALTER TABLE daily_scan ADD COLUMN` statements: `prior_day_high REAL`, `prior_close REAL`,
    `sma200 REAL`, `rvol_baseline REAL`, `scan_pass TEXT` (all nullable — SQLite ALTER ADD
    COLUMN cannot add NOT NULL without a default). Do NOT re-add gap_pct (already in 0001).
    Append `_MIGRATION_0002` to the `MIGRATIONS` list and change `CURRENT_VERSION = 2`. Leave
    `_MIGRATION_0001` and `run_migrations()` UNCHANGED (the ordered runner already gates by
    user_version). Add the four migration tests above to tests/state/test_migrations.py.
  </action>
  <verify>
    <automated>python3 -m pytest tests/state/test_migrations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_MIGRATION_0002' bot/state/migrations.py` >= 2 (definition + MIGRATIONS entry).
    - `grep -c 'CURRENT_VERSION = 2' bot/state/migrations.py` == 1.
    - All four new migration tests pass; existing test_migrations.py tests still pass.
    - PRAGMA table_info(daily_scan) includes all five new columns; user_version == 2.
    - _MIGRATION_0001 text is byte-for-byte unchanged (the v1 upgrade path must apply only 0002).
  </acceptance_criteria>
  <done>daily_scan extended with five nullable rich-context columns via migration 0002; user_version bumped to 2; idempotent and v1-upgrade-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bot/scanner/scanner.py — run_daily_scan filter + SMA200/RVOL + top-20 cap (SCAN-02/03/08)</name>
  <files>bot/scanner/scanner.py, tests/scanner/test_scanner.py</files>
  <read_first>
    - .planning/phases/02-premarket-scanner/02-PATTERNS.md §"bot/scanner/scanner.py" (imports, structlog event naming, raise-not-exit, config-driven) and §"Shared Patterns / Config-Driven Parameters" and §"RVOL Baseline"
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Research Area 3" (RVOL baseline = mean of prior 14 sessions, date < scan_date) and §"Research Area 7" (run_daily_scan sequence)
    - bot/strategy/trend_join_long.py (passes_daily_filters signature; daily_data needs lowercase open/high/low/close, iloc[-2]/iloc[-1])
    - bot/strategy/indicators.py (sma, rvol exact signatures and NaN/None semantics)
    - bot/config/loader.py (StrategyConfig fields: min_price_usd, d3_min_gap_pct, rvol_lookback_days)
    - bot/scanner/universe.py, bot/scanner/fetcher.py, bot/scanner/calendar.py (Plan 02-01 leaf APIs — see Interfaces block above)
    - tests/scanner/test_scanner.py (test_daily_filters, test_rvol_no_lookahead, test_top20_cap stubs to flesh out)
    - tests/gateway/test_gateway.py lines 41-65 (mock-injection factory analog)
  </read_first>
  <behavior>
    - test_daily_filters: a synthetic per-symbol frame that satisfies D1/D2/D3 + price passes; one that fails D3 (gap < cfg.d3_min_gap_pct) is excluded. Swapping cfg.d3_min_gap_pct higher excludes a previously-passing symbol (config-driven proof, D-12).
    - test_rvol_no_lookahead: a frame whose latest (today) volume is huge → rvol_baseline equals the mean of the PRIOR 14 sessions only (today excluded). Recomputing with today's row removed gives the identical baseline.
    - test_insufficient_history_excluded: a symbol with < (rvol_lookback_days) prior sessions → excluded from the watchlist, warning logged.
    - test_top20_cap: 25 synthetic passing candidates → exactly 20 persisted, ranked by gap% descending (rank 1 = highest gap).
  </behavior>
  <action>
    Implement `run_daily_scan(store, gateway, cfg, scan_date: date = None, scan_pass: str =
    "premarket") -> list` (gateway param accepted now; the subscribe call is wired in Plan
    02-03 — leave a clearly-marked seam, do NOT stub a fake watchlist). Sequence:
    (1) `scan_date = scan_date or now_et().date()`; if `not is_trading_day(scan_date)` →
    log `"scan_skipped_not_trading_day"` and return `[]` (no error). (2) `symbols =
    fetch_sp500_symbols()`. (3) `data, failed = download_daily_bars(symbols)` (propagates
    ScanDegradationError). (4) For each symbol build the lowercase daily frame via
    `get_ticker_frame`; skip None; require >= 2 rows AND >= cfg.rvol_lookback_days prior
    sessions (else exclude + warn). Compute `sma200 = indicators.sma(frame["close"].iloc[:-1],
    200)` (prior closes), build a volume_frame(date, volume) and compute `rvol_baseline` as
    the mean of the prior `cfg.rvol_lookback_days` sessions with `date < scan_date` (no
    look-ahead — mirror indicators.rvol's strict `<`; exclude today). Compute `gap_pct` as
    `(today_open - prior_close)/prior_close*100`. Call `TrendJoinLong(cfg).passes_daily_filters(
    code, frame, sma200)`; collect passing candidates with their prior_day_high (iloc[-2].high),
    prior_close (iloc[-2].close), sma200, rvol_baseline, gap_pct, and moomoo code
    (`yfinance_to_moomoo`). (5) Sort passing by gap_pct DESC, take top 20, assign rank 1..N.
    (6) Persist via the Task 3 `_persist_watchlist`. Return the list of moomoo codes (top 20).
    ALL thresholds from cfg — never literals (use cfg.d3_min_gap_pct etc. via passes_daily_filters
    and cfg.rvol_lookback_days for the baseline window). Use `get_logger(__name__)`. Flesh out the
    three named test_scanner.py stubs plus test_insufficient_history_excluded.
  </action>
  <verify>
    <automated>python3 -m pytest tests/scanner/test_scanner.py -k "daily_filters or rvol_no_lookahead or top20_cap or insufficient_history" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_daily_filters, test_rvol_no_lookahead, test_top20_cap, test_insufficient_history_excluded pass.
    - `grep -v '^#' bot/scanner/scanner.py | grep -Ec '(>=\s*3\.0|>=\s*2\.0|lookback.*=\s*14|min_price.*=\s*3)'` returns 0 — no hardcoded strategy literals (all via cfg / passes_daily_filters).
    - RVOL baseline uses strict `date < scan_date` (today excluded): test_rvol_no_lookahead proves the baseline is invariant to today's volume.
    - Exactly 20 rows when > 20 candidates pass; rank 1 has the highest gap_pct.
    - Non-trading-day scan returns [] and raises nothing.
  </acceptance_criteria>
  <done>run_daily_scan filters the universe via passes_daily_filters with config-driven thresholds, computes no-look-ahead SMA200/RVOL baseline, and produces a top-20 gap-ranked candidate set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Idempotent watchlist persistence — upsert into daily_scan (SCAN-05)</name>
  <files>bot/scanner/scanner.py, tests/scanner/test_scanner.py</files>
  <read_first>
    - .planning/phases/02-premarket-scanner/02-PATTERNS.md §"Shared Patterns / StateStore Usage" (exact INSERT ... ON CONFLICT(scan_date, code) DO UPDATE SET upsert with all 0002 columns)
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Research Area 5 / Idempotent Upsert Pattern"
    - bot/state/store.py (StateStore.conn property; parameterized execute + commit)
    - tests/conftest.py (tmp_state_db fixture — isolated SQLite per test)
    - tests/scanner/test_scanner.py (test_idempotency stub to flesh out)
  </read_first>
  <behavior>
    - test_idempotency: run_daily_scan twice for the same scan_date with the same candidate set → daily_scan row count is identical after the second run (no duplicates); created_at preserved, rank/gap_pct updated.
    - test_upsert_updates_rank: re-persisting a code with a changed rank/gap_pct updates the existing row (single row per (scan_date, code)).
  </behavior>
  <action>
    Implement `_persist_watchlist(conn, scan_date, candidates: list, scan_pass: str) -> None`
    using a single parameterized upsert per candidate: `INSERT INTO daily_scan (scan_date,
    code, gap_pct, rank, created_at, prior_day_high, prior_close, sma200, rvol_baseline,
    scan_pass) VALUES (?,?,?,?,?,?,?,?,?,?) ON CONFLICT(scan_date, code) DO UPDATE SET
    gap_pct=excluded.gap_pct, rank=excluded.rank, prior_day_high=excluded.prior_day_high,
    prior_close=excluded.prior_close, sma200=excluded.sma200, rvol_baseline=excluded.rvol_baseline,
    scan_pass=excluded.scan_pass` (created_at intentionally NOT in the UPDATE clause — first-seen
    timestamp is preserved). Use `now_et().isoformat()` (or UTC isoformat consistent with Phase 1)
    for created_at on insert; use ISO `scan_date` TEXT. ALWAYS use `?` placeholders (parameterized
    — never f-string interpolation of code/values; SQL-injection guard T-02-06). Call from
    run_daily_scan (Task 2). conn.commit() after the loop. Flesh out test_idempotency and add
    test_upsert_updates_rank using the tmp_state_db fixture.
  </action>
  <verify>
    <automated>python3 -m pytest tests/scanner/test_scanner.py -k "idempotency or upsert" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_idempotency, test_upsert_updates_rank pass.
    - `grep -c 'ON CONFLICT(scan_date, code) DO UPDATE' bot/scanner/scanner.py` >= 1.
    - All SQL values pass through `?` placeholders (no f-string/`%`/`.format` building the SQL — verify by reading: no `code` interpolated into the SQL string).
    - created_at is NOT in the DO UPDATE SET clause (first-seen timestamp preserved across re-scans).
    - Second run on the same scan_date yields the same daily_scan row count.
  </acceptance_criteria>
  <done>Watchlist persists via a parameterized idempotent upsert keyed on (scan_date, code); re-running the day updates rows without duplicating or resetting created_at.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner.py → daily_scan (SQLite) | Application-built rows written to the durable store |
| yfinance frame → indicator math | External numeric data feeds SMA200/RVOL/gap computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | _persist_watchlist upsert SQL | mitigate | All values bound via `?` placeholders (parameterized); never f-string/format the code or numeric values into SQL — no SQL injection surface |
| T-02-07 | Tampering / Integrity | look-ahead in RVOL baseline | mitigate | RVOL baseline uses strict `date < scan_date`; test_rvol_no_lookahead proves invariance to today's volume (PITFALLS #4) |
| T-02-08 | Information Disclosure / Integrity | wrong code format persisted | mitigate | Persist `code` in Moomoo `US.<sym>` format via yfinance_to_moomoo at ingest; test asserts persisted code prefix (Pitfall #5) so Phase 3 lookup matches |
| T-02-09 | Tampering | ScanDegradationError bubbling mid-scan | accept | Degradation gate raises before persistence; partial DB write avoided because filtering+persistence happen after the gate; caller handles the exception |
</threat_model>

<verification>
- `pytest tests/scanner/test_scanner.py tests/state/test_migrations.py -q` green.
- `pytest tests/ -q` green (migration 0002 does not break Phase 1 state tests).
- No hardcoded strategy literals in scanner.py (config-driven proof).
- Upsert is parameterized and idempotent.
</verification>

<success_criteria>
- Migration 0002 adds the five rich-context columns; user_version=2; idempotent + v1-upgrade-safe.
- run_daily_scan applies config-driven D1/D2/D3 filters, no-look-ahead RVOL baseline, top-20 gap cap.
- Persistence is an idempotent parameterized upsert (re-run = same row count).
</success_criteria>

<output>
Create `.planning/phases/02-premarket-scanner/02-02-SUMMARY.md` when done.
</output>
