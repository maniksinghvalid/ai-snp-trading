---
phase: 02-premarket-scanner
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - bot/scanner/__init__.py
  - tests/scanner/__init__.py
  - tests/scanner/test_universe.py
  - tests/scanner/test_fetcher.py
  - tests/scanner/test_scanner.py
  - tests/scanner/test_calendar.py
autonomous: false
requirements: [SCAN-01, SCAN-02, SCAN-03, SCAN-04, SCAN-05, SCAN-06, SCAN-07, SCAN-08, SIG-01]
user_setup: []

must_haves:
  truths:
    - "yfinance and pandas-market-calendars are installed and importable"
    - "tests/scanner/ exists with one failing-or-skipped stub per requirement so no 3 consecutive tasks lack an automated verify"
    - "bot/scanner/ is an importable package"
  artifacts:
    - path: "requirements.txt"
      provides: "yfinance==1.4.1 and pandas-market-calendars==5.4.0 pins"
      contains: "yfinance"
    - path: "bot/scanner/__init__.py"
      provides: "scanner subpackage init"
    - path: "tests/scanner/test_universe.py"
      provides: "SCAN-01 scrape + cache-fallback test stubs"
      contains: "def test_"
    - path: "tests/scanner/test_fetcher.py"
      provides: "SCAN-06 download + degradation test stubs"
      contains: "def test_"
    - path: "tests/scanner/test_scanner.py"
      provides: "SCAN-02/03/05/07/08 + SIG-01 test stubs"
      contains: "def test_"
    - path: "tests/scanner/test_calendar.py"
      provides: "SCAN-04 holiday + half-day test stubs"
      contains: "def test_"
  key_links:
    - from: "tests/scanner/*"
      to: "pytest"
      via: "collection from project root"
      pattern: "pytest tests/scanner"
---

<objective>
Lay the Phase 2 test and dependency foundation (Wave 0). Add the two new runtime
dependencies (`yfinance`, `pandas-market-calendars`) to `requirements.txt` behind
package-legitimacy human-verify checkpoints, install them, create the empty
`bot/scanner/` package init, and create the five `tests/scanner/` test files as
runnable stubs (one stub per requirement-mapped behavior from 02-VALIDATION.md).

Purpose: Per Nyquist Dimension 8, every later task must map to an automated test.
This plan creates those test files first so no 3 consecutive implementation tasks
lack an automated verify, and gates the new package installs behind the mandatory
legitimacy checkpoints (both packages are tagged `[ASSUMED]` in 02-RESEARCH.md).
Output: requirements.txt updated + installed, bot/scanner/__init__.py, and 5 test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-premarket-scanner/02-CONTEXT.md
@.planning/phases/02-premarket-scanner/02-RESEARCH.md
@.planning/phases/02-premarket-scanner/02-PATTERNS.md
@.planning/phases/02-premarket-scanner/02-VALIDATION.md
@requirements.txt
@tests/conftest.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification — yfinance and pandas-market-calendars (T-02-SC)</name>
  <read_first>
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Package Legitimacy Audit" (both packages tagged [ASSUMED]; slopcheck unavailable at research time)
  </read_first>
  <action>
    Pause for operator approval BEFORE any install. Present both packages and their PyPI
    source repos (see how-to-verify) and wait for an explicit "approved" before Task 2 runs.
    Do not install, do not auto-approve — this gate is blocking-human regardless of
    workflow.auto_advance because both packages are [ASSUMED] (slopcheck was unavailable).
  </action>
  <what-built>
    Nothing installed yet. This checkpoint precedes the install in Task 2. Both
    `yfinance==1.4.1` and `pandas-market-calendars==5.4.0` are tagged `[ASSUMED]`
    in 02-RESEARCH.md because slopcheck was unavailable at research time. Per the
    package legitimacy gate, installs must be human-approved before they run.
    This checkpoint is NOT auto-approvable regardless of workflow.auto_advance.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/yfinance/ — confirm the package is maintained by
       `ranaroussi`, source repo github.com/ranaroussi/yfinance, version 1.4.1 present.
    2. Open https://pypi.org/project/pandas-market-calendars/ — confirm source repo
       github.com/rsheftel/pandas_market_calendars, version 5.4.0 present.
    3. Confirm both names match exactly (no typosquat: not "yfinanced", "yfinance-api",
       "pandas-markets-calendar", etc.).
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms both PyPI pages resolve to the named source repos above.
    - Operator types "approved" (or rejects with the specific concern).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human-verify checkpoint; no automated gate (legitimacy is operator judgement)</automated>
    <human-check>Operator approves both packages per PyPI page inspection</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed to install, or describe the concern.</resume-signal>
  <done>Operator has approved both [ASSUMED] packages for install.</done>
</task>

<task type="auto">
  <name>Task 2: Add and install yfinance + pandas-market-calendars (SCAN-06, SCAN-04)</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current Phase 1 pins — append, do not reorder existing lines)
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Standard Stack" (exact versions: yfinance==1.4.1, pandas-market-calendars==5.4.0)
  </read_first>
  <action>
    Append a `# Phase 2: Premarket Scanner` comment block to requirements.txt with two
    pinned lines: `yfinance==1.4.1` and `pandas-market-calendars==5.4.0`. Keep all
    existing Phase 1 lines unchanged and in order. Then install them into the active
    environment with `pip install "yfinance==1.4.1" "pandas-market-calendars==5.4.0"`.
    Note: `pd.read_html` (Plan 01) needs an HTML parser; yfinance pulls in `lxml`
    transitively — confirm `python3 -c "import lxml"` succeeds after install, and if it
    does not, add `lxml` to requirements.txt and install it too (do not silently skip).
  </action>
  <verify>
    <automated>python3 -c "import yfinance, pandas_market_calendars, lxml; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yfinance, pandas_market_calendars, lxml"` exits 0.
    - `grep -c '^yfinance==1.4.1$' requirements.txt` returns 1.
    - `grep -c '^pandas-market-calendars==5.4.0$' requirements.txt` returns 1.
    - All Phase 1 requirement lines (moomoo-api, pandas, numpy, jsonschema, structlog) still present.
  </acceptance_criteria>
  <done>Both packages pinned in requirements.txt and importable; lxml available for pd.read_html.</done>
</task>

<task type="auto">
  <name>Task 3: Create bot/scanner package + tests/scanner Wave 0 test stubs</name>
  <files>bot/scanner/__init__.py, tests/scanner/__init__.py, tests/scanner/test_universe.py, tests/scanner/test_fetcher.py, tests/scanner/test_scanner.py, tests/scanner/test_calendar.py</files>
  <read_first>
    - .planning/phases/02-premarket-scanner/02-VALIDATION.md §"Per-Task Verification Map" and §"Wave 0 Requirements" (exact test names and which requirement each maps to)
    - .planning/phases/02-premarket-scanner/02-PATTERNS.md §"tests/scanner/*" entries (analogs: test_migrations.py fixtures, test_indicators.py synthetic DataFrames, test_gateway.py mock injection, test_et_helpers.py known-date assertions)
    - tests/strategy/__init__.py (empty-file analog) and tests/conftest.py (tmp_state_db, minimal_rules fixtures available automatically)
  </read_first>
  <action>
    Create empty `bot/scanner/__init__.py` (zero bytes) and empty
    `tests/scanner/__init__.py` (zero bytes). Create the four test files as RUNNABLE
    stubs — each test function decorated `@pytest.mark.skip(reason="Wave 0 stub — implemented in 02-0N")`
    so the suite stays green but every behavior has a named, collectible test slot.
    Create exactly these test function names (the downstream plans flesh them out):
    - test_universe.py: `test_wikipedia_scrape_returns_symbols`, `test_dot_to_dash_normalization` (BRK.B→BRK-B, BF.B→BF-B), `test_cache_fallback_on_scrape_failure`, `test_raises_when_no_cache_and_scrape_fails`.
    - test_fetcher.py: `test_download_returns_per_ticker_frames`, `test_lowercase_column_normalization`, `test_partial_failure_detected_via_shared_errors`, `test_10pct_failure_raises_ScanDegradationError`, `test_under_10pct_warns_and_proceeds`, `test_shared_errors_cleared_before_download`.
    - test_scanner.py: `test_daily_filters` (SCAN-02), `test_rvol_no_lookahead` (SCAN-03), `test_idempotency` (SCAN-05), `test_top20_cap` (SCAN-08), `test_rescan_idempotent` (SCAN-07), `test_active_candidate_protected` (SCAN-07/D-04), `test_subscribe_top20_only` (SIG-01).
    - test_calendar.py: `test_holiday_rejection` (date(2024,1,1) not a trading day), `test_half_day_close` (date(2023,11,24) close == "13:00"), `test_normal_day_close` (a known regular day close == "16:00"), `test_prior_n_trading_days_excludes_ref_date`.
    Each stub body: a single `pytest.fail("Wave 0 stub")` under the skip marker (skip
    prevents it from failing the suite). Use the project module docstring format from
    02-PATTERNS.md §"Module Docstring Format" on each file.
  </action>
  <verify>
    <automated>python3 -m pytest tests/scanner/ -q --collect-only | grep -c "test_" && python3 -m pytest tests/scanner/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `bot/scanner/__init__.py` and `tests/scanner/__init__.py` exist and are importable packages.
    - `pytest tests/scanner/ --collect-only` collects all 21 named test functions listed above.
    - `pytest tests/scanner/ -q` exits 0 (all skipped, none failed — suite stays green).
    - `pytest tests/ -q` still green (no regression in Phase 1 suites).
    - Every requirement ID SCAN-01..08 and SIG-01 has at least one named test slot (cross-check against 02-VALIDATION.md map).
  </acceptance_criteria>
  <done>bot/scanner package created; all 21 Wave 0 test stubs collectible and skipped; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local env | Untrusted third-party package code installed into the runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | pip install yfinance / pandas-market-calendars | mitigate | Both `[ASSUMED]` per 02-RESEARCH.md; blocking-human legitimacy checkpoint (Task 1) verifies PyPI source repos before Task 2 install; checkpoint is never auto-approvable |
| T-02-01 | Tampering | requirements.txt version drift | mitigate | Exact `==` pins (yfinance==1.4.1, pandas-market-calendars==5.4.0); no range that could pull a future malicious release |
</threat_model>

<verification>
- `python3 -c "import yfinance, pandas_market_calendars, lxml"` exits 0.
- `pytest tests/scanner/ -q` green (21 skipped stubs).
- `pytest tests/ -q` green (no Phase 1 regression).
</verification>

<success_criteria>
- requirements.txt pins yfinance==1.4.1 and pandas-market-calendars==5.4.0; both importable.
- bot/scanner/ is an importable package.
- All 21 Wave 0 test stubs collectible (one slot per SCAN-01..08, SIG-01 behavior).
- Package legitimacy checkpoint approved before install.
</success_criteria>

<output>
Create `.planning/phases/02-premarket-scanner/02-00-SUMMARY.md` when done.
</output>
