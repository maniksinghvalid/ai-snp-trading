# Phase 2 — Artifacts This Phase Produces

> Every symbol Phase 2 creates or extends, so Phase 3+ can consume by reference
> without re-reading source. Generated by the planner alongside the PLAN files.

## New files

| File | Plan | Role |
|------|------|------|
| `bot/scanner/__init__.py` | 02-00 | scanner subpackage init (empty) |
| `bot/scanner/universe.py` | 02-01 | S&P 500 universe fetch + dated cache + fallback |
| `bot/scanner/fetcher.py` | 02-01 | yfinance batch daily-bar download + degradation gate |
| `bot/scanner/calendar.py` | 02-01 | NYSE trading-day / half-day gate |
| `bot/scanner/scanner.py` | 02-02 / 02-03 | run_daily_scan + run_intraday_rescan orchestrators |
| `tests/scanner/__init__.py` + 4 test files | 02-00 | Wave 0 test scaffolds → implemented across 02-01/02/03 |

## New public functions / classes

| Symbol | File | Signature (as planned) |
|--------|------|------------------------|
| `fetch_sp500_symbols` | universe.py | `(cache_dir="data") -> list[str]` (yfinance-format) |
| `wiki_to_yfinance` | universe.py | `(symbol: str) -> str`  (`BRK.B`→`BRK-B`) |
| `yfinance_to_moomoo` | universe.py | `(yf_symbol: str) -> str`  (`BRK-B`→`US.BRK-B`) |
| `download_daily_bars` | fetcher.py | `(yf_symbols, threads=5, degradation_threshold=0.10) -> (data, failed_set)` |
| `get_ticker_frame` | fetcher.py | `(data, symbol) -> Optional[pd.DataFrame]` (lowercase cols, None if absent/all-NaN) |
| `ScanDegradationError` | fetcher.py | `Exception` subclass (>=10% yfinance failures) |
| `is_trading_day` | calendar.py | `(dt: date) -> bool` |
| `get_market_close_et` | calendar.py | `(dt: date) -> str`  ("13:00" half-day / "16:00" normal) |
| `get_prior_n_trading_days` | calendar.py | `(ref_date: date, n: int) -> list` (strictly prior NYSE days) |
| `run_daily_scan` | scanner.py | `(store, gateway, cfg, scan_date=None, scan_pass="premarket") -> list[moomoo_codes]` |
| `run_intraday_rescan` | scanner.py | `(store, gateway, cfg, active_codes: set, scan_date=None, scan_pass="intraday") -> list` |
| `_compute_candidates` (private) | scanner.py | shared filter path for daily + re-scan |
| `_persist_watchlist` (private) | scanner.py | idempotent upsert into daily_scan |

## Extended existing symbols

| Symbol | File | Change |
|--------|------|--------|
| `MoomooGateway.subscribe` | bot/gateway/gateway.py | NEW `async def subscribe(codes, subtypes=None)` — K_5M, run_in_executor, fails closed (SIG-01) |
| `_MIGRATION_0002` | bot/state/migrations.py | NEW migration string (5 ALTER TABLE ADD COLUMN) |
| `MIGRATIONS` | bot/state/migrations.py | appended `_MIGRATION_0002` |
| `CURRENT_VERSION` | bot/state/migrations.py | `1` → `2` |

## New daily_scan columns (migration 0002, D-08)

| Column | Type | Meaning |
|--------|------|---------|
| `prior_day_high` | REAL | prior session high (for Phase 3 D1 context) |
| `prior_close` | REAL | prior session close |
| `sma200` | REAL | 200-day SMA at scan time |
| `rvol_baseline` | REAL | mean volume of prior 14 completed sessions (RVOL denominator) |
| `scan_pass` | TEXT | originating pass: `premarket` \| `intraday` |

(`gap_pct`, `rank`, `scan_date`, `code`, `created_at` already exist from migration 0001.)

## New runtime dependencies (requirements.txt, Plan 02-00)

| Package | Pin |
|---------|-----|
| `yfinance` | `==1.4.1` |
| `pandas-market-calendars` | `==5.4.0` |
| (`lxml`) | transitive via yfinance — for `pd.read_html`; pinned only if not auto-pulled |

## CLI / manual entrypoint

- `run_daily_scan` / `run_intraday_rescan` are the callable entrypoints for manual/test runs.
  **Scheduling them (APScheduler cron) is Phase 5 — not produced here.**
