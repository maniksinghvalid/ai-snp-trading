---
phase: 02-premarket-scanner
plan: 03
type: execute
wave: 4
depends_on: ["02-02"]
files_modified:
  - bot/gateway/gateway.py
  - bot/scanner/scanner.py
  - tests/gateway/test_gateway.py
  - tests/scanner/test_scanner.py
autonomous: true
requirements: [SCAN-07, SIG-01]
user_setup: []

must_haves:
  truths:
    - "MoomooGateway.subscribe(codes, [SubType.K_5M]) wraps the SDK subscribe in run_in_executor and is called only for the capped top-20 list, never the full universe"
    - "run_daily_scan calls gateway.subscribe with exactly the persisted top-20 moomoo codes after persistence"
    - "run_intraday_rescan re-ranks the union of existing + new candidates by gap %, keeps top 20, but never evicts a code in active_codes (D-04)"
    - "An intraday re-scan does not duplicate daily_scan rows (idempotent upsert, D-05) and subscribes only newly-added codes"
  artifacts:
    - path: "bot/gateway/gateway.py"
      provides: "MoomooGateway.subscribe async method"
      contains: "async def subscribe"
    - path: "bot/scanner/scanner.py"
      provides: "run_intraday_rescan, gateway.subscribe wiring in run_daily_scan"
      exports: ["run_intraday_rescan"]
  key_links:
    - from: "bot/scanner/scanner.py"
      to: "MoomooGateway.subscribe"
      via: "subscribe only the capped top-20 codes"
      pattern: "\\.subscribe\\("
    - from: "bot/gateway/gateway.py"
      to: "moomoo SubType.K_5M"
      via: "run_in_executor-wrapped quote_ctx.subscribe"
      pattern: "K_5M"
    - from: "bot/scanner/scanner.py"
      to: "active_codes protection (D-04)"
      via: "union active_codes into top-20 before cap"
      pattern: "active_codes"
---

<objective>
Complete the scanner: add `MoomooGateway.subscribe()` (K_5M, run_in_executor pattern,
SIG-01), wire it into `run_daily_scan` so the bot subscribes ONLY the capped top-20
codes, and add `run_intraday_rescan` — the idempotent re-scan entrypoint that re-ranks
the union of existing + newly-qualifying candidates by gap % while NEVER evicting an
already-active candidate (D-04/D-05, SCAN-07). This finishes Phase 2's end-to-end data
path: universe → bars → filter → top-20 persist → live 5m subscription.

Purpose: SIG-01 keeps live subscriptions inside Moomoo quota (20 of 100 slots); D-04
protects a live 5m feed from being churned out by a marginally-higher-gap newcomer.
Scheduling these entrypoints is Phase 5 — this plan delivers the callable entrypoints only.
Output: gateway.subscribe(), run_daily_scan subscribe wiring, run_intraday_rescan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-premarket-scanner/02-CONTEXT.md
@.planning/phases/02-premarket-scanner/02-RESEARCH.md
@.planning/phases/02-premarket-scanner/02-PATTERNS.md
@.planning/phases/02-premarket-scanner/02-VALIDATION.md
@.planning/phases/02-premarket-scanner/02-02-SUMMARY.md
@bot/gateway/gateway.py
@bot/scanner/scanner.py
@tests/gateway/test_gateway.py
@tests/scanner/test_scanner.py
@skills/moomooapi/scripts/subscribe/subscribe.py
---
# Interfaces this plan consumes/extends (exact):
# - Existing gateway helpers (reuse): _check_ret(ret, data, action) raises GatewayError on ret != RET_OK
# - Existing async pattern: loop = asyncio.get_event_loop(); await loop.run_in_executor(None, fn)
# - moomoo SDK (deferred import inside method): from moomoo import SubType, Session
#     ctx.subscribe(code_list, [SubType.K_5M], is_first_push=True, subscribe_push=True,
#                   extended_time=False, session=Session.NONE) -> (ret, msg)
# - run_daily_scan(store, gateway, cfg, scan_date, scan_pass) -> list[moomoo_codes]  (Plan 02-02; add subscribe call)
# - _persist_watchlist(conn, scan_date, candidates, scan_pass)  (Plan 02-02; reuse for re-scan)
# - daily_scan UNIQUE(scan_date, code); columns incl. gap_pct, rank, scan_pass (migration 0002)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MoomooGateway.subscribe() — K_5M run_in_executor wrapper (SIG-01)</name>
  <files>bot/gateway/gateway.py, tests/gateway/test_gateway.py</files>
  <read_first>
    - .planning/phases/02-premarket-scanner/02-PATTERNS.md §"bot/gateway/gateway.py — add subscribe() method" (exact method body, deferred SubType/Session import, _check_ret reuse, _logger.info event)
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Research Area 6" (subscribe signature, quota tiers, 60s hold)
    - bot/gateway/gateway.py lines 282-299 (get_acc_list/get_positions run_in_executor analog) and lines 172-182 (_check_ret)
    - tests/gateway/test_gateway.py lines 41-65 (_make_gateway_with_mocks factory; mock _quote_ctx)
    - skills/moomooapi/scripts/subscribe/subscribe.py (reference subscribe() call signature)
  </read_first>
  <behavior>
    - test_subscribe_wraps_executor: gateway with a mock _quote_ctx whose subscribe returns (RET_OK, "") → await gw.subscribe(["US.AAPL"]) calls quote_ctx.subscribe once with [SubType.K_5M] and is_first_push/subscribe_push True.
    - test_subscribe_defaults_to_k5m: calling subscribe without subtypes uses [SubType.K_5M].
    - test_subscribe_raises_on_non_ret_ok: quote_ctx.subscribe returns (non-zero, "err") → raises GatewayError.
  </behavior>
  <action>
    Insert `async def subscribe(self, codes: list, subtypes: list = None) -> None` into the
    "Async Wrappers" section of bot/gateway/gateway.py (after get_positions). Body: deferred
    `from moomoo import SubType, Session` inside the method (matches the gateway's SDK
    deferred-import convention so tests run without a live SDK call path); default
    `subtypes = [SubType.K_5M]` when None; `loop = asyncio.get_event_loop()`; define
    `_subscribe_blocking()` calling `self._quote_ctx.subscribe(codes, subtypes,
    is_first_push=True, subscribe_push=True, extended_time=False, session=Session.NONE)`,
    capturing `(ret, msg)`, then `_check_ret(ret, msg, "subscribe")`; `await
    loop.run_in_executor(None, _subscribe_blocking)`; finish with `_logger.info("subscribed_k5m",
    codes=codes, count=len(codes))`. Do NOT subscribe the full universe — callers pass the
    capped top-20 (enforced by run_daily_scan in Task 2). Add the three gateway tests using
    the existing `_make_gateway_with_mocks` helper (extend it or wire `gw._quote_ctx.subscribe`).
  </action>
  <verify>
    <automated>python3 -m pytest tests/gateway/test_gateway.py -k subscribe -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The three subscribe tests pass.
    - `grep -c 'async def subscribe' bot/gateway/gateway.py` == 1.
    - `grep -c 'SubType.K_5M' bot/gateway/gateway.py` >= 1.
    - subscribe uses run_in_executor (non-blocking) and raises GatewayError on non-RET_OK via _check_ret.
    - Existing gateway tests still pass (no change to connect/reconcile paths).
  </acceptance_criteria>
  <done>Gateway exposes an async subscribe() that wraps quote_ctx.subscribe for K_5M in run_in_executor and fails closed on non-RET_OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire gateway.subscribe into run_daily_scan — top-20 only (SIG-01)</name>
  <files>bot/scanner/scanner.py, tests/scanner/test_scanner.py</files>
  <read_first>
    - bot/scanner/scanner.py (run_daily_scan from Plan 02-02 — the marked subscribe seam after persistence)
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Research Area 7" (asyncio.run bridge for sync scanner calling async gateway.subscribe) and §"Module Decomposition"
    - .planning/phases/02-premarket-scanner/02-PATTERNS.md §"bot/scanner/scanner.py" (asyncio.run bridge note — run_daily_scan stays sync)
    - tests/scanner/test_scanner.py (test_subscribe_top20_only stub to flesh out; mock gateway)
  </read_first>
  <behavior>
    - test_subscribe_top20_only: run_daily_scan with 25 passing candidates and a mock gateway → gateway.subscribe called exactly once with a list of length 20 (the persisted top-20 moomoo codes), NOT the ~500 universe.
    - test_subscribe_skipped_when_empty: a non-trading-day or empty watchlist → gateway.subscribe is NOT called.
  </behavior>
  <action>
    At the marked seam in `run_daily_scan` (after `_persist_watchlist`), call the async
    gateway.subscribe with the top-20 moomoo code list using the asyncio bridge:
    `asyncio.run(gateway.subscribe(top20_codes))` (run_daily_scan remains sync per 02-PATTERNS.md;
    do NOT convert it to async). Guard: only call subscribe when `top20_codes` is non-empty
    AND gateway is not None (tests inject a mock gateway; a None gateway path logs and skips —
    supports unit tests that exercise filtering without a broker). Pass ONLY the capped list —
    assert in code intent via a comment referencing SIG-01; the test enforces len==20. Flesh out
    test_subscribe_top20_only and add test_subscribe_skipped_when_empty with an AsyncMock gateway.
  </action>
  <verify>
    <automated>python3 -m pytest tests/scanner/test_scanner.py -k "subscribe_top20 or subscribe_skipped" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_subscribe_top20_only and test_subscribe_skipped_when_empty pass.
    - gateway.subscribe is called with a list of exactly 20 codes when >20 pass; never with the full universe.
    - subscribe is not called on an empty/non-trading-day result.
    - `grep -c '\.subscribe(' bot/scanner/scanner.py` >= 1 and the call site passes the capped list (read-verify it is top20, not the symbols list).
  </acceptance_criteria>
  <done>run_daily_scan subscribes exactly the persisted top-20 codes via gateway.subscribe after persistence, and skips subscription when there is nothing to subscribe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: run_intraday_rescan — idempotent merge, protect active candidates (SCAN-07, D-04/D-05)</name>
  <files>bot/scanner/scanner.py, tests/scanner/test_scanner.py</files>
  <read_first>
    - .planning/phases/02-premarket-scanner/02-RESEARCH.md §"Research Area 5 / Protecting Active Candidates (D-04)" and §"Research Area 7 / run_intraday_rescan" and §"Common Pitfalls 6"
    - .planning/phases/02-premarket-scanner/02-CONTEXT.md §Decisions D-04/D-05 (re-rank union by gap%, keep top 20, never evict an active/subscribed candidate; idempotency via UNIQUE + upsert)
    - bot/scanner/scanner.py (run_daily_scan + _persist_watchlist from Plan 02-02 to reuse)
    - tests/scanner/test_scanner.py (test_rescan_idempotent, test_active_candidate_protected stubs)
  </read_first>
  <behavior>
    - test_rescan_idempotent: run a premarket scan, then run_intraday_rescan for the same scan_date with the same/overlapping candidates → daily_scan row count does not grow for already-present codes (upsert); ranks may change.
    - test_active_candidate_protected: an active_code that ranks 22nd on re-rank is STILL kept in the persisted top-20 (it is not evicted); a non-active newcomer at rank 21 is excluded. The protected set = union(active_codes, top-by-gap) capped at 20 with active codes guaranteed in.
    - test_rescan_subscribes_only_new: re-scan subscribes only codes not already in active_codes (no re-subscribe of live feeds).
  </behavior>
  <action>
    Implement `run_intraday_rescan(store, gateway, cfg, active_codes: set, scan_date: date =
    None, scan_pass: str = "intraday") -> list`. Reuse the run_daily_scan computation path
    (factor the shared "compute candidates" logic into a private `_compute_candidates(cfg,
    scan_date) -> list` if not already extracted, so daily and re-scan share one code path —
    do NOT duplicate the filter logic). Re-rank the union of newly-qualifying candidates by
    gap_pct DESC. Build the protected top-20 (D-04): start with all codes in `active_codes`
    that still qualify, then fill remaining slots (up to 20 total) with the highest-gap
    non-active candidates; never DELETE rows for the scan_date — upsert only via
    _persist_watchlist (D-05). Assign rank over the final protected list. Subscribe (asyncio.run
    bridge) ONLY the codes in the final list that are NOT already in active_codes (avoid
    re-subscribing live feeds). Return the final moomoo code list. Flesh out
    test_rescan_idempotent, test_active_candidate_protected, and add test_rescan_subscribes_only_new.
  </action>
  <verify>
    <automated>python3 -m pytest tests/scanner/test_scanner.py -k "rescan" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_rescan_idempotent, test_active_candidate_protected, test_rescan_subscribes_only_new pass.
    - An active_code ranking below 20 by gap is still present in the persisted/returned top-20 (D-04).
    - Re-scan never issues DELETE on daily_scan; row count for the day does not grow for existing codes (D-05).
    - Re-scan subscribes only codes absent from active_codes.
    - Filter logic is shared with run_daily_scan (one _compute_candidates path; no duplicated D1/D2/D3 code).
  </acceptance_criteria>
  <done>run_intraday_rescan re-ranks the union by gap%, guarantees active candidates stay in the top-20, upserts idempotently, and subscribes only new codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner.py → MoomooGateway (broker) | Subscription requests cross into broker quota/OpenD |
| re-scan merge → daily_scan | Re-ranked rows written; active-feed protection enforced in app logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial of Service | over-subscription beyond quota | mitigate | run_daily_scan/run_intraday_rescan pass ONLY the capped top-20 to gateway.subscribe (SIG-01); test asserts len==20, never the full universe (Pitfall #11 / API_LIMITS quota) |
| T-02-11 | Integrity | active live-feed candidate churned out on re-scan | mitigate | D-04 active_codes union is guaranteed into the top-20 before the cap; re-scan never DELETEs rows; test_active_candidate_protected proves a sub-20-rank active code is retained |
| T-02-12 | Tampering | re-scan duplicates / corrupts watchlist rows | mitigate | D-05 idempotent parameterized upsert on UNIQUE(scan_date, code); test_rescan_idempotent proves no row growth for existing codes |
| T-02-13 | Spoofing/Tampering | non-RET_OK subscribe silently ignored | mitigate | gateway.subscribe fails closed via _check_ret → GatewayError; caller surfaces it (no silent empty subscription) |
</threat_model>

<verification>
- `pytest tests/scanner/test_scanner.py tests/gateway/test_gateway.py -q` green.
- `pytest tests/ -q` full suite green (Phase 2 complete; all 21 scanner stubs now implemented).
- subscribe called only with the top-20; active candidates protected on re-scan; re-scan idempotent.
</verification>

<success_criteria>
- MoomooGateway.subscribe() wraps K_5M subscribe in run_in_executor, fails closed on non-RET_OK.
- run_daily_scan subscribes exactly the capped top-20 (SIG-01), never the universe.
- run_intraday_rescan re-ranks by gap%, protects active candidates (D-04), upserts idempotently (D-05), subscribes only new codes.
</success_criteria>

<output>
Create `.planning/phases/02-premarket-scanner/02-03-SUMMARY.md` when done.
</output>
