---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - bot/safety/et_helpers.py
  - bot/safety/logger.py
  - bot/safety/kill_switch.py
  - tests/safety/test_et_helpers.py
  - tests/safety/test_logger.py
  - tests/safety/test_kill_switch.py
autonomous: true
requirements: [SAFE-04, SAFE-05, SVC-03, SVC-04]
must_haves:
  truths:
    - "ET helpers return DST-correct America/New_York aware datetimes via zoneinfo — never naive datetimes or fixed UTC offsets (SVC-04)."
    - "structlog is configured to emit structured, JSON-ready events to a rotating file plus a readable stderr stream, preserving [WARN]/[ERROR] severity (SVC-03)."
    - "The kill switch triggers on both a sentinel file-touch and SIGINT, sets a shutdown Event, logs a structured shutdown line, and invokes a registered state-flush callback (SAFE-04)."
    - "Shutdown records an append-only JSONL audit entry without overwriting prior entries (SAFE-05, reuses 01-01 audit_log)."
  artifacts:
    - path: "bot/safety/et_helpers.py"
      provides: "zoneinfo ET helpers: now_et, to_et, is_within_entry_window-ready primitives"
      exports: ["ET", "now_et", "to_et"]
      contains: "America/New_York"
    - path: "bot/safety/logger.py"
      provides: "structlog rotating-file + stderr configuration"
      exports: ["configure_logging", "get_logger"]
      contains: "structlog"
    - path: "bot/safety/kill_switch.py"
      provides: "KillSwitch: sentinel-file + SIGINT → shutdown Event + state-flush callback"
      exports: ["KillSwitch"]
      contains: "SIGINT"
  key_links:
    - from: "bot/safety/kill_switch.py"
      to: "signal.SIGINT"
      via: "signal.signal(SIGINT, handler)"
      pattern: "SIGINT"
    - from: "bot/safety/kill_switch.py"
      to: "bot/safety/audit_log.py"
      via: "append_audit shutdown event on trigger"
      pattern: "append_audit"
    - from: "bot/safety/logger.py"
      to: "structlog"
      via: "configure_logging wires rotating file + renderer"
      pattern: "structlog"
---

<objective>
Build the remaining foundation safety primitives: zoneinfo-based US Eastern time helpers (SVC-04), a structlog rotating-file + stderr logging configuration (SVC-03), and a kill switch (SAFE-04) that triggers on both a sentinel file-touch and SIGINT, sets a shutdown Event, logs a structured shutdown line, runs a registered state-flush callback, and records the shutdown to the append-only JSONL audit log (SAFE-05).

Purpose: Correct ET/DST handling, structured logs, and a clean kill-switch + state-flush are cross-cutting infrastructure every later phase relies on. Getting timezone correctness and graceful shutdown right from day one prevents the DST and "crash loses state" failure classes called out in PITFALLS.
Output: ET helpers, configured structured logging, and a tested kill switch wired to shutdown + state flush + audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files this plan creates (exclude from drift-verification as pre-existing):
- `bot/safety/et_helpers.py`: `ET` (ZoneInfo), `now_et()`, `to_et(dt)`
- `bot/safety/logger.py`: `configure_logging(log_dir=..., level=...)`, `get_logger(name=None)`
- `bot/safety/kill_switch.py`: `KillSwitch` (sentinel path, SIGINT handler, shutdown `threading.Event`, `register_flush(cb)`, `triggered`)
- Test files: `tests/safety/test_et_helpers.py`, `tests/safety/test_logger.py`, `tests/safety/test_kill_switch.py`
Note: `bot/safety/__init__.py` and `bot/safety/audit_log.py` are created by 01-01 — this plan imports `append_audit`, it does not recreate it.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: zoneinfo ET helpers (SVC-04) + structlog rotating logger (SVC-03)</name>
  <files>bot/safety/et_helpers.py, bot/safety/logger.py, tests/safety/test_et_helpers.py, tests/safety/test_logger.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (§"bot/safety/et_helpers.py" — `ET = ZoneInfo("America/New_York")`, never fixed offsets/pytz; §"bot/safety/logger.py" — structlog config, preserve [WARN]/[ERROR] semantics, section-header comment style)
    - .planning/research/PITFALLS.md (§Pitfall 6 "Timezone Naivety" — always aware datetimes, test across DST transitions; §Security "mask credentials in logs")
    - .planning/research/STACK.md (structlog 26.1.0, zoneinfo stdlib; §"Telegram one-way alert" NOT in this phase)
    - .planning/phases/01-foundation/01-CONTEXT.md (Claude's Discretion: structlog rotating-file JSON-ready contextvars-aware; zoneinfo ET DST-correct, no naive datetimes)
  </read_first>
  <action>
    Create `bot/safety/et_helpers.py` defining `ET = ZoneInfo("America/New_York")`, `now_et() -> datetime` (returns `datetime.now(tz=ET)` — aware, never naive), and `to_et(dt) -> datetime` (converts any aware datetime to ET; rejects/locale-izes naive input explicitly rather than assuming UTC). Never use a fixed UTC offset or pytz (PITFALLS #6). Create `bot/safety/logger.py` defining `configure_logging(log_dir="logs", level="INFO")` that configures structlog (26.1.0) with: a `logging.handlers.RotatingFileHandler` writing JSON-rendered events to a rotating file under `log_dir`, a readable ConsoleRenderer to stderr, contextvars support (works across the asyncio boundary), and a processor chain that includes a timestamp and level. Preserve the existing `[WARN]`/`[ERROR]` severity distinction from common.py (PATTERNS.md §logger). Add `get_logger(name=None)` returning a bound structlog logger. Ensure no credentials are ever logged (do not log FutuConfig/passwords — PITFALLS §Security). Write `tests/safety/test_et_helpers.py`: assert `now_et().tzinfo` is the ET zone and the result is aware (`utcoffset()` is not None); assert a DST-summer date (e.g. 2026-07-01 12:00) converts with offset -4h and a DST-winter date (e.g. 2026-01-01 12:00) with -5h (proves DST-correctness, not a fixed offset). Write `tests/safety/test_logger.py`: call `configure_logging` against a tmp_path log dir, emit an info and a warning event via `get_logger`, and assert the rotating file is created and contains the JSON-rendered events with correct level fields.
  </action>
  <verify>
    <automated>python -m pytest tests/safety/test_et_helpers.py tests/safety/test_logger.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `bot/safety/et_helpers.py` contains `America/New_York` via `ZoneInfo` and defines `now_et` and `to_et`; `grep -n "pytz\|timedelta(hours=" bot/safety/et_helpers.py` returns nothing (no fixed offsets, no pytz).
    - `now_et()` returns an aware datetime (`.tzinfo` is the ET zone, `.utcoffset()` is not None).
    - A July ET datetime has offset -4h (EDT) and a January ET datetime has offset -5h (EST) — test asserts both, proving DST handling.
    - `bot/safety/logger.py` defines `configure_logging` and `get_logger`, imports `structlog`, and configures a `RotatingFileHandler`.
    - After `configure_logging(tmp_log_dir)` and emitting events, a log file exists under the dir and contains the rendered events with `info`/`warning` levels (test asserts).
    - `python -m pytest tests/safety/test_et_helpers.py tests/safety/test_logger.py -q` exits 0.
  </acceptance_criteria>
  <done>ET helpers produce DST-correct aware datetimes via zoneinfo (SVC-04) and structlog is configured for rotating-file JSON + readable stderr output preserving severity (SVC-03), both unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: KillSwitch — sentinel file + SIGINT → shutdown Event + state flush + audit (SAFE-04/SAFE-05)</name>
  <files>bot/safety/kill_switch.py, tests/safety/test_kill_switch.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (§"bot/safety/kill_switch.py" — file-sentinel check + `signal.signal(signal.SIGINT, handler)`; on trigger set a threading.Event, log structured shutdown, flush state; sentinel path configurable)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-discretion: kill switch via file-touch AND SIGINT → graceful shutdown + state flush; SAFE-04; SAFE-05 audit append-only)
    - .planning/research/PITFALLS.md (§"Looks Done But Isn't" — startup state recovery / graceful shutdown; §Anti-Pattern: never swallow shutdown silently)
    - bot/safety/audit_log.py (append_audit — created in 01-01; import and reuse for the shutdown audit entry)
    - bot/safety/logger.py (get_logger — for the structured shutdown line)
  </read_first>
  <action>
    Create `bot/safety/kill_switch.py` defining `KillSwitch`. Constructor takes a configurable `sentinel_path` (default from an env var, e.g. `BOT_KILL_FILE`, else a sensible default) and exposes a `threading.Event` accessed via a `triggered` property / `is_set()`-style check. Provide `register_flush(callback)` to register the state-flush callback (the StateStore flush is wired in a later phase; this phase accepts any callable). Provide `install()` that registers `signal.signal(signal.SIGINT, self._handle_signal)`. Provide `check_file()` that returns True if the sentinel file exists (the orchestration loop in a later phase will poll this). Both trigger paths (`_handle_signal` for SIGINT and a `trigger(reason)` method invoked when the sentinel is detected) converge on a single `_trigger(reason)` that: sets the shutdown Event, emits a structured shutdown log line via `get_logger` (SAFE-04 graceful shutdown), invokes every registered flush callback (state flush), and writes an append-only audit entry via `append_audit({"event": "kill_switch", "reason": reason, ...})` (SAFE-05 — reuses the 01-01 audit log, append-only, never overwrites). Ensure `_trigger` is idempotent (a second trigger does not double-flush). Write `tests/safety/test_kill_switch.py`: (a) creating the sentinel file makes `check_file()` True and calling `trigger`/the poll path sets the Event, runs the registered flush callback (assert a sentinel/mock was called), and appends a `kill_switch` audit entry (against a tmp audit path); (b) simulate SIGINT by invoking the installed handler directly (call `_handle_signal(signal.SIGINT, None)`) and assert the same Event-set + flush + audit behavior; (c) assert idempotency — triggering twice flushes once.
  </action>
  <verify>
    <automated>python -m pytest tests/safety/test_kill_switch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `bot/safety/kill_switch.py` defines `class KillSwitch`, references `signal.SIGINT`, and reads a configurable sentinel path (default overridable via env var).
    - Touching the sentinel file makes `check_file()` return True; triggering sets the shutdown `threading.Event`.
    - A registered flush callback is invoked exactly once on trigger (test asserts call count == 1 even across two trigger calls — idempotent).
    - Triggering writes an append-only audit entry with `event == "kill_switch"` and a `reason` (verified against a tmp audit path; prior audit lines preserved).
    - Invoking the SIGINT handler directly produces the same Event-set + flush + audit behavior as the file path (test asserts both paths).
    - `bot/safety/kill_switch.py` imports `append_audit` from `bot/safety/audit_log.py` (does not redefine it).
    - `python -m pytest tests/safety/test_kill_switch.py -q` exits 0.
  </acceptance_criteria>
  <done>The kill switch fires on both file-touch and SIGINT, sets a shutdown Event, emits a structured shutdown log, runs the state-flush callback once (idempotent), and records the shutdown to the append-only JSONL audit log (SAFE-04 + SAFE-05, success criterion 6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS signal / sentinel file → bot | SIGINT or a file-touch is the operator's shutdown command; must produce a clean, audited stop |
| host clock / timezone → strategy timing | host TZ is an invisible assumption; ET conversion must be DST-correct |
| bot → log files | logs must not leak credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Denial of Service | kill_switch / state flush | mitigate | Graceful shutdown sets a shutdown Event and runs the state-flush callback before exit (SAFE-04); idempotent trigger prevents a double-flush race. A clean stop avoids leaving positions unmanaged with corrupt state. |
| T-01-13 | Tampering | shutdown audit entry | mitigate | Shutdown writes an append-only `kill_switch` JSONL entry (SAFE-05) so every stop is recorded immutably; prior entries preserved. |
| T-01-14 | Repudiation / Correctness | et_helpers timezone | mitigate | zoneinfo America/New_York with DST-verified offsets (-4h summer / -5h winter); no fixed offset or pytz (PITFALLS #6) — prevents force-close/entry-window firing at the wrong real clock time. |
| T-01-15 | Information Disclosure | logger | mitigate | Logging config never logs FutuConfig/credentials; tokens/passwords masked per PITFALLS §Security. |
</threat_model>

<verification>
- `python -m pytest tests/safety/test_et_helpers.py tests/safety/test_logger.py tests/safety/test_kill_switch.py -q` exits 0.
- `grep -n "pytz\|timedelta(hours=" bot/safety/et_helpers.py` returns nothing (zoneinfo only).
- `grep -n "SIGINT" bot/safety/kill_switch.py` and `grep -n "append_audit" bot/safety/kill_switch.py` both match.
- Kill switch trigger is idempotent (single flush) and audited append-only.
</verification>

<success_criteria>
- ET helpers are DST-correct zoneinfo aware datetimes — no naive datetimes or fixed UTC offsets (SVC-04).
- structlog rotating-file + stderr logging is configured and emits structured JSON-ready events (SVC-03).
- Kill switch (file-touch and SIGINT) triggers a clean shutdown with a state flush, recorded to the append-only JSONL audit log without overwriting prior entries (SAFE-04 + SAFE-05, success criterion 6).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
