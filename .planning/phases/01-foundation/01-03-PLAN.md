---
phase: 01-foundation
plan: 03
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - rules.json
  - bot/config/__init__.py
  - bot/config/schema.py
  - bot/config/loader.py
  - bot/strategy/__init__.py
  - bot/strategy/indicators.py
  - bot/strategy/core.py
  - bot/strategy/trend_join_long.py
  - tests/config/__init__.py
  - tests/config/test_loader.py
  - tests/strategy/__init__.py
  - tests/strategy/test_indicators.py
  - tests/strategy/test_config_driven.py
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "rules.json at repo root contains the canonical Trend Join Long parameters and loads + jsonschema-validates at startup; malformed or missing config fails fast with a clear ConfigError (D-discretion: jsonschema)."
    - "Indicator functions (SMA200, RVOL with prior-completed-days cutoff, swing_low_2_2, D1/D2/D3 filters) compute correctly against synthetic DataFrames with zero network/I/O."
    - "RVOL lookback uses only strictly-prior completed trading days — no row with date >= the signal date appears in the denominator (PITFALLS #4 no look-ahead)."
    - "No strategy literal is hardcoded in StrategyCore/TrendJoinLong — a behavioral test swaps baseline vs modified rules.json and observes filter/indicator outputs change accordingly (D-12)."
  artifacts:
    - path: "rules.json"
      provides: "canonical CFG-01 strategy config (single source of truth)"
      contains: "Trend Join Long"
    - path: "bot/config/loader.py"
      provides: "load_strategy_config -> StrategyConfig; fail-fast ConfigError on missing/malformed"
      exports: ["load_strategy_config", "StrategyConfig", "ConfigError"]
      contains: "def load_strategy_config("
    - path: "bot/strategy/indicators.py"
      provides: "pure indicator functions: sma, rvol, swing_low_2_2"
      exports: ["sma", "rvol", "swing_low_2_2"]
    - path: "bot/strategy/core.py"
      provides: "StrategyCore ABC with abstract filter/stop hooks"
      exports: ["StrategyCore"]
      contains: "class StrategyCore"
    - path: "bot/strategy/trend_join_long.py"
      provides: "TrendJoinLong(StrategyCore) reading every param from StrategyConfig"
      exports: ["TrendJoinLong"]
      contains: "class TrendJoinLong"
  key_links:
    - from: "bot/config/loader.py"
      to: "jsonschema.validate"
      via: "validate rules.json against bot/config/schema.py SCHEMA at load"
      pattern: "validate"
    - from: "bot/strategy/trend_join_long.py"
      to: "StrategyConfig"
      via: "all D1/D2/D3 + intraday + exit params read from cfg, never literals"
      pattern: "self\\._cfg\\."
---

<objective>
Build the pure strategy layer: `rules.json` (CFG-01, repo root) as the single source of truth, a fail-fast jsonschema loader/validator mapping it to a `StrategyConfig` dataclass, the `StrategyCore` ABC, the `TrendJoinLong` concrete implementation reading every parameter from config, and the pure indicator functions (SMA200, RVOL with no look-ahead, swing_low_2_2). All logic is I/O-free and unit-tested against synthetic DataFrames.

Purpose: This is the brain the live bot and the Phase 6 backtester share unchanged. Externalizing every constant to `rules.json` (CFG-01) means the strategy can change without code edits, and proving config-drivenness behaviorally (swap config, observe output change) guarantees no literal silently overrides the file.
Output: A loadable, validated config; an ABC + concrete strategy with zero hardcoded strategy constants; correct, look-ahead-free indicators — all green under pytest with no network.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<artifacts_this_phase_produces>
Symbols and files this plan creates (exclude from drift-verification as pre-existing):
- New packages: `bot/config/` (`__init__.py`), `bot/strategy/` (`__init__.py`), `tests/config/` (`__init__.py`), `tests/strategy/` (`__init__.py`)
- `rules.json` (repo root)
- `bot/config/schema.py`: `SCHEMA` (jsonschema dict)
- `bot/config/loader.py`: `load_strategy_config`, `StrategyConfig` (dataclass), `ConfigError`
- `bot/strategy/indicators.py`: `sma`, `rvol`, `swing_low_2_2`
- `bot/strategy/core.py`: `StrategyCore` (ABC) with abstract methods `passes_daily_filters`, `passes_intraday_filters`, `compute_initial_stop`, `compute_swing_low_2_2`
- `bot/strategy/trend_join_long.py`: `TrendJoinLong`
- Test files: `tests/config/test_loader.py`, `tests/strategy/test_indicators.py`, `tests/strategy/test_config_driven.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rules.json + jsonschema loader/validator → StrategyConfig (CFG-01)</name>
  <files>rules.json, bot/config/__init__.py, bot/config/schema.py, bot/config/loader.py, tests/config/__init__.py, tests/config/test_loader.py</files>
  <read_first>
    - .planning/PROJECT.md (§"The Strategy — Trend Join Long" — the EXACT canonical JSON block; rules.json must reproduce it verbatim as the runtime config)
    - .planning/phases/01-foundation/01-PATTERNS.md (§"bot/config/loader.py" — env-var/get_config analog adapted to a file source; fail-fast early-exit style raising a project ConfigError)
    - .planning/phases/01-foundation/01-CONTEXT.md (Claude's Discretion: rules.json at repo root, validated with jsonschema, malformed/missing fails fast with clear error; D-12 behavioral config-drivenness)
    - .planning/research/PITFALLS.md (§Security "config injection via malformed rules.json" rationale — validate before use)
  </read_first>
  <behavior>
    - Test: `load_strategy_config("rules.json")` returns a `StrategyConfig` whose fields equal the canonical PROJECT.md values (min_price_usd=3.0, D3 gap=3.0, rvol_min=2.0, rvol_lookback_days=14, entry window 10:05/15:30, force_close 15:51, partial_trigger_R=0.75, partial_fraction≈0.3333, breakeven_trigger_R=1.0, max_risk_per_trade_pct=1.0, max_position_size_pct=10, max_concurrent_positions=5, max_trades_per_day=5).
    - Test: a missing file raises `ConfigError` with a message naming the path.
    - Test: malformed JSON (syntactically invalid) raises `ConfigError`.
    - Test: schema-invalid JSON (e.g. `rvol_min` as a string, or a required key removed) raises `ConfigError` mentioning the failing field (jsonschema validation, not a silent default).
  </behavior>
  <action>
    Create `rules.json` at repo root reproducing the canonical strategy JSON from PROJECT.md §"The Strategy" verbatim (CFG-01 single source of truth). Create `bot/config/schema.py` defining `SCHEMA` — a jsonschema dict that requires every top-level group (universe_filters, daily_filters, intraday_filters, time_filter, exit, risk) and types each field (numbers as number, the time strings as string), with `required` lists so a removed key fails validation. Create `bot/config/loader.py` defining `StrategyConfig` (a dataclass with typed fields mirroring the rules.json keys — flatten nested groups into clearly named fields, e.g. `min_price_usd`, `d3_min_gap_pct`, `rvol_min`, `rvol_lookback_days`, `earliest_entry_et`, `latest_entry_et`, `force_close_et`, `partial_profit_trigger_r`, `partial_profit_fraction`, `breakeven_trigger_r`, `max_risk_per_trade_pct`, `max_position_size_pct`, `max_concurrent_positions`, `max_trades_per_day`), `ConfigError(Exception)`, and `load_strategy_config(path="rules.json") -> StrategyConfig`. The loader: reads the file (raise `ConfigError` naming the path on `FileNotFoundError`), parses JSON (raise `ConfigError` on `json.JSONDecodeError`), runs `jsonschema.validate(data, SCHEMA)` (raise `ConfigError` with the validation message on `ValidationError`), then maps the validated dict into `StrategyConfig`. Per PATTERNS.md "Raise, don't sys.exit": the loader RAISES ConfigError; the future main() exits. Write `tests/config/test_loader.py` covering the four behaviors above.
  </action>
  <verify>
    <automated>python -m pytest tests/config/test_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rules.json` exists at repo root and contains `"strategy_name": "Trend Join Long"` and all six parameter groups from PROJECT.md.
    - `bot/config/loader.py` contains `def load_strategy_config(`, `class StrategyConfig`, `class ConfigError`.
    - `load_strategy_config("rules.json")` returns a StrategyConfig whose values equal the canonical PROJECT.md values (test asserts each field).
    - Missing file → `ConfigError` naming the path; malformed JSON → `ConfigError`; schema-invalid JSON → `ConfigError` mentioning the failing field (three parametrized cases).
    - `grep -n "jsonschema" bot/config/loader.py` (or schema import) confirms validation is wired; `grep -n "sys.exit" bot/config/loader.py` returns nothing.
    - `python -m pytest tests/config/test_loader.py -q` exits 0.
  </acceptance_criteria>
  <done>rules.json is the canonical CFG-01 config, loaded and jsonschema-validated into a typed StrategyConfig; missing/malformed/schema-invalid configs fail fast with a clear ConfigError naming the problem (success criterion 4, fail-fast half).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure indicators — SMA200, RVOL (no look-ahead), swing_low_2_2</name>
  <files>bot/strategy/__init__.py, bot/strategy/indicators.py, tests/strategy/__init__.py, tests/strategy/test_indicators.py</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (§Pattern 3 — `compute_swing_low_2_2` reference: pivot low at index i requires low[i] < low[i-1],low[i-2] AND low[i] < low[i+1],low[i+2]; `compute_initial_stop` = lod*0.99)
    - .planning/research/PITFALLS.md (§Pitfall 4 "RVOL Look-Ahead Bias" — denominator strictly prior 14 COMPLETED trading days, no current-day row; add a test asserting no denominator row has date >= signal date)
    - .planning/phases/01-foundation/01-PATTERNS.md (§"bot/strategy/indicators.py" — pure pandas, no I/O; safe_float reuse)
    - .planning/PROJECT.md (§"The Strategy" — SMA200, RVOL 14-day lookback, swing_low_5m_2_2 definitions)
  </read_first>
  <behavior>
    - Test (sma): `sma(series, 200)` on a known synthetic series equals the hand-computed mean of the trailing 200 closes; returns NaN/None when fewer than 200 points.
    - Test (rvol): `rvol(daily_volume_df, signal_date, lookback_days=14)` divides current-day (or candidate) volume by the mean of the 14 strictly-prior completed sessions; assert NO row with `date >= signal_date` is included in the denominator (look-ahead guard) — feed a frame where today's volume is huge and confirm it does not depress the denominator.
    - Test (swing_low_2_2): on a synthetic 5m low array with a clean pivot (2 down, 2 up around index i), returns that pivot low; returns None when there are fewer than 5 bars or no confirmed pivot.
  </behavior>
  <action>
    Create `bot/strategy/indicators.py` with PURE functions (no I/O, no network): `sma(series, period)` (trailing mean; insufficient data → NaN/None), `rvol(volume_frame, signal_date, lookback_days)` (denominator = mean volume over the strictly-prior `lookback_days` COMPLETED sessions, i.e. rows with date `<` signal_date — implement the cutoff explicitly per PITFALLS #4; never include the signal date), and `swing_low_2_2(lows)` (2-bar-left / 2-bar-right pivot-low confirmation per ARCHITECTURE.md §Pattern 3, returning the pivot low or None). Reuse `safe_float` from `bot/_utils.py` for null-safe reads. Keep all numeric parameters as function arguments — these are mechanical math primitives; the strategy class (Task 3) supplies the config-driven parameters (period=200, lookback=cfg.rvol_lookback_days). Write `tests/strategy/test_indicators.py` covering the three behaviors, with the RVOL test explicitly asserting the denominator excludes any date >= signal_date.
  </action>
  <verify>
    <automated>python -m pytest tests/strategy/test_indicators.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `bot/strategy/indicators.py` defines `sma`, `rvol`, `swing_low_2_2` and imports nothing network/I/O-related (no sqlite, no moomoo, no requests).
    - `sma` matches a hand-computed trailing mean and returns NaN/None below the period length.
    - `rvol` denominator contains no row with `date >= signal_date` (test asserts the look-ahead guard directly); a large current-day volume does not appear in the denominator.
    - `swing_low_2_2` returns the correct pivot on a clean synthetic series and None when unavailable.
    - `python -m pytest tests/strategy/test_indicators.py -q` exits 0 and runs with no network (no fixtures hitting OpenD/yfinance).
  </acceptance_criteria>
  <done>SMA200, look-ahead-free RVOL, and swing_low_2_2 are pure, correct, and unit-tested against synthetic DataFrames with zero network calls (success criterion 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StrategyCore ABC + TrendJoinLong (config-driven) + behavioral config-drivenness test (CFG-01/D-12)</name>
  <files>bot/strategy/core.py, bot/strategy/trend_join_long.py, tests/strategy/test_config_driven.py</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (§Pattern 3 — the StrategyCore ABC signatures `passes_daily_filters`, `passes_intraday_filters`, `compute_initial_stop`, `compute_swing_low_2_2`, and the TrendJoinLong reference implementation; note the reference uses literals — this plan must replace EVERY literal with a cfg field per D-12)
    - .planning/phases/01-foundation/01-PATTERNS.md (§"bot/strategy/core.py" no-analog → research defaults; §"bot/strategy/trend_join_long.py" — config-driven param access `self._cfg.<field>`, `_check_d1/_check_d2/_check_d3` private methods)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12 behavioral proof: swap baseline vs modified rules.json, assert outputs change; CFG-01 no hardcoded strategy literal in StrategyCore)
    - bot/config/loader.py (StrategyConfig fields the strategy reads)
    - bot/strategy/indicators.py (sma/rvol/swing_low_2_2 to call)
  </read_first>
  <behavior>
    - Test (filters): with the canonical config, `passes_daily_filters` returns True for a synthetic bar above prior-day high, prior close above SMA200, gap >= 3%, price >= $3; returns False when any single condition fails.
    - Test (intraday): `passes_intraday_filters` returns True only when close > premarket_high AND close >= hod AND rvol >= cfg.rvol_min.
    - Test (stop): `compute_initial_stop(lod)` equals `lod * (1 - cfg.max ... )` — i.e. LOD minus 1% read from config, NOT a hardcoded 0.99.
    - Test (config-drivenness, D-12): instantiate TrendJoinLong with a BASELINE config and a MODIFIED config (e.g. d3 gap 3.0 → 6.0, rvol_min 2.0 → 5.0); feed the SAME synthetic bar that passes under baseline but should FAIL under the modified thresholds; assert the boolean output changes. This proves the parameter flows from config and is not hardcoded.
  </behavior>
  <action>
    Create `bot/strategy/core.py` defining `StrategyCore(abc.ABC)` with `@abstractmethod` hooks `passes_daily_filters(code, daily_data, sma200)`, `passes_intraday_filters(code, bars_5m, premarket_high, hod, rvol)`, `compute_initial_stop(lod)`, `compute_swing_low_2_2(bars_5m)` (signatures from ARCHITECTURE.md §Pattern 3). No I/O inside the ABC. Create `bot/strategy/trend_join_long.py` defining `TrendJoinLong(StrategyCore)` whose constructor takes a `StrategyConfig` (`self._cfg = cfg`). EVERY strategy constant must be read from `self._cfg` — the D3 gap threshold from `self._cfg.d3_min_gap_pct`, the price floor from `self._cfg.min_price_usd`, the RVOL minimum from `self._cfg.rvol_min`, the stop percentage derived from config (not a literal `0.99`), etc. (D-12: no hardcoded strategy literal). Decompose the daily filter into private `_check_d1`/`_check_d2`/`_check_d3` methods per PATTERNS.md naming. `compute_swing_low_2_2` delegates to `indicators.swing_low_2_2`. Write `tests/strategy/test_config_driven.py` implementing the behavioral D-12 proof: build a baseline `StrategyConfig` and a modified one (tighten d3 gap and rvol_min), run the same inputs through both, and assert the filter outputs differ — this is the refactor-proof "no hardcoded literal" criterion. Include the filter/intraday/stop behavior tests too.
  </action>
  <verify>
    <automated>python -m pytest tests/strategy/test_config_driven.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `bot/strategy/core.py` contains `class StrategyCore` subclassing `abc.ABC` with `@abstractmethod` on all four hooks.
    - `bot/strategy/trend_join_long.py` contains `class TrendJoinLong(StrategyCore)` and reads parameters via `self._cfg.<field>` (grep shows multiple `self._cfg.` accesses; no bare numeric strategy literals like `3.0`, `2.0`, `0.99` appear as filter thresholds — they come from cfg).
    - Behavioral config-drivenness test: the SAME synthetic bar yields a DIFFERENT `passes_daily_filters` result under baseline vs modified config (D-12) — test passes.
    - `compute_initial_stop(lod)` equals LOD minus the config-driven 1% (not a hardcoded constant) — test asserts using a config whose stop pct is changed.
    - `passes_intraday_filters` honors `cfg.rvol_min` (changing rvol_min flips the result for a borderline rvol).
    - `python -m pytest tests/strategy/test_config_driven.py -q` exits 0 with no network.
  </acceptance_criteria>
  <done>StrategyCore ABC + TrendJoinLong implement the D1/D2/D3 and intraday filters and stop rule reading every parameter from StrategyConfig, proven config-driven by a behavioral baseline-vs-modified swap test (CFG-01, success criteria 3 and 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rules.json (config file) → strategy | a malformed/injected config must not silently produce wrong strategy behavior |
| (no network in this layer) | strategy code is pure; no external trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | rules.json / loader | mitigate | jsonschema validation at load (typed, required-key enforced); malformed/missing/schema-invalid configs fail fast with ConfigError before any strategy decision — a config injection cannot produce silent out-of-range parameters. |
| T-01-10 | Repudiation | hardcoded strategy literal | mitigate | Behavioral config-drivenness test (D-12) guarantees every parameter flows from rules.json, so the single source of truth cannot be silently overridden by a Python literal. |
| T-01-11 | Information Disclosure | strategy layer | accept | Pure-logic layer reads no credentials and performs no I/O; nothing sensitive to disclose. |
</threat_model>

<verification>
- `python -m pytest tests/config tests/strategy -q` exits 0.
- `grep -n "Trend Join Long" rules.json` matches (canonical config present).
- `grep -c "self._cfg." bot/strategy/trend_join_long.py` is > 5 (parameters flow from config).
- RVOL denominator excludes any date >= signal_date (look-ahead test green).
- Baseline-vs-modified config swap changes filter output (D-12 behavioral proof green).
</verification>

<success_criteria>
- rules.json (CFG-01) loads and jsonschema-validates at startup; malformed/missing fails fast with a clear error (success criterion 4).
- Every strategy constant is read from config — no strategy literal hardcoded in StrategyCore/TrendJoinLong, proven behaviorally (success criterion 4, D-12).
- TrendJoinLong indicator/filter functions (SMA200, RVOL, swing_low_2_2, D1/D2/D3) pass unit tests against synthetic DataFrames with no network calls (success criterion 3).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
