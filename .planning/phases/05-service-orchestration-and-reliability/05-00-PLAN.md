---
phase: 05-service-orchestration-and-reliability
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - rules.json
  - bot/config/schema.py
  - bot/config/loader.py
  - bot/gateway/gateway.py
  - bot/position/manager.py
  - bot/service/__init__.py
  - tests/service/__init__.py
  - tests/service/conftest.py
  - tests/service/test_bot.py
  - tests/service/test_watchdog.py
  - tests/service/test_alerter.py
  - tests/service/test_report.py
  - tests/gateway/test_gateway.py
  - .env.example
  - .gitignore
autonomous: false
requirements: [SVC-01, SVC-02, SCAN-07, ALERT-01, ALERT-02, ALERT-03, ALERT-04, DASH-01]
user_setup:
  - service: telegram
    why: "Telegram push alerts (ALERT-01..04) — operator must create a bot + chat to receive alerts; bot no-ops gracefully if unconfigured (D-12/D-13)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather -> /newbot -> copy the HTTP API token"
      - name: TELEGRAM_CHAT_ID
        source: "Send a message to the bot, then GET https://api.telegram.org/bot<TOKEN>/getUpdates and read result[].message.chat.id"

must_haves:
  truths:
    - "apscheduler==3.11.2 is pinned in requirements.txt and importable"
    - "rules.json has a service block; StrategyConfig exposes every Phase-5 tunable (no hardcoded literal in Phase-5 code)"
    - "MoomooGateway.get_global_state() exists, returns a dict with connected/qot_logined/trd_logined, and never raises"
    - "PositionManager accepts optional on_entry_alert / on_exit_alert callbacks (None default, backward compatible)"
    - "tests/service/ package exists with failing (RED) stubs for bot/watchdog/alerter/report"
    - ".env.example documents TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID; reports/ and logs/ are gitignored"
  artifacts:
    - path: "bot/service/__init__.py"
      provides: "new service package"
      min_lines: 1
    - path: "tests/service/test_bot.py"
      provides: "RED unit stubs for TradingBot (SVC-01, SCAN-07, R-04-01)"
      contains: "test_premarket_scan_job_called"
    - path: "tests/service/test_watchdog.py"
      provides: "RED unit stubs for OpenDWatchdog (SVC-02)"
      contains: "test_disconnect_disables_entries"
    - path: "tests/service/test_alerter.py"
      provides: "RED unit stubs for TelegramAlerter (ALERT-01..04)"
      contains: "test_send_failure_does_not_raise"
    - path: "tests/service/test_report.py"
      provides: "RED unit stubs for ReportBuilder (DASH-01)"
      contains: "test_r_histogram_buckets"
    - path: "rules.json"
      provides: "service config block (CFG-01)"
      contains: "service"
  key_links:
    - from: "bot/config/loader.py"
      to: "rules.json service block"
      via: "load_strategy_config maps service.* into StrategyConfig fields"
      pattern: "svc_cfg\\s*=\\s*data\\.get\\(\"service\""
    - from: "bot/gateway/gateway.py"
      to: "self._quote_ctx.get_global_state()"
      via: "run_in_executor deferred-SDK-import method"
      pattern: "get_global_state"
---

<objective>
Wave-0 foundation for Phase 5. Adds the one new runtime dependency (`apscheduler`, with a package-legitimacy checkpoint), extends the CFG-01 config pipeline with a `service` block so NO Phase-5 schedule/poll/backoff literal is ever hardcoded, adds the new `MoomooGateway.get_global_state()` health-check method the watchdog needs, threads optional alert callbacks through `PositionManager` for the alerter fan-out, documents the Telegram secrets, and lays down the `bot/service/` + `tests/service/` packages with failing (RED) test stubs that the later waves turn green.

Purpose: Every other Phase-5 plan (05-01..05-04) depends on these shared artifacts. Building them once here keeps the four feature slices parallel and conflict-free.
Output: pinned `apscheduler`, `service` config block + loader fields, `get_global_state()`, PositionManager alert hooks, `bot/service/` package, `tests/service/` RED stubs, `.env.example` + `.gitignore` updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-service-orchestration-and-reliability/05-CONTEXT.md
@.planning/phases/05-service-orchestration-and-reliability/05-RESEARCH.md
@.planning/phases/05-service-orchestration-and-reliability/05-PATTERNS.md
@.planning/phases/05-service-orchestration-and-reliability/05-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols/files NEWLY created across Phase 5 (exclude these from source-grounding drift checks — they do not exist before this phase):
- Classes: `TradingBot` (bot/service/bot.py), `OpenDWatchdog` (bot/service/watchdog.py), `TelegramAlerter` (bot/service/alerter.py), `ReportBuilder` (bot/service/report.py)
- Functions: `main` (bot/main.py), `build_daily_html` / `_build_r_histogram` / `write_reports` (bot/service/report.py), `TradingBot._job_premarket_scan` / `_job_market_open_subscribe` / `_job_intraday_rescan` / `_job_force_close` / `_job_eod_report` / `_readiness_gate` / `run` / `_register_jobs`
- New gateway method: `MoomooGateway.get_global_state()` (bot/gateway/gateway.py)
- New PositionManager params: `on_entry_alert`, `on_exit_alert` (bot/position/manager.py __init__)
- Entry points: `python -m bot` (bot/__main__.py → bot/main.py)
- New StrategyConfig fields (service.*): `premarket_scan_et`, `market_open_et`, `intraday_rescan_interval_min`, `intraday_rescan_start_et`, `intraday_rescan_end_et`, `eod_report_et`, `watchdog_poll_interval_s`, `watchdog_reconnect_initial_s`, `watchdog_reconnect_cap_s`, `alerts_enabled`, `misfire_grace_scan_s`, `misfire_grace_rescan_s`, `force_close_misfire_grace_s`, `launchd_throttle_interval_s`, `crash_loop_alert_threshold`
- New file paths: `bot/service/*`, `bot/main.py`, `bot/__main__.py`, `deploy/com.bot.trading.plist`, `deploy/run_forever.sh`, `reports/`, `tests/service/*`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package-legitimacy checkpoint — apscheduler</name>
  <files>requirements.txt</files>
  <what-built>RESEARCH.md §"Package Legitimacy Audit" already verified `apscheduler==3.11.2` as [VERIFIED: pypi.org] (MIT, 14+ yrs, 36.3M downloads/month, official repo github.com/agronholm/apscheduler, pushed 2026-06-22). slopcheck was unavailable at research time, so this human gate confirms the single new runtime dependency before it is pinned/installed (Phase-2 yfinance/pandas-market-calendars precedent; threat T-05-SC).</what-built>
  <action>Pause for the operator to confirm apscheduler legitimacy on the public registries before Task 1 pins/installs it. This is the package-legitimacy gate (never auto-approvable); the executor does not install until "approved" is received.</action>
  <how-to-verify>
    1. Open https://pypi.org/project/APScheduler/3.11.2/ — confirm version 3.11.2, MIT license, "Homepage" → github.com/agronholm/apscheduler.
    2. Open https://github.com/agronholm/apscheduler — confirm it is the official actively-maintained repo (stars, recent commits, no fork/typosquat name).
    3. Confirm the name is exactly `apscheduler` (not `apsheduler`, `async-apscheduler`, or a look-alike).
  </how-to-verify>
  <verify><human-check>Operator confirms apscheduler==3.11.2 legitimacy on pypi.org + github.com/agronholm/apscheduler and approves the install.</human-check></verify>
  <done>Operator typed "approved" after verifying the package on PyPI + the official GitHub repo; Task 1 may pin + install apscheduler==3.11.2.</done>
  <resume-signal>Type "approved" to proceed with pinning + install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Pin apscheduler, scaffold bot/service + tests/service, update .env.example + .gitignore</name>
  <files>requirements.txt, bot/service/__init__.py, tests/service/__init__.py, tests/service/conftest.py, .env.example, .gitignore</files>
  <read_first>
    - requirements.txt (current pinned deps — append apscheduler in the right format)
    - .env.example (FUTU_* env-var convention; bot does NOT auto-load .env — add Telegram block in the same style)
    - .gitignore (existing patterns — add reports/ and logs/)
    - bot/__init__.py (package docstring style for the new bot/service/__init__.py)
    - tests/conftest.py (fixture conventions: tmp_state_db monkeypatch pattern at lines 26-35)
    - .planning/phases/05-service-orchestration-and-reliability/05-PATTERNS.md (§"tests/service/..." analog: tests/service/conftest.py adds monkeypatch.setenv TELEGRAM_BOT_TOKEN/CHAT_ID empty)
    - .planning/phases/05-service-orchestration-and-reliability/05-RESEARCH.md (§"Recommended Project Structure" for the bot/service/ + tests/service/ + reports/ + deploy/ layout)
  </read_first>
  <action>
    Append `apscheduler==3.11.2` to requirements.txt under a new `# Phase 5: Service Orchestration` comment header, then `pip install apscheduler==3.11.2`. Create `bot/service/__init__.py` (3-6 line package docstring naming bot.service.bot/watchdog/alerter/report). Create `tests/service/__init__.py` (empty package marker) and `tests/service/conftest.py` adding a fixture that does `monkeypatch.setenv("TELEGRAM_BOT_TOKEN", "")` and `monkeypatch.setenv("TELEGRAM_CHAT_ID", "")` (so TelegramAlerter is disabled by default in unit tests) plus reuse of the existing `tmp_state_db` style. Add a `# ----- Telegram alerts (optional — bot no-ops if unset, D-12/D-13) -----` block to `.env.example` documenting `TELEGRAM_BOT_TOKEN=` and `TELEGRAM_CHAT_ID=` (blank, with the @BotFather + getUpdates sourcing note). Add `reports/` and `logs/` to `.gitignore` (D-16: reports/ gitignored; launchd StandardOut/Err live in logs/). Do NOT put any secret in rules.json (D-13).
  </action>
  <verify>
    <automated>python -c "import apscheduler; print(apscheduler.__version__)" && grep -q "apscheduler==3.11.2" requirements.txt && python -c "import bot.service" && grep -q "TELEGRAM_BOT_TOKEN" .env.example && grep -qE "^reports/" .gitignore && grep -qE "^logs/" .gitignore</automated>
  </verify>
  <done>apscheduler 3.11.2 importable + pinned; bot.service importable; tests/service package + conftest exist; .env.example documents both Telegram vars (blank); reports/ and logs/ gitignored. No secret added to rules.json.</done>
</task>

<task type="auto">
  <name>Task 2: Add service config block to rules.json + schema.py + StrategyConfig loader (CFG-01)</name>
  <files>rules.json, bot/config/schema.py, bot/config/loader.py</files>
  <read_first>
    - rules.json (existing groups; add a new top-level "service" object alongside "execution")
    - bot/config/schema.py (§"execution" block lines 141-167 — copy the required-list + typed-properties structure; "additionalProperties": True already allows the new key)
    - bot/config/loader.py (StrategyConfig dataclass lines 101-111 for the field pattern; load_strategy_config mapping lines 161-196 for the `ex_cfg = data.get("execution", {})` → typed-field pattern)
    - .planning/phases/05-service-orchestration-and-reliability/05-PATTERNS.md (§"bot/config/schema.py — add service block" and §"bot/config/loader.py — extend StrategyConfig" — exact field list and mapping shape)
    - .planning/phases/05-service-orchestration-and-reliability/05-CONTEXT.md (Claude's Discretion: all numeric tunables → rules.json, no hardcoded literals)
  </read_first>
  <action>
    Add a `"service"` object to rules.json with these keys (sensible ET-correct defaults): `premarket_scan_et` "08:30", `market_open_et` "09:30", `intraday_rescan_interval_min` 30, `intraday_rescan_start_et` "09:55", `intraday_rescan_end_et` "12:55" (SCAN-07 window), `eod_report_et` "15:55", `watchdog_poll_interval_s` 60, `watchdog_reconnect_initial_s` 5, `watchdog_reconnect_cap_s` 60 (D-10), `alerts_enabled` true (D-13 non-secret toggle), `misfire_grace_scan_s` 3600, `misfire_grace_rescan_s` 600, `force_close_misfire_grace_s` 300 (D-03/Pitfall 7), `launchd_throttle_interval_s` 30 (D-06), `crash_loop_alert_threshold` 5 (D-06). Add a matching `"service"` block to schema.py `SCHEMA["properties"]` with all keys in `required` and typed (string for *_et, integer for *_s / *_min / *_threshold, number where fractional allowed, boolean for alerts_enabled), and add `"service"` to the top-level `required` list. Extend the StrategyConfig dataclass with the corresponding flat fields and map them in load_strategy_config via `svc_cfg = data.get("service", {})`. Keep secrets OUT (D-13 — only non-secret toggles here).
  </action>
  <verify>
    <automated>python -c "from bot.config.loader import load_strategy_config as L; c=L('rules.json'); assert c.intraday_rescan_interval_min==30 and c.watchdog_poll_interval_s==60 and c.alerts_enabled is True and c.force_close_misfire_grace_s==300 and c.intraday_rescan_end_et=='12:55'; print('service config OK')"</automated>
  </verify>
  <done>rules.json has a validated service block; StrategyConfig exposes all 15 service.* fields; a config-swap (changing a service value in rules.json) changes the loaded StrategyConfig value (CFG-01 behavioral proof, no Phase-5 literal in code).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add MoomooGateway.get_global_state() + PositionManager alert callbacks</name>
  <files>bot/gateway/gateway.py, bot/position/manager.py, tests/gateway/test_gateway.py</files>
  <read_first>
    - bot/gateway/gateway.py (§subscribe() lines 423-459 — the deferred-import + `loop = asyncio.get_running_loop()` + inner `def _blocking()` + run_in_executor pattern; §get_equity() lines 330-387 — the try/except → warning log → fallback-return-never-raises convention; module RET_OK import + _check_ret at line 194; insert the new method in the "Async Wrappers (run_in_executor)" section after line 493)
    - skills/moomooapi/scripts/quote/get_global_state.py (reference: `ctx.get_global_state()` returns `(ret, data)` where data is a DICT with keys qot_logined, trd_logined, server_ver, market_us — NOT a DataFrame)
    - bot/position/manager.py (§__init__ lines 121-141 — add two optional params; §_on_entry_fill line 388 and §_on_exit_fill line 438 — the hook points where alert callbacks should be invoked guarded by `if self._on_entry_alert is not None`)
    - .planning/phases/05-service-orchestration-and-reliability/05-PATTERNS.md (§"bot/gateway/gateway.py — add get_global_state()" exact pattern; §Shared "Fire-and-forget task dispatch")
    - .planning/phases/05-service-orchestration-and-reliability/05-RESEARCH.md (Pattern 2 get_global_state full body; Open Question #2 — callback injection at __init__, None default, backward compatible)
    - tests/gateway/test_gateway.py (mock-injection helper lines 41-65; gw._quote_ctx = MagicMock() — extend for get_global_state)
  </read_first>
  <behavior>
    - get_global_state returns {"connected": True, ...} when ret==RET_OK and data["qot_logined"] and data["trd_logined"] are both truthy
    - get_global_state returns {"connected": False, "qot_logined": False, "trd_logined": False} when ret != RET_OK
    - get_global_state returns {"connected": False, ...} (never raises) when the SDK call throws an exception
    - PositionManager(__init__) accepts on_entry_alert=None and on_exit_alert=None and stores them; existing call sites (no callback) still construct successfully
    - _on_entry_fill invokes on_entry_alert(...) only when it is not None; _on_exit_fill invokes on_exit_alert(...) only when not None — neither callback's failure breaks fill processing
  </behavior>
  <action>
    Add `async def get_global_state(self) -> dict` to MoomooGateway mirroring the subscribe()/get_equity() pattern: `loop = asyncio.get_running_loop()`, inner `def _blocking(): return self._quote_ctx.get_global_state()`, `ret, data = await loop.run_in_executor(None, _blocking)`; return `{"connected": bool(data.get("qot_logined")) and bool(data.get("trd_logined")), "qot_logined": ..., "trd_logined": ..., "server_ver": str(...), "market_us": str(...)}` on RET_OK, `{"connected": False, "qot_logined": False, "trd_logined": False}` on non-RET_OK or empty, and the same fallback dict inside `except Exception:` with `_logger.warning("get_global_state_exception", exc_info=True)`. Add `on_entry_alert: Optional[Callable] = None` and `on_exit_alert: Optional[Callable] = None` to PositionManager.__init__ (store as `self._on_entry_alert` / `self._on_exit_alert`); at the end of _on_entry_fill, if `self._on_entry_alert is not None` call it with the new position's code/qty/entry_price/initial_stop; at the relevant points in _on_exit_fill, if `self._on_exit_alert is not None` call it with code + exit_reason + r_multiple. Wrap each callback invocation so a callback exception is logged via _logger and never propagates into fill processing (ALERT-04 isolation at the source). Add the corresponding green assertions into tests/gateway/test_gateway.py (mock _quote_ctx.get_global_state) — the alerter-callback content tests live in tests/service/test_alerter.py (05-03).
  </action>
  <verify>
    <automated>pytest tests/gateway/test_gateway.py -x -q && python -c "import inspect; from bot.position.manager import PositionManager; p=inspect.signature(PositionManager.__init__).parameters; assert 'on_entry_alert' in p and 'on_exit_alert' in p; print('callbacks present')"</automated>
  </verify>
  <done>get_global_state returns a connected/qot_logined/trd_logined dict and never raises (verified with mocked RET_OK, non-RET_OK, and exception paths); PositionManager.__init__ accepts on_entry_alert/on_exit_alert (None default, backward compatible) and invokes them guarded; gateway test suite green.</done>
</task>

<task type="auto">
  <name>Task 4: Create RED test stubs for bot/watchdog/alerter/report (Wave 0 Nyquist scaffold)</name>
  <files>tests/service/test_bot.py, tests/service/test_watchdog.py, tests/service/test_alerter.py, tests/service/test_report.py, .planning/phases/05-service-orchestration-and-reliability/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-service-orchestration-and-reliability/05-VALIDATION.md (§"Per-Task Verification Map" — the exact test function names each later wave must turn green)
    - .planning/phases/05-service-orchestration-and-reliability/05-PATTERNS.md (§"tests/service/..." — analog tests/gateway/test_gateway.py mock-injection + async test structure; _make_bot_with_mocks injects mock gateway/store/position_manager/alerter)
    - tests/gateway/test_gateway.py (header + AsyncMock/MagicMock import pattern lines 1-33)
    - tests/position/test_manager.py (async test + AsyncMock pattern lines 1-31)
    - tests/conftest.py (fixtures available: tmp_state_db, minimal_rules, make_fill_event, make_bar_event)
  </read_first>
  <action>
    Create four RED test files, each containing the named tests from 05-VALIDATION.md so the later waves have concrete targets. Each stub uses `pytest.importorskip("bot.service.bot")` (or the relevant module) OR an `xfail(reason="implemented in 05-0N")` / `pytest.raises(ImportError)` marker so the suite COLLECTS cleanly and the tests are RED-but-expected until their plan lands — do NOT leave bare `assert False`. Files + functions:
    - tests/service/test_bot.py: test_premarket_scan_job_called, test_intraday_rescan_job_called, test_force_close_job_called, test_readiness_gate_blocks_entries, test_kill_switch_flush_registered, test_scheduler_has_required_jobs
    - tests/service/test_watchdog.py: test_disconnect_disables_entries, test_reconnect_reenables_entries
    - tests/service/test_alerter.py: test_entry_alert_content, test_exit_alert_for_all_reasons, test_daily_summary_content, test_send_failure_does_not_raise
    - tests/service/test_report.py: test_html_report_sections, test_r_histogram_buckets
    Mark each with `@pytest.mark.asyncio` where the target is a coroutine. Update 05-VALIDATION.md frontmatter `wave_0_complete: true` and `nyquist_compliant: true`.
  </action>
  <verify>
    <automated>pytest tests/service/ -q --co | grep -q "test_premarket_scan_job_called" && pytest tests/service/ -q 2>&1 | tail -3</automated>
  </verify>
  <done>tests/service/ collects all 14 named test functions; suite runs without collection errors (tests are xfail/skip-until-implemented, not crashes); 05-VALIDATION.md marked wave_0_complete + nyquist_compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip registry → local env | apscheduler install pulls third-party code (supply chain) |
| rules.json → StrategyConfig | config values cross into runtime; secrets must NOT live here |
| env vars → process | TELEGRAM_* secrets enter via environment only (D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | apscheduler npm/pip install | mitigate | Task 0 blocking-human package-legitimacy checkpoint before pin/install; pinned to exact 3.11.2; RESEARCH audit table [VERIFIED: pypi.org] |
| T-05-00-01 | Information Disclosure | Telegram secrets in config | mitigate | D-13: secrets ONLY via env (.env.example documents them blank); rules.json service block holds non-secret toggles only; verify task asserts no token in rules.json |
| T-05-00-02 | Tampering | force_close misfire grace | mitigate | service.force_close_misfire_grace_s defaults to 300 (not None) — Pitfall 7 (no spurious next-morning force-close) |
| T-05-00-03 | Spoofing | alert callback input (ticker/reason) | accept | Raw strings only flow to alerter later (05-03 escapes for HTML); at this layer callbacks just receive position fields — no sink yet |
</threat_model>

<verification>
- `python -c "import apscheduler"` succeeds (pinned 3.11.2)
- `load_strategy_config('rules.json')` returns a config with all service.* fields
- `MoomooGateway.get_global_state` mocked paths (RET_OK / non-RET_OK / exception) all behave
- `PositionManager.__init__` accepts the two new callbacks
- `pytest tests/service/ -q` collects 14 named tests; `pytest tests/gateway/test_gateway.py -x -q` green
</verification>

<success_criteria>
- apscheduler==3.11.2 pinned + importable; legitimacy checkpoint approved
- service config block validated end-to-end through StrategyConfig (CFG-01, no hardcoded Phase-5 literal)
- get_global_state() present, dict-returning, never-raising
- PositionManager alert callbacks present (backward compatible)
- bot/service/ + tests/service/ packages exist; 14 RED stubs collect
- Telegram secrets documented in .env.example; reports/ + logs/ gitignored
</success_criteria>

<output>
Create `.planning/phases/05-service-orchestration-and-reliability/05-00-SUMMARY.md` when done
</output>
