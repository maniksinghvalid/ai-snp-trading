---
phase: 03-intraday-signal-and-risk-engine
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - bot/signal/signal_engine.py
  - tests/signal/test_signal_engine.py
autonomous: true
requirements: [SIG-03, SIG-04, RISK-04, RISK-05]
must_haves:
  truths:
    - "A SignalEvent is emitted only when I1/I2/I3 all pass AND the ET clock is inside the entry window (SIG-03)"
    - "Entry window is [earliest_entry_et inclusive, latest_entry_et exclusive): 10:04:59 out, 10:05:00 in, 15:29:59 in, 15:30:00 out (RESEARCH Pitfall 6)"
    - "No signal when live broker position count >= max_concurrent_positions (SIG-04, RISK-04), checked against get_positions() broker truth"
    - "No signal when filled_count + session pending tally >= max_trades_per_day; closing a position does NOT reset the daily cap (RISK-05, D-08/D-09)"
    - "A code with no/zero premarket high emits no signal that session (D-03)"
  artifacts:
    - path: "bot/signal/signal_engine.py"
      provides: "SignalEngine — on_bar() orchestrates I1/I2/I3 + entry-window + concurrent-cap + daily-cap gates, emits SignalEvent"
      contains: "class SignalEngine"
      min_lines: 90
    - path: "tests/signal/test_signal_engine.py"
      provides: "SIG-03/SIG-04/RISK-04/RISK-05 gate-combination + boundary coverage"
      contains: "test_entry_window_boundaries"
  key_links:
    - from: "bot/signal/signal_engine.py"
      to: "bot/strategy/trend_join_long.py"
      via: "TrendJoinLong(cfg).passes_intraday_filters(code, bars_df, premarket_high, hod, rvol)"
      pattern: "passes_intraday_filters"
    - from: "bot/signal/signal_engine.py"
      to: "bot/gateway/gateway.py"
      via: "await gateway.get_positions() for the concurrent-cap gate"
      pattern: "get_positions"
    - from: "bot/signal/signal_engine.py"
      to: "bot/safety/et_helpers.py"
      via: "now_et() for the entry-window gate"
      pattern: "now_et"
    - from: "bot/signal/signal_engine.py"
      to: "daily_trade_count table"
      via: "SELECT filled_count FROM daily_trade_count WHERE session_date = ?"
      pattern: "daily_trade_count"
---

<objective>
Build the `SignalEngine` — the orchestrator that takes a closed `BarEvent` from `BarAggregator`, feeds the already-built `TrendJoinLong.passes_intraday_filters()` (I1 above premarket high, I2 above HOD, I3 RVOL ≥ 2.0), and composes four independent gates — the 10:05–15:30 ET entry window, the 5-concurrent-position cap (broker truth), the daily new-entry cap, and the D-03 missing-premarket-high guard — emitting a `SignalEvent` only when every gate passes.

Purpose: This is the decision layer for SIG-03 and SIG-04 plus RISK-04/RISK-05's gates. Each gate is independent (RESEARCH success criterion #3 requires the daily cap to bind even when the concurrent cap has free slots), so they must be evaluated separately and short-circuit with a specific logged reason. All thresholds come from `StrategyConfig` — zero literals (CFG-01).

Output: `bot/signal/signal_engine.py` and the fleshed-out `tests/signal/test_signal_engine.py` covering every gate and the entry-window boundary table.
</objective>

<artifacts_this_phase_produces>
New symbols created by THIS plan (excluded from drift verification):
- **Class:** `SignalEngine` (bot/signal/signal_engine.py)
Consumes (created in 03-01, not drift): `BarEvent`, `SignalEvent` (bot/signal/events.py), `daily_trade_count` table (migration 0003). Reuses existing Phase 1/2 symbols: `TrendJoinLong.passes_intraday_filters`, `StrategyConfig`, `MoomooGateway.get_positions`, `now_et`, `StateStore`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-intraday-signal-and-risk-engine/03-CONTEXT.md
@.planning/phases/03-intraday-signal-and-risk-engine/03-RESEARCH.md
@.planning/phases/03-intraday-signal-and-risk-engine/03-PATTERNS.md
@bot/strategy/trend_join_long.py
@bot/config/loader.py
@bot/safety/et_helpers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SignalEngine.on_bar() — I1/I2/I3 feed + entry-window gate + missing-premarket-high guard (SIG-03, D-03)</name>
  <files>bot/signal/signal_engine.py, tests/signal/test_signal_engine.py</files>
  <behavior>
    - test_all_gates_required: a SignalEvent is emitted only when passes_intraday_filters() returns True AND the clock is in-window; flipping any single filter input to a failing value (close below premarket_high, close below hod, rvol below cfg.rvol_min) yields no signal.
    - test_entry_window_boundaries: table-driven across mocked now_et() values — 10:04:59 → out, 10:05:00 → in, 15:29:59 → in, 15:30:00 → out (earliest inclusive, latest exclusive; RESEARCH Pitfall 6). Parametrize boundary times.
    - test_missing_premarket_high_no_signal: a code whose frozen premarket_high is 0.0/undefined emits no signal that session and logs a specific reason (D-03 — never fall back to prior-day high).
    - test_config_driven_window: swapping cfg.earliest_entry_et / latest_entry_et changes which boundary times pass (behavioral CFG-01 proof, Phase 1 D-12 style).
  </behavior>
  <read_first>
    - bot/strategy/trend_join_long.py lines 112–144 (`passes_intraday_filters(code, bars_5m, premarket_high, hod, rvol)` — I1 `close > premarket_high`, I2 `close >= hod`, I3 `rvol >= cfg.rvol_min`; it reads `bars_5m.iloc[-1]["close"]`)
    - bot/safety/et_helpers.py (full file — `now_et()` returns aware ET datetime; `.time()` for the gate)
    - bot/config/loader.py lines 84–99 (`earliest_entry_et`, `latest_entry_et` are HH:MM strings; `rvol_min`, `max_concurrent_positions`, `max_trades_per_day` fields)
    - bot/scanner/scanner.py (orchestrator analog — imports, `get_logger(__name__)`, config-driven early-return-with-logged-reason pattern, `TrendJoinLong(cfg)` instantiation; 03-PATTERNS.md lines 143–209)
    - bot/signal/events.py (BarEvent in, SignalEvent out — created in 03-01)
    - .planning/phases/03-intraday-signal-and-risk-engine/03-RESEARCH.md Code Examples "Entry-Window Time Gate" (inclusive/exclusive boundary), "RVOL Computation from Watchlist Baseline", Pitfall 2 (never recompute RVOL — read rvol_baseline), Pitfall 6 (boundary off-by-one)
  </read_first>
  <action>
Create `bot/signal/signal_engine.py` defining `class SignalEngine`. Constructor `__init__(self, cfg: StrategyConfig, gateway, store: StateStore)` storing the three; instantiate `self._strategy = TrendJoinLong(cfg)` once (scanner analog). Module + class + method docstrings; `_logger = get_logger(__name__)`; zero numeric literals — every threshold/time from `self._cfg.*` (CFG-01).

Maintain `self._premarket_highs: Dict[str, float]` frozen for the session (set via a `set_premarket_highs(mapping)` method or passed in; the live snapshot fetch itself is the gateway one-shot near 09:30 — D-01 source is out of this engine's pure path, but the engine OWNS the gate). Per D-03, a code absent from `_premarket_highs` or with a value <= 0 emits no signal — early-return with `_logger.info("signal_skipped_no_premarket_high", code=...)`.

Implement `_in_entry_window(self) -> bool` exactly per RESEARCH "Entry-Window Time Gate": parse `cfg.earliest_entry_et` / `cfg.latest_entry_et` "HH:MM" into `datetime.time`; return `earliest <= now_et().time() < latest` (earliest INCLUSIVE, latest EXCLUSIVE — RESEARCH Pitfall 6). Document the boundary semantics in the method docstring.

Implement `async def on_bar(self, event: BarEvent) -> Optional[SignalEvent]`. Build a single-row (or rolling-deque-backed) `bars_5m` DataFrame from the BarEvent for `passes_intraday_filters` (it reads `iloc[-1]["close"]`). Read `rvol` from the watchlist `rvol_baseline` ratio (Pitfall 2 — never recompute RVOL; read the stored baseline). Gate order with specific early-return + logged reason for each: (1) premarket-high present (D-03); (2) `passes_intraday_filters(code, bars_5m, premarket_high, event.hod, rvol)` True (SIG-03 I1/I2/I3); (3) `_in_entry_window()` True (SIG-03 time gate). Concurrent-cap and daily-cap gates are added in Task 2. On all-pass, build and return a `SignalEvent(code, bar=event, premarket_high, hod=event.hod, lod=event.lod, rvol, emitted_at=now_et())`.

Flesh out `tests/signal/test_signal_engine.py` (stub from 03-01) per `<behavior>`: construct `StrategyConfig` from test values (or load test rules.json), mock `now_et` via monkeypatch for the boundary table, pass `gateway=None` where the gate isn't exercised. No broker, no network.
  </action>
  <verify>
    <automated>pytest tests/signal/test_signal_engine.py::test_all_gates_required -x -q</automated>
    <automated>pytest tests/signal/test_signal_engine.py::test_entry_window_boundaries -x -q</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `pytest tests/signal/test_signal_engine.py::test_entry_window_boundaries -x` exits 0 — 10:04:59 out, 10:05:00 in, 15:29:59 in, 15:30:00 out.
    - BEHAVIOR: `pytest tests/signal/test_signal_engine.py::test_all_gates_required -x` exits 0 — signal only when filters + window both pass.
    - SOURCE: `bot/signal/signal_engine.py` contains no numeric/time literal for thresholds — `_in_entry_window` parses `cfg.earliest_entry_et`/`cfg.latest_entry_et`; I1/I2/I3 delegated to `TrendJoinLong.passes_intraday_filters` (no reimplemented filter math).
    - BEHAVIOR: a code with premarket_high <= 0 emits no signal and logs `signal_skipped_no_premarket_high` (D-03).
  </acceptance_criteria>
  <done>SignalEngine feeds passes_intraday_filters and gates on the inclusive/exclusive ET entry window and the D-03 premarket-high guard; boundary table-driven test green; all thresholds config-driven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Concurrent-position cap (broker truth) + daily new-entry cap gates (SIG-04, RISK-04, RISK-05, D-08/D-09)</name>
  <files>bot/signal/signal_engine.py, tests/signal/test_signal_engine.py</files>
  <behavior>
    - test_concurrent_cap: when mocked gateway.get_positions() returns >= cfg.max_concurrent_positions open codes, on_bar() emits no signal (SIG-04 / RISK-04) and logs the reason; below the cap a passing setup still emits.
    - test_daily_cap_blocks_after_max: with filled_count + pending tally == cfg.max_trades_per_day, on_bar() emits no signal even when a concurrent slot is free.
    - test_daily_cap_independent_of_concurrent: closing positions (concurrent count drops to 0) does NOT reset the daily cap — once filled_count + pending >= max_trades_per_day, no further entries fire that session (RISK-05, D-08/D-09).
    - test_blocked_signal_does_not_consume_pending: a setup rejected by the concurrent-cap gate increments NEITHER the pending tally NOR the daily count (D-11).
    - test_session_pending_tally_increments_on_emit: each emitted SignalEvent that will become an intent increments the in-memory pending tally (so the engine self-limits before any fill registers — D-09 burst guard).
  </behavior>
  <read_first>
    - bot/gateway/gateway.py lines 290–299 (`get_positions()` async wrapper returning `(ret, data)` — broker truth for the concurrent count; RESEARCH Pitfall 4 notes refresh_cache, but read the data length here)
    - bot/state/store.py lines 127–199 (`StateStore.open()`, `.conn` property — read `daily_trade_count.filled_count` via parameterized SQL; migration 0003 from 03-01 created the table)
    - bot/scanner/scanner.py lines 199–238 (parameterized `?`-binding SELECT + structlog event pattern — 03-PATTERNS.md lines 172–191)
    - bot/config/loader.py lines 95–99 (`max_concurrent_positions`, `max_trades_per_day`)
    - .planning/phases/03-intraday-signal-and-risk-engine/03-CONTEXT.md D-08/D-09/D-10/D-11 (fill-authoritative counter; (filled + pending) gate; re-entry only when flat + no pending; blocked signals consume nothing)
    - .planning/phases/03-intraday-signal-and-risk-engine/03-RESEARCH.md Pitfall 4 (stale concurrent count), Pitfall 5 (daily cap counts fills not intents — Phase 3 NEVER writes daily_trade_count)
  </read_first>
  <action>
Extend `SignalEngine` with the concurrent-cap and daily-cap gates inside `on_bar()`, evaluated as independent early-returns AFTER the filter + window gates from Task 1, each with a specific `_logger.info(reason, ...)`.

Concurrent-cap gate (SIG-04, RISK-04): `ret, data = await self._gateway.get_positions()`; count distinct open codes from broker truth; if `count >= self._cfg.max_concurrent_positions`, emit no signal. This is broker-truth, not an in-memory guard (RESEARCH Pitfall 4 — the concurrent count must come from `get_positions()`). For the D-10 re-entry rule, the same broker-flat check applies: a code already present in `get_positions()` is skipped (no double-entry).

Daily-cap gate (RISK-05, D-08/D-09): maintain `self._pending_count: int` in memory (the session pending tally), incremented ONLY when an OrderIntent will be emitted (the emit happens in RiskEngine/03-03, but the SignalEngine owns the gate decision; coordinate the increment at the boundary the 03-03 plan wires — for this task, expose `note_intent_emitted()` / `note_intent_resolved()` so the tally is adjustable, and increment on each emitted SignalEvent that the pipeline will size). Read `filled_count` via parameterized `SELECT filled_count FROM daily_trade_count WHERE session_date = ?` keyed on `now_et().date()` ISO string (default 0 if no row). Gate: if `filled_count + self._pending_count >= self._cfg.max_trades_per_day`, emit no signal. Phase 3 NEVER writes `daily_trade_count` (Pitfall 5 — that column is fill-authoritative, written by Phase 4). Blocked-by-cap signals consume neither counter (D-11).

Flesh out the remaining `tests/signal/test_signal_engine.py` tests per `<behavior>`: mock `gateway.get_positions()` to return a fake `(RET_OK, df_or_list)` of N codes; use an in-memory StateStore (monkeypatch `BOT_STATE_DB=:memory:` or construct with `db_path=":memory:"`) seeded with a `daily_trade_count` row to drive the daily-cap assertions; drive `_pending_count` to assert the independence-of-concurrent property.
  </action>
  <verify>
    <automated>pytest tests/signal/test_signal_engine.py::test_concurrent_cap -x -q</automated>
    <automated>pytest tests/signal/test_signal_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `pytest tests/signal/test_signal_engine.py::test_concurrent_cap -x` exits 0 — at/over `max_concurrent_positions` (from `get_positions()`) no signal emits.
    - BEHAVIOR: a daily-cap test proves closing positions does NOT reset the cap (filled_count + pending >= max_trades_per_day blocks further entries even with free concurrent slots) — RISK-05 independence.
    - SOURCE: `bot/signal/signal_engine.py` reads concurrent count from `gateway.get_positions()` (broker truth, not an in-memory list) and NEVER executes an UPDATE/INSERT on `daily_trade_count` (Pitfall 5 — fill-authoritative).
    - BEHAVIOR: a signal blocked by the concurrent-cap gate increments neither the pending tally nor the daily count (D-11).
  </acceptance_criteria>
  <done>Concurrent cap binds on broker truth; daily cap binds on (filled_count + session pending) independently of the concurrent cap and is never reset by closing positions; blocked signals consume no entry slot; full signal_engine suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SignalEngine → broker (get_positions) | Concurrent-cap decision depends on broker-truth position count; a stale/empty read could under-count and allow an over-limit entry |
| SignalEngine → StateStore (daily_trade_count) | The over-trading guard reads a persisted counter; a burst of intents in one bar must not blow past the cap before any fill registers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering / Integrity | Daily-cap over-trading guard (burst of intents in one bar — D-09) | mitigate | Gate on `filled_count + in-memory pending tally`; increment the pending tally at intent emission so a burst within one bar is bounded BEFORE any fill registers (D-09 burst guard) — `test_daily_cap_blocks_after_max` proves it |
| T-03-05 | Spoofing / Integrity | Concurrent-cap bypass via stale position read | mitigate | Concurrent count sourced from `gateway.get_positions()` broker truth (not an in-memory guard); RESEARCH Pitfall 4 flags `refresh_cache` for the live variant so the count is current — `test_concurrent_cap` proves the gate binds |
| T-03-06 | Elevation of Privilege | Phase 3 corrupting the fill-authoritative counter | mitigate | SignalEngine NEVER writes `daily_trade_count` (read-only); only Phase 4 increments it at fill (D-08) — source assertion verifies no UPDATE/INSERT on the table (Pitfall 5) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (Package Legitimacy Audit empty); no install task to gate. |
</threat_model>

<verification>
- `pytest tests/signal/test_signal_engine.py -x -q` green (SIG-03 gates + boundary table, SIG-04/RISK-04 concurrent cap, RISK-05 daily cap independence).
- `pytest tests/signal/ -x -q` green (no regression in bar_aggregator).
- `pytest tests/ -x -q` green at wave merge.
</verification>

<success_criteria>
- ROADMAP Phase 3 success criterion #2: signal generated only when I1/I2/I3 hold simultaneously AND time within 10:05–15:30 ET — confirmed by the boundary-time table-driven test.
- ROADMAP Phase 3 success criterion #3: no signal at/over 5 concurrent positions or at/after 15:30 ET or once the daily cap is reached; the daily cap is enforced independently of the concurrent cap (close-then-no-entry test).
- All gate thresholds/times read from StrategyConfig (CFG-01); D-03 missing-premarket-high guard active.
</success_criteria>

<output>
Create `.planning/phases/03-intraday-signal-and-risk-engine/03-02-SUMMARY.md` when done.
</output>
