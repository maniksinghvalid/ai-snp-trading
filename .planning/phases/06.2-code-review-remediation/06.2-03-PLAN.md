---
phase: 06.2-code-review-remediation
plan: 03
type: execute
wave: 3
depends_on: [06.2-01, 06.2-02]
files_modified:
  - bot/gateway/gateway.py
  - bot/service/bot.py
  - bot/scanner/scanner.py
  - tests/gateway/test_gateway.py
  - tests/scanner/test_scanner.py
autonomous: true
requirements: [SAFE-02, SAFE-03, TDD-REGRESSION, SUITE-GREEN]

must_haves:
  truths:
    - "reconcile_once and startup_reconcile share one extracted reconcile core; there is a single guard set (no duplicated ~200-line logic, no guard drift)"
    - "The shared core inherits the finding 1.2 failed-query guard (a failed broker query skips the cycle)"
    - "An intraday rescan reuses the same trading-day's daily download instead of re-downloading the full ~500-symbol universe every 30 min"
  artifacts:
    - path: "bot/gateway/gateway.py"
      provides: "_reconcile_core shared path delegated to by both reconcile entry points"
      contains: "_reconcile_core"
    - path: "bot/scanner/scanner.py"
      provides: "run_intraday_rescan reuses a caller-owned date-keyed daily-bar cache"
      contains: "run_intraday_rescan"
  key_links:
    - from: "bot/gateway/gateway.py::startup_reconcile"
      to: "bot/gateway/gateway.py::_reconcile_core"
      via: "startup_reconcile delegates to the shared core"
      pattern: "_reconcile_core\\("
    - from: "bot/service/bot.py::_job_intraday_rescan"
      to: "bot/scanner/scanner.py::run_intraday_rescan"
      via: "passes bot._daily_bar_cache keyed on scan_date"
      pattern: "_daily_bar_cache"
---

<objective>
Close the two Tier 3 hygiene findings (3.1, 3.2). 3.1 extracts the ~200-line duplication between `reconcile_once` and `startup_reconcile` into a single shared reconcile core so both entry points share one guard set — this must land AFTER Plan 01/1.2 so the shared path inherits the corrected failed-query handling. 3.2 stops the intraday rescan from re-downloading the full ~500-symbol daily universe every 30 minutes by reusing a trading-day-keyed daily-bar cache.

Purpose: Remove guard drift risk between the two reconcile paths and eliminate wasteful repeated downloads — maintainability, not new behavior.
Output: Two atomic `fix(t3.N)` commits (regression test first, then fix) and a full green suite; phase-end verifier gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-code-review-remediation/06.2-SPEC.md
@.planning/phases/06.2-code-review-remediation/06.2-RESEARCH.md
@.planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md
@.planning/phases/06.2-code-review-remediation/06.2-01-SUMMARY.md
@.planning/phases/06.2-code-review-remediation/06.2-02-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Finding 3.1 — extract a shared reconcile core; both entry points delegate</name>
  <files>bot/gateway/gateway.py, tests/gateway/test_gateway.py</files>
  <read_first>
    - bot/gateway/gateway.py lines 818-1023 (reconcile_once — includes the Plan 01/1.2 failed-query guard) and 1025-1195 (startup_reconcile — the ~200-line near-duplicate)
    - bot/gateway/gateway.py lines 334-390 (get_positions contract) and the mark_position_closed / orphan-adopt / protect blocks in both methods
    - .planning/phases/06.2-code-review-remediation/06.2-01-SUMMARY.md (confirm the exact 1.2 guard shape now in reconcile_once so the shared core preserves it)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 3 row 3.1 and "Ordering & dependencies" (3.1 lands after 1.2), and the "Refuted" note (startup_reconcile guard is functionally equivalent — superseded by this dedup)
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 3.1"
    - tests/gateway/test_gateway.py (existing reconcile tests incl. Plan 01 test_reconcile_once_skips_on_broker_query_failure — must still pass)
  </read_first>
  <behavior>
    RED — write tests/gateway/test_gateway.py::test_startup_reconcile_delegates_to_shared_core first: startup_reconcile and reconcile_once both drive the SAME shared close/adopt/protect logic (e.g. patch/spy the extracted _reconcile_core and assert both entry points call it with the built broker_map). Preserve existing behavior: Plan 01's test_reconcile_once_skips_on_broker_query_failure must remain green (the failed-query guard stays in reconcile_once, not inside the shared core).
  </behavior>
  <action>
    In bot/gateway/gateway.py extract the common close/adopt/protect body into `async def _reconcile_core(self, store, manager, alerter, broker_map: dict) -> dict` containing the single guard set (duplicate-code orphan guard from 2.5, mark-closed, protect). Add a `_build_broker_map(broker_data) -> dict` helper for the shared parse. Rewrite reconcile_once to: call get_positions, apply the 1.2 failed-query guard (return {} + alert on ret != RET_OK / None — unchanged from Plan 01), build broker_map via _build_broker_map, then `return await self._reconcile_core(store, manager, alerter, broker_map)`. Rewrite startup_reconcile to build broker_map the same way (empty on non-RET_OK) and delegate to `_reconcile_core(store, manager, alerter=None, broker_map=broker_map)`. Net effect: one guard set, no ~200-line duplication, identical behavior. Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short` (all prior reconcile tests must still pass). Commit as `fix(t3.1): extract shared reconcile core; both entry points delegate`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/gateway/test_gateway.py::test_startup_reconcile_delegates_to_shared_core tests/gateway/test_gateway.py::test_reconcile_once_skips_on_broker_query_failure -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/gateway/gateway.py contains `async def _reconcile_core(self, store, manager, alerter, broker_map` and a broker_map builder helper
    - Both reconcile_once and startup_reconcile delegate to _reconcile_core (proven by the spy/patch test)
    - The Plan 01 failed-query guard remains in reconcile_once (test_reconcile_once_skips_on_broker_query_failure still passes)
    - The duplicated ~200-line close/adopt/protect body no longer exists in both methods
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>reconcile_once and startup_reconcile share one core path with a single guard set; the 1.2 failed-query guard is preserved. One atomic `fix(t3.1)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 2: Finding 3.2 — intraday rescan reuses a trading-day-keyed daily-bar cache</name>
  <files>bot/service/bot.py, bot/scanner/scanner.py, tests/scanner/test_scanner.py</files>
  <read_first>
    - bot/scanner/scanner.py lines 236-240 (run_daily_scan downloads via download_daily_bars — the single source of truth) and 434-495 (run_intraday_rescan; where the daily download happens per call)
    - bot/scanner/fetcher.py line 285 (download_daily_bars signature/return: (data, failed))
    - bot/service/bot.py lines 594-648 (_job_intraday_rescan worker; the run_intraday_rescan call site) and TradingBot.__init__ (where to add self._daily_bar_cache)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 3 row 3.2, RESEARCH Open Question 3 (cache location — caller-owned dict keyed on trading date; avoid a module-level mutable singleton per architecture constraints)
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 3.2"
    - tests/scanner/test_scanner.py (existing _make_daily_frame helpers; how download_daily_bars is mocked)
  </read_first>
  <behavior>
    RED — write tests/scanner/test_scanner.py::test_rescan_reuses_daily_download_cache first: with download_daily_bars mocked, calling run_intraday_rescan twice for the SAME scan_date using a shared caller-owned cache dict invokes download_daily_bars exactly once (second call reuses the cached bars); a different scan_date triggers a fresh download.
  </behavior>
  <action>
    Add a `daily_bars_cache: dict | None = None` optional parameter to bot/scanner/scanner.py::run_intraday_rescan (keyed on scan_date). Inside, if a cache dict is provided and contains scan_date, reuse `cache[scan_date]` instead of calling download_daily_bars; otherwise download once and store it under scan_date in the provided cache. This keeps the cache owned by the caller (no module-level mutable singleton, consistent with the codebase's immutable-config constraint). In bot/service/bot.py::TradingBot.__init__ add `self._daily_bar_cache: dict = {}`, and in _job_intraday_rescan pass `daily_bars_cache=self._daily_bar_cache` to run_intraday_rescan. Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t3.2): reuse trading-day-keyed daily-bar cache across intraday rescans`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/scanner/test_scanner.py::test_rescan_reuses_daily_download_cache -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/scanner/scanner.py::run_intraday_rescan accepts a caller-owned `daily_bars_cache` dict keyed on scan_date and reuses cached bars for a repeated same-day call
    - download_daily_bars is called exactly once across two same-day run_intraday_rescan calls sharing the cache (proven by the test); a different scan_date re-downloads
    - bot/service/bot.py::TradingBot.__init__ initializes self._daily_bar_cache and _job_intraday_rescan passes it in
    - No module-level mutable cache singleton is introduced
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>An intraday rescan no longer re-downloads the full ~500-symbol universe every 30 minutes; the same trading day's daily bars are downloaded once and reused. One atomic `fix(t3.2)` commit exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenD broker → reconcile core | Both reconcile entry points cross the broker boundary; a single guard set prevents drift where one path trusts a failed query |
| yfinance daily download → rescan | Cached daily bars must be scoped to the trading date so a stale day's data is never reused across days |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-12 | Tampering (guard drift) | gateway reconcile paths (3.1) | mitigate | Single _reconcile_core with one guard set; both entry points delegate so the 1.2 failed-query guard cannot drift out of one path |
| T-06.2-13 | Tampering (stale reuse across days) | scanner daily-bar cache (3.2) | mitigate | Cache keyed strictly on scan_date; a different trading date forces a fresh download (no cross-day reuse) |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit N/A — internal edits only) |
</threat_model>

<verification>
- After each finding task: `python3 -m pytest tests/ -q --tb=short` must report 0 failed.
- After both findings (Tier 3 complete): `python3 -m pytest tests/ -v` — zero failures.
- Phase gate: run `/gsd-verify-work` after Tier 3.
</verification>

<success_criteria>
- Two atomic `fix(t3.N)` commits exist, each with its regression test written first.
- `python3 -m pytest tests/ -v` is green, zero failures.
- One shared reconcile core (single guard set, 1.2 guard preserved); daily bars downloaded once per trading day and reused across rescans.
- Phase verifier (`/gsd-verify-work`) passes.
</success_criteria>

<artifacts_this_phase_produces>
New symbols introduced by Plan 03 (exclude from cross-AI drift verification — created here):

**New gateway helpers**
- bot/gateway/gateway.py::_reconcile_core(self, store, manager, alerter, broker_map)
- bot/gateway/gateway.py::_build_broker_map(broker_data) (or equivalently-named shared broker-map parser)

**New scanner parameter + bot cache**
- bot/scanner/scanner.py::run_intraday_rescan new optional parameter `daily_bars_cache`
- bot/service/bot.py::TradingBot attribute `self._daily_bar_cache`

**New tests**
- tests/gateway/test_gateway.py::test_startup_reconcile_delegates_to_shared_core
- tests/scanner/test_scanner.py::test_rescan_reuses_daily_download_cache

**Reused (NOT new) — do not flag as drift**
- reconcile_once / startup_reconcile (gateway.py:818/1025), the Plan 01 1.2 guard + test_reconcile_once_skips_on_broker_query_failure, download_daily_bars (fetcher.py:285), run_intraday_rescan active_codes protection (scanner.py:434)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06.2-code-review-remediation/06.2-03-SUMMARY.md` when done
</output>
