---
phase: 06.2-code-review-remediation
verified: 2026-07-07T23:45:00Z
status: human_needed
score: 16/16 automated findings verified; 1 human gate outstanding
overrides_applied: 0
gaps: []
human_verification:
  - test: "Tier 1 live premarket→close paper UAT on a DEDICATED SIMULATE account (not shared acc 1727266)"
    expected: "(a) an entry fill is managed immediately via register_position/on_fill — NOT orphan-adopted ~75s later; (b) the position's stop equals rules.json initial_stop, never LOD-1%; (c) the force-close job's scheduled run time matches today's calendar close-9min; (d) after force-close, the EOD report shows the trade with non-zero P&L rows"
    why_human: "Requires a live OpenD connection and a real/paper market session; cannot be verified by static analysis or the unit-test suite. This is the phase's own explicit blocking checkpoint (06.2-01-PLAN.md Task 6, 'never auto-approvable')."
---

# Phase 06.2: Code-Review Remediation Verification Report

**Phase Goal:** Close all 16 confirmed findings from the 2026-07-02 multi-agent code review (develop vs main) so the bot is safe for an unattended live paper run (Tier 1 blockers, findings 1.1-1.5), behaviorally correct (Tier 2, findings 2.1-2.8), and maintainable (Tier 3, findings 3.1-3.2).
**Verified:** 2026-07-07T23:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths — Tier 1 (Live-Run Blockers)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1.1 | A filled entry is registered with PositionManager immediately in `_process_bar`, not orphan-adopted ~75s later; entry stop = intent stop (rules.json), never LOD-1% | ✓ VERIFIED | `bot/service/bot.py:300-325` — `fill = await self._execution_engine.consume_intent(intent)`; on fill: builds `PositionState(phase=AWAITING_FILL, initial_stop=intent.stop_price, entry_order_id=fill.order_id)`, calls `register_position(pos)` then `on_fill(fill)`. Test: `tests/service/test_bot.py::test_process_bar_registers_position_on_fill` — PASSED (asserts `register_position` called once, `on_fill` called once with the fill) |
| #5 | `signal_engine._pending_count` is decremented on every intent resolution — both fill and abandon paths | ✓ VERIFIED | `bot/service/bot.py:319-325` — abandon branch calls `store.resolve_pending_intent(..., "ABANDONED")`; `note_intent_resolved()` called unconditionally after the if/else (both paths). Tests: `test_process_bar_resolves_intent_on_fill`, `test_process_bar_resolves_intent_on_abandon`, `TestNoteIntentResolved::test_note_intent_resolved_decrements_pending_count` — all PASSED |
| 1.2 | A failed `position_list_query` (ret != RET_OK) skips the reconcile cycle; positions never marked externally-closed from a failed query | ✓ VERIFIED | `bot/gateway/gateway.py:1333-1350` — guard immediately after `get_positions()`: `if ret_pos != RET_OK or broker_data is None: ... return {}` before any `broker_map` construction. Test: `test_reconcile_once_skips_on_broker_query_failure` — PASSED |
| 1.3 | After an OpenD reconnect, the 5m bar handler is re-registered so bars flow again | ✓ VERIFIED | `bot/service/watchdog.py:225-228` — `if self._bot._bar_agg is not None: self._gateway.set_handler(self._bot._bar_agg)` + `bar_handler_reregistered_after_reconnect` log. Test: `test_reconnect_reregisters_bar_handler` — PASSED |
| 1.4 | Cancel-replace computes replacement quantity from post-cancel `dealt_qty`, never a pre-cancel snapshot | ✓ VERIFIED | `bot/execution/engine.py:521-539` — re-queries `get_order_status(order_id)` after cancel, uses `post_cancel_filled` (not `order_filled_this_round`, the pre-cancel value) to compute `total_filled`/remaining. Test: `test_manage_exit_requeries_dealt_qty_after_cancel` — PASSED |
| 1.5 | The force-close job fires at market-close minus 9 minutes on half-days (12:51 ET), rescheduled each trading day | ✓ VERIFIED | `bot/service/bot.py:466-480` (DateTrigger registration, id="force_close") and `:592-609` (`_job_premarket_scan` calls `get_force_close_time_et(today)` then `reschedule_job("force_close", ...)`, logs `force_close_job_rescheduled`). Test: `test_premarket_scan_reschedules_force_close_for_half_day` — PASSED |

### Observable Truths — Tier 2 (Correctness)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 2.1 | Concurrent-position gate counts only bot-owned (DB) rows with `remaining_quantity>0`, never qty=0 rows or manual holdings | ✓ VERIFIED | `bot/signal/signal_engine.py:600-604` — `open_position_count = sum(1 for p in self._store.get_open_positions() if (p.get("remaining_quantity") or 0) > 0 and p.get("phase") != "CLOSED")`. Test: `TestSignalEngineConcurrentCap::test_signal_engine_excludes_zero_qty_and_external_positions` — PASSED |
| 2.2 | Intraday rescan passes real active codes (open positions + in-flight intents); open-position symbols never evicted | ✓ VERIFIED | `bot/service/bot.py:786-798` — builds `active_codes` from `get_open_positions()` (qty>0, not CLOSED) + `get_pending_intent_codes()`, passed to `run_intraday_rescan(active_codes=active_codes, ...)`. Test: `test_intraday_rescan_passes_open_position_codes` — PASSED |
| 2.3 | A completed trade writes a row to the trades table on final exit fill | ✓ VERIFIED | `bot/position/manager.py:708-748` — on `remaining_quantity == 0`, calls `self._store.record_trade(position_id=..., entry_price=pos.entry_price, exit_price=fill.avg_fill_price, ...)`; never called on a partial exit (guarded by the `if pos.remaining_quantity == 0:` block). Test: `TestExitFill::test_exit_fill_writes_trade_row_to_store` — PASSED |
| 2.4 | bid/ask snapshot failure raises `GatewayError` instead of returning 0.0; a stop-out is never silently aborted | ✓ VERIFIED | `bot/gateway/gateway.py:988,1017` — `raise GatewayError(...)` on ret!=RET_OK and on zero-price-after-fallback. `bot/execution/engine.py:106-` — `_get_price_with_fallback` bounded retry + last-known-good fallback, wired into all 5 bid/ask call sites (entry + exit). Test: `TestGetBidAskPriceRaisesOnFailure::test_get_bid_price_raises_on_snapshot_failure` — PASSED |
| 2.5 | Orphan adoption never inserts a duplicate open row for a code already open | ✓ VERIFIED | `bot/gateway/gateway.py:1236-1245` — SELECT-before-INSERT guard: `if code in open_pos_codes: log("orphan_adoption_skipped_duplicate_code"); continue`. Test: `TestOrphanOwnershipGuard::test_adopt_orphan_rejects_duplicate_code` — PASSED |
| 2.6 | A stale `prior_row` (>5 days before scan_date) skips the symbol instead of inflating the gap | ✓ VERIFIED | `bot/scanner/scanner.py:212-223` — computes `prior_date`, `if (scan_ts.date() - prior_date).days > 5: log("scanner_skipped_stale_prior_row"); return None`. Test: `TestEvaluateSymbolTodayPrice::test_evaluate_symbol_skips_on_stale_prior_row` — PASSED |
| 2.7 | Stop-out exit alerts use the stop price as the exit proxy so R is meaningful (not ~0) | ✓ VERIFIED | `bot/position/manager.py:934,958` (`_trigger_stop_out`) — `exit_proxy = pos.trail_stop or entry` (both alert sites). `_on_exit_fill`'s post-fill R (unaffected, still uses real fill price) unchanged. Test: `test_stop_out_alert_uses_stop_as_exit_proxy` — PASSED |
| 2.8 | Orphan-adoption stop is computed from rules.json `initial_stop_pct`, never a hardcoded `lod*0.99` | ✓ VERIFIED | `bot/gateway/gateway.py:305,317,1447` — `__init__(..., initial_stop_pct: float = 1.0)`, `_compute_orphan_stop` returns `lod * (1.0 - self._initial_stop_pct/100.0)`; `bot/main.py:77` threads `cfg.initial_stop_pct` in. Test: `TestComputeOrphanStopUsesConfigPct::test_compute_orphan_stop_uses_config_pct` — PASSED |

### Observable Truths — Tier 3 (Hygiene)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 3.1 | `reconcile_once`/`startup_reconcile` share one extracted reconcile core; single guard set, no ~200-line duplication; the 1.2 failed-query guard is preserved | ✓ VERIFIED | `bot/gateway/gateway.py:1030` (`_build_broker_map`), `:1054` (`_reconcile_core`), `:1318-1355` (`reconcile_once` keeps the 1.2 guard, then delegates), `:1357-` (`startup_reconcile` delegates with `alerter=None`). Tests: `test_startup_reconcile_delegates_to_shared_core` AND `test_reconcile_once_skips_on_broker_query_failure` (regression, still preserved) — both PASSED |
| 3.2 | Intraday rescan reuses the same trading-day's daily download instead of re-downloading the ~500-symbol universe every 30 min | ✓ VERIFIED | `bot/scanner/scanner.py:282,337-342` (`daily_bars_cache` param, reuse-or-populate-by-`scan_date`); `bot/service/bot.py:129,801` (`self._daily_bar_cache = {}`, threaded into `run_intraday_rescan`). Test: `TestIntradayRescan::test_rescan_reuses_daily_download_cache` — PASSED |

**Score:** 16/16 findings verified in code with a passing regression test that pins the fix (test asserts the specific pre-fix-bug behavior cannot occur — e.g. `mark_position_closed` NOT called, replacement qty computed from post-cancel value, duplicate insert rejected, etc.)

### Full Suite Result (run directly by this verifier, not taken from SUMMARY claims)

```
$ python3 -m pytest tests/ -q
649 passed, 1 skipped in 37.22s
```

Matches all three SUMMARYs' claimed counts (496 after Plan 01, 647 after Plan 02, 649 after Plan 03; the 1 skip is the pre-existing manual live-SDK test). 0 failures.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `bot/service/bot.py` | `_process_bar` fill-wiring + force_close DateTrigger reschedule | ✓ VERIFIED | Contains `register_position(`, `on_fill(fill)`, `DateTrigger`, `reschedule_job` |
| `bot/gateway/gateway.py` | reconcile guard, `_reconcile_core`, orphan dup guard, config-driven orphan stop, GatewayError raises | ✓ VERIFIED | All present and wired (see per-finding rows above) |
| `bot/service/watchdog.py` | `_on_reconnect` re-registers bar handler | ✓ VERIFIED | `set_handler(` present, guarded, logged |
| `bot/execution/engine.py` | post-cancel `dealt_qty` re-query; price fallback helper | ✓ VERIFIED | `_get_price_with_fallback`, post-cancel re-query present |
| `bot/signal/signal_engine.py` | gate counts DB-owned qty>0 positions | ✓ VERIFIED | `get_open_positions()`-derived count |
| `bot/state/store.py` | trade writer (`record_trade`, pre-existing, now wired to a live caller) | ✓ VERIFIED | `def record_trade` at line 344, called from `manager.py:737` |
| `bot/scanner/scanner.py` | prior_row staleness skip; daily-bar cache reuse | ✓ VERIFIED | Both present |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `bot/service/bot.py::_process_bar` | `bot/position/manager.py::on_fill` | `position_manager.on_fill(fill)` after `register_position(pos)` | ✓ WIRED |
| `bot/service/watchdog.py::_on_reconnect` | `bot/gateway/gateway.py::set_handler` | `gateway.set_handler(bot._bar_agg)` after resubscribe | ✓ WIRED |
| `bot/service/bot.py::_job_premarket_scan` | APScheduler `force_close` job | `reschedule_job('force_close', DateTrigger(...))` | ✓ WIRED |
| `bot/position/manager.py::_on_exit_fill` | `bot/state/store.py::record_trade` | called when `remaining_quantity == 0` | ✓ WIRED |
| `bot/service/bot.py::_job_intraday_rescan` | `bot/scanner/scanner.py::run_intraday_rescan` | `active_codes=` assembled from DB + pending intents | ✓ WIRED |
| `bot/main.py` | `bot/gateway/gateway.py::MoomooGateway` | `initial_stop_pct=cfg.initial_stop_pct` | ✓ WIRED |
| `bot/gateway/gateway.py::startup_reconcile` | `bot/gateway/gateway.py::_reconcile_core` | delegates with `alerter=None` | ✓ WIRED |
| `bot/service/bot.py::_job_intraday_rescan` | `bot/scanner/scanner.py::run_intraday_rescan` | `daily_bars_cache=self._daily_bar_cache` | ✓ WIRED |

### Requirements Coverage

All requirement IDs declared across the three plans' frontmatter were cross-referenced against `.planning/REQUIREMENTS.md`:

| Requirement | Source Plan | Status | Evidence |
|-------------|------------|--------|----------|
| EXEC-01, EXEC-05, POS-04, SVC-02, ALERT-01 | 06.2-01 | SATISFIED (pre-existing, from Phase 4/5) | These IDs appear in REQUIREMENTS.md marked `[ ]`/"Pending" in the traceability table, but ROADMAP.md confirms Phase 4 and Phase 5 are `[x]` complete (2026-06-24) and this is a documented, pre-existing staleness of REQUIREMENTS.md's traceability table (not introduced or worsened by 06.2 — see MEMORY.md note "REQUIREMENTS.md traceability stale"). Plan 06.2-01 correctly *reuses* the existing EXEC-05 order_id-matching and POS-04 calendar-aware force_close machinery (per 06.2-SPEC.md "Key mechanic") rather than re-implementing them; the actual gap fixed here was the wiring (Finding 1.1) and the calendar-trigger mechanism (Finding 1.5), which are 06.2-scoped and verified above. |
| POS-05, SAFE-02, SAFE-03, RISK-05 | 06.2-01/02/03 | SATISFIED | Marked Complete/Implemented in REQUIREMENTS.md; 06.2 fixes reinforce existing behavior (broker-truth-wins reconcile, daily entry cap) without contradicting the requirement text |
| RISK-04, CFG-01, ALERT-02, ALERT-03 | 06.2-02 | SATISFIED | Marked Complete in REQUIREMENTS.md; 06.2-02 fixes correctness bugs in already-implemented features (cap counting, config-driven stop, exit-alert R) |
| TDD-REGRESSION, SUITE-GREEN, UAT-TIER1 | 06.2-01/02/03 | Not itemized in REQUIREMENTS.md | These are phase-level process-contract labels (stated in ROADMAP.md's Phase 06.2 "Requirements" line: "Regression-test-first (TDD) per finding; full suite green per tier; live premarket UAT gates Tier 1 sign-off"), not formal requirement IDs — correctly not present in REQUIREMENTS.md as line items. TDD-REGRESSION and SUITE-GREEN are satisfied (verified above). UAT-TIER1 is the one open item — see Human Verification. |

No orphaned requirements found mapped to Phase 06.2 in REQUIREMENTS.md (06.2 is a bug-fix/gap-closure phase layered on top of existing Phase 4/5 requirements, not a net-new requirement phase).

### Anti-Patterns Found

None. Scanned all 9 modified source files (`bot/service/bot.py`, `bot/gateway/gateway.py`, `bot/service/watchdog.py`, `bot/execution/engine.py`, `bot/signal/signal_engine.py`, `bot/state/store.py`, `bot/position/manager.py`, `bot/scanner/scanner.py`, `bot/main.py`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` — zero matches.

### Deferred Item (informational, not a gap)

`bot/position/manager.py:585,833` — the `force_close` and `partial`-profit exit alerts still compute `exit_proxy = pos.avg_fill_price or entry` (same R≈0 imprecision class as Finding 2.7), logged in `deferred-items.md` by the executor. This is correctly out of scope: 06.2-SPEC.md's Finding 2.7 text and the plan's `<action>` explicitly scope the fix to `_trigger_stop_out` only ("~724 and ~747" in the original line numbering / `TestExitFill`/stop-out sites), and neither `force_close` nor `partial` exits have a natural "stop" reference to substitute — picking one is a design decision beyond this finding's boundary. Does not affect the truth "stop-out exit alerts use the stop price as the exit proxy," which is fully verified.

## Critical Process Finding: Tier 1 live-UAT gate was bypassed before Tier 2/3 execution

This is not a code gap (all 16 findings are correctly implemented and tested), but it is a violation of the phase's own explicit contract that must be surfaced:

- `06.2-01-PLAN.md` Task 6 is a `checkpoint:human-verify` with `gate="blocking"`: *"Do NOT auto-advance (this checkpoint is never auto-approvable) ... Pause and wait for the operator's explicit sign-off before any Tier 2 (Plan 02) work begins."*
- `06.2-SPEC.md` "Ordering & dependencies": *"Tier 1 completes → full test suite + live premarket UAT before Tier 2/3 merge."*
- `ROADMAP.md` line ~301-304 itself documents: *"Wave 2 (blocked on Wave 1 completion)"* — yet `06.2-02-PLAN.md` is marked `[x]` complete.
- `.planning/phases/06.2-code-review-remediation/06.2-VALIDATION.md` row `06.2-01-06` (the live UAT checkpoint) is still `⬜ pending` — never updated to green, and no UAT session artifact (log excerpt, dashboard screenshot, Telegram transcript) exists anywhere in the repo or `~/.futu_trade_audit.jsonl` (that file's `opend_reconnect`/`opend_disconnect` entries near the Tier 3 completion timestamp are test-suite noise — 84k lines spanning back to 2026-06-12, including a `crash_sim_test` event type — not a live trading session).
- Git timestamps confirm the sequencing: Tier 1 merged 2026-07-02 13:11 (`7018662`); Tier 2 started 2026-07-07 15:30 (`a4b0d9b`) and Tier 3 finished 2026-07-07 16:19 (`8f49b4c`) — five days later, same day, with no UAT evidence in between.

**Recommendation:** Before this phase can be considered fully closed per its own contract, the operator must either (a) perform the live premarket→close UAT on a dedicated SIMULATE account and update `06.2-VALIDATION.md`/this VERIFICATION.md accordingly, or (b) explicitly accept the deviation via a VERIFICATION.md override (documenting why the gate was skipped and that Tier 2/3 are accepted without it). This does not block using the code — all fixes are real and tested — but it does mean the phase's own "unattended live paper run" safety claim for Tier 1 has not yet been exercised end-to-end against a live broker.

### Human Verification Required

#### 1. Tier 1 Live Premarket→Close Paper UAT

**Test:** On a DEDICATED SIMULATE account (not shared account 1727266), launch the bot before premarket with OpenD running and logged in on 127.0.0.1:11111. Let it scan, subscribe, and take at least one entry through to force-close.
**Expected:** (a) the entry fill is managed immediately via register_position/on_fill — confirmed by the entry alert firing before any orphan-adoption event in the logs; (b) the position's stop equals the rules.json `initial_stop` (not LOD-1%); (c) the force-close job's scheduled run time matches today's calendar close-9min; (d) after force-close, the EOD report shows the trade with non-zero P&L.
**Why human:** Requires a live OpenD connection and a real market/paper session; this is the phase's own explicit blocking checkpoint and cannot be satisfied by the unit-test suite.

## Gaps Summary

No code-level gaps. All 16 findings (Tier 1: 5, Tier 2: 8, Tier 3: 2) are implemented in the codebase with a passing regression test that pins the specific pre-fix bug behavior, verified by direct source inspection (not SUMMARY claims) and by running the full suite myself (649 passed, 1 skipped, 0 failed). The sole outstanding item is the live premarket→close UAT sign-off for Tier 1 — an explicit human-only gate per the phase's own contract — which has not yet been performed, and which the phase's plans proceeded past (Tier 2/3 executed) without waiting for, per ROADMAP.md's own "blocked on Wave 1 completion" annotation. This is flagged as a process/governance finding for the operator's attention, not as a reason to reject the code changes.

---

_Verified: 2026-07-07T23:45:00Z_
_Verifier: Claude (gsd-verifier)_
