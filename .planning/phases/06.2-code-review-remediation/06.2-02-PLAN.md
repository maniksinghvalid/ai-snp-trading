---
phase: 06.2-code-review-remediation
plan: 02
type: execute
wave: 2
depends_on: [06.2-01]
files_modified:
  - bot/signal/signal_engine.py
  - bot/service/bot.py
  - bot/state/store.py
  - bot/position/manager.py
  - bot/gateway/gateway.py
  - bot/scanner/scanner.py
  - bot/execution/engine.py
  - bot/main.py
  - tests/signal/test_signal_engine.py
  - tests/service/test_bot.py
  - tests/state/test_store.py
  - tests/position/test_manager.py
  - tests/gateway/test_gateway.py
  - tests/scanner/test_scanner.py
autonomous: true
requirements: [RISK-04, RISK-05, CFG-01, POS-05, ALERT-02, ALERT-03, SAFE-03, TDD-REGRESSION, SUITE-GREEN]

must_haves:
  truths:
    - "The concurrent-position gate counts only bot-owned (DB positions table) rows with remaining_quantity>0, never qty=0 rows or manual holdings on the shared account"
    - "Intraday rescan passes real active codes (open positions + in-flight intents); open-position symbols are never evicted/unsubscribed"
    - "A completed trade writes a row to the trades table on final exit fill, so the EOD report shows real trades and P&L"
    - "bid/ask snapshot failure raises GatewayError instead of returning 0.0; a stop-out is never silently aborted at a $0 limit"
    - "Orphan adoption never inserts a duplicate open row for a code already open"
    - "A stale prior_row (>5 days before scan_date) skips the symbol instead of inflating the gap"
    - "Stop-out exit alerts use the stop price as the exit proxy so R is meaningful (not ~0)"
    - "Orphan-adoption stop is computed from rules.json initial_stop_pct, never a hardcoded lod*0.99"
  artifacts:
    - path: "bot/state/store.py"
      provides: "insert_trade writer for the trades table"
      contains: "def insert_trade"
    - path: "bot/signal/signal_engine.py"
      provides: "gate counts DB-owned qty>0 positions"
      contains: "get_open_positions"
    - path: "bot/gateway/gateway.py"
      provides: "bid/ask raise on failure; orphan dup guard; config-driven orphan stop"
      contains: "GatewayError"
    - path: "bot/scanner/scanner.py"
      provides: "prior_row staleness skip"
      contains: "prior_row"
  key_links:
    - from: "bot/position/manager.py::_on_exit_fill"
      to: "bot/state/store.py::insert_trade"
      via: "insert_trade(trade_dict) when remaining_quantity == 0"
      pattern: "insert_trade\\("
    - from: "bot/service/bot.py::_job_intraday_rescan"
      to: "bot/scanner/scanner.py::run_intraday_rescan"
      via: "active_codes assembled from store.get_open_positions + get_pending_intent_codes"
      pattern: "active_codes="
    - from: "bot/main.py"
      to: "bot/gateway/gateway.py::MoomooGateway"
      via: "MoomooGateway(get_gateway_config(), initial_stop_pct=strategy_cfg.initial_stop_pct)"
      pattern: "initial_stop_pct"
---

<objective>
Close the eight Tier 2 correctness findings (2.1–2.8) so the bot's gates, reporting, alerts, and orphan handling are behaviorally correct once positions actually flow through the manager (Plan 01 / 1.1). Fixes 2.1 (gate counting), 2.2 (rescan active codes), and 2.3 (trades writer) depend on the Plan 01 fill path landing first. The remaining five harden broker reads (2.4), orphan uniqueness (2.5), scanner staleness (2.6), exit-alert R (2.7), and config-driven orphan stops (2.8).

Purpose: Make cap enforcement, subscription hygiene, EOD reporting, exit pricing, and orphan adoption trustworthy on a shared/paper account.
Output: Eight atomic `fix(t2.N)` commits (regression test first, then fix) and a full green suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-code-review-remediation/06.2-SPEC.md
@.planning/phases/06.2-code-review-remediation/06.2-RESEARCH.md
@.planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md
@.planning/phases/06.2-code-review-remediation/06.2-01-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Finding 2.1 — concurrent-position gate counts only bot-owned qty>0 positions</name>
  <files>bot/signal/signal_engine.py, tests/signal/test_signal_engine.py</files>
  <read_first>
    - bot/signal/signal_engine.py lines 75 (self._store), 505-540 (gate 4/5 block: open_position_count = len(positions_data) at 520, max_concurrent_positions check at 533)
    - bot/state/store.py lines 407-430 (get_open_positions returns list of dicts with column names incl. phase, remaining_quantity)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.1
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.1" and "StrategyConfig Defaults for Tests"
    - tests/signal/test_signal_engine.py (make_cfg, _EXECUTION_DEFAULTS/_SERVICE_DEFAULTS, existing gate-count tests)
  </read_first>
  <behavior>
    RED — write tests/signal/test_signal_engine.py::test_signal_engine_excludes_zero_qty_and_external_positions first: with store.get_open_positions returning a mix (one row remaining_quantity=0, one CLOSED-phase, and N with remaining_quantity>0 and phase not CLOSED), the computed open_position_count equals only the N qty>0 non-closed rows — qty=0 and external/non-DB rows do not count toward the cap.
  </behavior>
  <action>
    In bot/signal/signal_engine.py gate block, replace `open_position_count = len(positions_data)` (line 520) with a count sourced from `self._store.get_open_positions()`, summing rows where `(row.get("remaining_quantity") or 0) > 0` and `row.get("phase") != "CLOSED"`. This counts only bot-owned positions (DB positions table), excluding qty=0 rows and manual holdings visible only in the broker snapshot on shared account 1727266. Keep the broker `get_positions()` snapshot for the open_codes / re-entry logic if still used, but base the CAP count on the DB. Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.1): count only bot-owned qty>0 positions for the concurrency cap`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/signal/test_signal_engine.py::test_signal_engine_excludes_zero_qty_and_external_positions -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/signal/signal_engine.py derives the cap count from self._store.get_open_positions() filtered to remaining_quantity>0 and phase != "CLOSED"
    - qty=0 rows and rows not present in the DB positions table do not increment open_position_count (proven by the test)
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>The RISK-04 concurrency gate reflects only bot-managed live positions; manual/zero-qty holdings on the shared account no longer saturate the cap. One atomic `fix(t2.1)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 2: Finding 2.2 — intraday rescan passes real active codes (open + in-flight)</name>
  <files>bot/service/bot.py, tests/service/test_bot.py</files>
  <read_first>
    - bot/service/bot.py lines 594-648 (_job_intraday_rescan; the active_codes=set() at 632 and the run_intraday_rescan call)
    - bot/scanner/scanner.py lines 319-357 (_subscribe_new_codes / _unsubscribe_evicted_codes — active_codes protected from eviction) and 434-495 (run_intraday_rescan honoring active_codes protection + 20-cap)
    - bot/state/store.py lines 407 (get_open_positions) and 539 (get_pending_intent_codes)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.2
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.2"
    - .planning/phases/06.2-code-review-remediation/06.2-01-SUMMARY.md (fill path — positions now enter the manager/DB)
  </read_first>
  <behavior>
    RED — write tests/service/test_bot.py::test_intraday_rescan_passes_open_position_codes first: with store.get_open_positions returning a position for US.AAPL (qty>0) and get_pending_intent_codes returning US.MSFT, _job_intraday_rescan calls scanner.run_intraday_rescan with active_codes containing both US.AAPL and US.MSFT (not an empty set).
  </behavior>
  <action>
    In bot/service/bot.py::_job_intraday_rescan, build `active_codes` inside the worker from real state: union of codes from `self._store.get_open_positions()` where remaining_quantity>0 and phase != "CLOSED", plus `self._store.get_pending_intent_codes()`. Pass that set as `active_codes=active_codes` to run_intraday_rescan (replacing `active_codes=set()` at line 632). Rely on the existing scanner protection (active_codes are never evicted/unsubscribed and the watchlist stays capped at 20). Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.2): pass real active codes to intraday rescan to protect open positions`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/service/test_bot.py::test_intraday_rescan_passes_open_position_codes -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/service/bot.py::_job_intraday_rescan passes a non-empty `active_codes` assembled from get_open_positions (qty>0) plus get_pending_intent_codes
    - The literal `active_codes=set()` no longer appears in _job_intraday_rescan
    - An open-position code is included in active_codes so the scanner cannot evict/unsubscribe it (proven by the test)
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>Rescan no longer leaks subscriptions past the 20-cap and never unsubscribes a symbol with an open position or in-flight intent. One atomic `fix(t2.2)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 3: Finding 2.3 — trades table writer on final exit fill</name>
  <files>bot/state/store.py, bot/position/manager.py, tests/state/test_store.py, tests/position/test_manager.py</files>
  <read_first>
    - bot/state/store.py lines 291-334 (upsert_position lock+execute+commit pattern to copy), 340-406 (get_closed_trades / get_daily_trade_stats — the readers with no writer)
    - bot/state/migrations.py migration 0001 (trades schema: trade_id PK, position_id NOT NULL, code NOT NULL, entry_price NOT NULL, exit_price NOT NULL, quantity NOT NULL, exit_reason NOT NULL, r_multiple, closed_at NOT NULL)
    - bot/position/manager.py lines 499-560 (_on_exit_fill: closes at remaining_quantity==0, computes r_multiple, has pos.entry_price / pos.initial_stop / pos.full_quantity / pos.pending_exit_reason; fill.avg_fill_price / fill.fill_time)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.3, RESEARCH Pattern 5 / Pitfall 6 (NOT NULL columns → write the complete row only at final exit)
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.3" and "DB-First Write"
    - tests/state/test_store.py (existing store fixtures, tmp_state_db) and tests/position/test_manager.py (_make_pos, _make_fill)
  </read_first>
  <behavior>
    RED — write two failing tests first:
    - tests/state/test_store.py — insert_trade(trade_dict) writes a row retrievable via get_closed_trades(session_date) with matching entry_price/exit_price/quantity/r_multiple/closed_at.
    - tests/position/test_manager.py::test_exit_fill_writes_trade_row_to_store — driving _on_exit_fill to remaining_quantity==0 against a real store (tmp_state_db) inserts exactly one trades row with exit_price == fill.avg_fill_price and entry_price == pos.entry_price; a PARTIAL exit (remaining_quantity>0) writes NO trades row.
  </behavior>
  <action>
    In bot/state/store.py add `def insert_trade(self, trade: dict) -> None` using the exact lock+execute+commit pattern from upsert_position; INSERT OR IGNORE into trades all nine columns (trade_id, position_id, code, entry_price, exit_price, quantity, exit_reason, r_multiple, closed_at) from the dict (r_multiple via .get). In bot/position/manager.py::_on_exit_fill, when `pos.remaining_quantity == 0` (the CLOSED branch, DB-first before returning), build the complete trade dict: trade_id=str(uuid4()), position_id=pos.position_id, code=pos.code, entry_price=pos.entry_price, exit_price=fill.avg_fill_price, quantity=pos.full_quantity, exit_reason=pos.pending_exit_reason or "exit_fill", r_multiple=round(r_multiple,2) (reuse the R computed in the alert block), closed_at=fill.fill_time.isoformat(); call self._store.insert_trade(trade). Never insert on a partial exit (schema requires exit_price/closed_at NOT NULL). Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.3): write completed trade row to trades table on final exit fill`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/position/test_manager.py::test_exit_fill_writes_trade_row_to_store tests/state/test_store.py -k insert_trade -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/state/store.py contains `def insert_trade(self, trade: dict)` writing all nine trades columns under self._lock with commit
    - bot/position/manager.py::_on_exit_fill calls self._store.insert_trade only when pos.remaining_quantity == 0 (never on a partial exit)
    - The inserted row has exit_price == fill.avg_fill_price and entry_price == pos.entry_price
    - get_closed_trades / get_daily_trade_stats return the written trade (EOD report no longer 0 trades / $0)
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>Every fully-closed position writes exactly one trades row; the EOD report and daily summary reflect real trades and P&L. One atomic `fix(t2.3)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 4: Finding 2.4 — bid/ask snapshot failure raises GatewayError; stop-out never silently aborts</name>
  <files>bot/gateway/gateway.py, bot/execution/engine.py, tests/gateway/test_gateway.py</files>
  <read_first>
    - bot/gateway/gateway.py lines 772-812 (get_ask_price and get_bid_price — both return 0.0 on ret != RET_OK / empty data at 786/807) and 148 (GatewayError)
    - bot/execution/engine.py (exit-limit computation / stop-out path that consumes get_bid_price/get_ask_price; where a last-bar close is available as fallback)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.4, RESEARCH Pitfall 4 (0.0 sentinel → negative/zero exit limits)
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.4" and "GatewayError Raise Pattern"
    - tests/gateway/test_gateway.py (GatewayError import at line 29, _make_gateway_with_mocks)
  </read_first>
  <behavior>
    RED — write tests/gateway/test_gateway.py::test_get_bid_price_raises_on_snapshot_failure first: when the snapshot returns ret != RET_OK (or empty data), get_bid_price raises GatewayError (does not return 0.0); likewise a zero/N-A price raises rather than returning 0.0.
  </behavior>
  <action>
    In bot/gateway/gateway.py::get_bid_price and get_ask_price, replace the `return 0.0` on failure with `raise GatewayError(...)` including code and ret in the message; also raise if the parsed price is 0.0 after last_price fallback (halted/illiquid). In bot/execution/engine.py, wrap the stop-out/exit bid/ask read so a GatewayError triggers a bounded retry and then falls back to the last bar close price for the exit limit — the stop-out is never silently aborted or priced at $0. Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.4): raise on bid/ask snapshot failure and fall back to last close for stop-out`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/gateway/test_gateway.py::test_get_bid_price_raises_on_snapshot_failure -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/gateway/gateway.py::get_bid_price and get_ask_price raise GatewayError on ret != RET_OK / empty data (no `return 0.0` on the failure path)
    - A zero parsed price (after last_price fallback) raises GatewayError rather than returning 0.0
    - bot/execution/engine.py handles the GatewayError with retry + last-bar-close fallback so a stop-out is never aborted at a $0 limit
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>A snapshot failure can never propagate 0.0 into a negative/zero exit limit; the engine retries then falls back to last close so stop-outs always execute. One atomic `fix(t2.4)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 5: Finding 2.5 — orphan adoption rejects a duplicate open row for an already-open code</name>
  <files>bot/gateway/gateway.py, bot/state/store.py, tests/gateway/test_gateway.py</files>
  <read_first>
    - bot/gateway/gateway.py lines 975-991 (orphan-adoption INSERT OR IGNORE keyed on position_id PK, not code) and adopt_orphan at ~1022
    - bot/state/store.py lines 407-430 (get_open_positions — reuse to find an existing open row by code) and 291-334 (upsert_position guard pattern)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.5, RESEARCH Pitfall 5 (application-layer SELECT-before-INSERT, not a schema migration)
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.5"
    - tests/gateway/test_gateway.py (_make_gateway_with_mocks)
  </read_first>
  <behavior>
    RED — write tests/gateway/test_gateway.py::test_adopt_orphan_rejects_duplicate_code first: when an open (non-CLOSED) position for the code already exists in the store, the orphan-adoption path does NOT insert a second row and logs orphan_adoption_skipped_duplicate_code; when no open row exists, adoption proceeds and inserts one row.
  </behavior>
  <action>
    Add an application-layer SELECT-before-INSERT guard in the orphan-adoption path (bot/gateway/gateway.py, lines ~975-991 / adopt_orphan): before inserting, check for an existing open position by code — either add `def get_open_position_by_code(self, code: str) -> dict | None` to bot/state/store.py (filtering get_open_positions on code and phase != CLOSED) or reuse get_open_positions and filter inline. If an open row for the code exists, log `orphan_adoption_skipped_duplicate_code` with code and skip (continue); otherwise insert as today. No schema migration. Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.5): guard orphan adoption against duplicate open code rows`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/gateway/test_gateway.py::test_adopt_orphan_rejects_duplicate_code -x</automated>
  </verify>
  <acceptance_criteria>
    - The orphan-adoption path performs a SELECT-before-INSERT check on code (open, non-CLOSED) before inserting
    - When an open row for the code exists, no second row is inserted and `orphan_adoption_skipped_duplicate_code` is logged
    - No new schema migration is added
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>Two orphan-adoption cycles for the same symbol can no longer create duplicate open rows / double-managed positions. One atomic `fix(t2.5)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 6: Finding 2.6 — scanner skips a symbol on a stale prior_row</name>
  <files>bot/scanner/scanner.py, tests/scanner/test_scanner.py</files>
  <read_first>
    - bot/scanner/scanner.py lines 47-145 (_evaluate_symbol; prior_frame mask date < scan_date; prior_row = prior_frame.iloc[-1] at 142; prior_close_val/prior_high_val at 144-145)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.6 (restore staleness check removed with CR-02), RESEARCH Pitfall 7 (date-diff > 5 days → skip)
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.6"
    - tests/scanner/test_scanner.py (existing _make_daily_frame helpers, _evaluate_symbol tests)
  </read_first>
  <behavior>
    RED — write tests/scanner/test_scanner.py::test_evaluate_symbol_skips_on_stale_prior_row first: when the most-recent prior row's date is more than 5 days before scan_date (e.g. week-old yfinance data), _evaluate_symbol returns None and logs scanner_skipped_stale_prior_row; a fresh prior row (<=5 days) evaluates normally.
  </behavior>
  <action>
    In bot/scanner/scanner.py::_evaluate_symbol, immediately after `prior_row = prior_frame.iloc[-1]` (line 142), derive prior_date = prior_row.name.date() if hasattr(prior_row.name, "date") else prior_row.name, and if `(scan_date - prior_date).days > 5`, log `scanner_skipped_stale_prior_row` (code, prior_date) at warning and return None before computing prior_close/gap. This restores the staleness guard removed alongside the CR-02 today-row fix. Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.6): skip symbol when prior_row is stale (>5 days before scan_date)`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/scanner/test_scanner.py::test_evaluate_symbol_skips_on_stale_prior_row -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/scanner/scanner.py::_evaluate_symbol computes prior_date from prior_row.name and returns None when (scan_date - prior_date).days > 5
    - The skip logs `scanner_skipped_stale_prior_row`
    - A prior row within 5 days evaluates normally (proven by the test)
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>Stale yfinance data no longer inflates the gap% and produces false setups; stale-prior symbols are skipped. One atomic `fix(t2.6)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 7: Finding 2.7 — stop-out exit alert uses the stop price as the exit proxy</name>
  <files>bot/position/manager.py, tests/position/test_manager.py</files>
  <read_first>
    - bot/position/manager.py lines 671-760 (_trigger_stop_out; exit_proxy = pos.avg_fill_price or entry at ~724 and ~747; r_multiple = (exit_proxy - entry)/risk)
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.7 (exit alerts report R≈0 because exit_proxy = entry price)
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.7"
    - tests/position/test_manager.py (_make_pos with trail_stop, existing alert tests)
  </read_first>
  <behavior>
    RED — write tests/position/test_manager.py::test_stop_out_alert_uses_stop_as_exit_proxy first: driving a stop-out with entry != trail_stop, the exit alert's R-multiple is computed from trail_stop (so R ≈ -1 for a stop that is one risk-unit below entry), NOT from avg_fill_price/entry (which would yield R ≈ 0).
  </behavior>
  <action>
    In bot/position/manager.py::_trigger_stop_out, change the pre-fill exit-alert proxy from `exit_proxy = pos.avg_fill_price or entry` to `exit_proxy = pos.trail_stop or entry` at the stop-out alert computation site(s) (~724 and ~747), so the R-multiple in the alert reflects the price we are stopping out at. Do NOT change the actual post-fill R computation in _on_exit_fill (that correctly uses the real fill price). Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.7): use stop price as exit proxy in stop-out alert R`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/position/test_manager.py::test_stop_out_alert_uses_stop_as_exit_proxy -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/position/manager.py::_trigger_stop_out computes exit_proxy from pos.trail_stop (falling back to entry), not pos.avg_fill_price, at the stop-out alert site(s)
    - The stop-out alert R-multiple is non-zero and reflects the stop distance (proven by the test)
    - bot/position/manager.py::_on_exit_fill still uses fill.avg_fill_price for the post-fill R (unchanged)
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>Stop-out Telegram alerts report a meaningful R (≈ -1 at the stop), not R ≈ 0. One atomic `fix(t2.7)` commit exists.</done>
</task>

<task type="tdd">
  <name>Task 8: Finding 2.8 — orphan-adoption stop reads initial_stop_pct from rules.json</name>
  <files>bot/gateway/gateway.py, bot/main.py, tests/gateway/test_gateway.py</files>
  <read_first>
    - bot/gateway/gateway.py lines 256-260 (MoomooGateway.__init__ signature: cfg only) and 1222-1238 (_compute_orphan_stop returning lod*0.99)
    - bot/config/loader.py lines 90 and 194 (StrategyConfig.initial_stop_pct, parse_initial_stop_rule) — used identically by bot/strategy/trend_join_long.py:150 (lod * (1 - cfg.initial_stop_pct/100))
    - bot/main.py line 75 (MoomooGateway(get_gateway_config()) construction site) and where StrategyConfig is loaded in main.py
    - .planning/phases/06.2-code-review-remediation/06.2-SPEC.md Tier 2 row 2.8 (CFG-01: rules.json single source of truth), RESEARCH Pattern 6, Assumption A6
    - .planning/phases/06.2-code-review-remediation/06.2-PATTERNS.md "Finding 2.8"
    - tests/gateway/test_gateway.py (_make_gateway_with_mocks / TestGatewayConfig)
  </read_first>
  <behavior>
    RED — write tests/gateway/test_gateway.py::test_compute_orphan_stop_uses_config_pct first: a MoomooGateway constructed with initial_stop_pct=2.0 computes _compute_orphan_stop(lod=100.0) == 98.0 (lod*(1-2/100)); the default (no param) yields lod*0.99 (1.0%) as a safe fallback; lod<=0 returns 0.0.
  </behavior>
  <action>
    In bot/gateway/gateway.py::MoomooGateway.__init__, add an optional `initial_stop_pct: float = 1.0` parameter and store `self._initial_stop_pct = initial_stop_pct`. Change _compute_orphan_stop (line 1238) from `return float(lod * 0.99)` to `return float(lod * (1.0 - self._initial_stop_pct / 100.0))` (keep the lod<=0 → 0.0 guard). In bot/main.py line 75, thread the loaded StrategyConfig's initial_stop_pct into the construction: `MoomooGateway(get_gateway_config(), initial_stop_pct=<strategy_cfg>.initial_stop_pct)` (use the StrategyConfig already loaded in main.py; if not yet loaded there, load it via the existing loader). Verify RED fails, implement, verify GREEN, run `python3 -m pytest tests/ -q --tb=short`. Commit as `fix(t2.8): compute orphan-adoption stop from rules.json initial_stop_pct`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/gateway/test_gateway.py::test_compute_orphan_stop_uses_config_pct -x</automated>
  </verify>
  <acceptance_criteria>
    - bot/gateway/gateway.py::MoomooGateway.__init__ accepts `initial_stop_pct: float = 1.0` and stores self._initial_stop_pct
    - bot/gateway/gateway.py::_compute_orphan_stop returns lod * (1 - self._initial_stop_pct/100.0), not a hardcoded lod*0.99
    - bot/main.py passes the StrategyConfig initial_stop_pct into MoomooGateway at construction
    - `python3 -m pytest tests/ -q --tb=short` reports 0 failed
  </acceptance_criteria>
  <done>The orphan-adoption stop follows rules.json (CFG-01 single source of truth), matching the live strategy's initial_stop, not a hardcoded 1%. One atomic `fix(t2.8)` commit exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared broker account → gate | Manual/zero-qty holdings on shared account 1727266 must not count toward the bot's concurrency cap |
| OpenD snapshot → exit pricing | A failed/zero snapshot must not become a $0 exit limit |
| yfinance data → scan decision | Stale daily bars must not drive gap-based setups |
| rules.json → orphan stop | Strategy constants must come from config, never hardcoded in the gateway |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-06 | Spoofing (false position count) | signal_engine gate (2.1) | mitigate | Count only bot-owned (DB positions table) qty>0 rows; external/manual holdings cannot saturate the cap |
| T-06.2-07 | Denial of Service (subscription leak / eviction of open positions) | bot._job_intraday_rescan (2.2) | mitigate | Pass real active_codes so open/in-flight symbols are protected from eviction and the 20-cap holds |
| T-06.2-08 | Tampering (data integrity) | gateway.get_bid_price/get_ask_price (2.4) | mitigate | Raise GatewayError on failure; engine retries then falls back to last close — never a $0 exit |
| T-06.2-09 | Elevation of privilege (double position) | gateway orphan adoption (2.5) | mitigate | Application-layer SELECT-before-INSERT guard on open code |
| T-06.2-10 | Tampering (stale external data) | scanner._evaluate_symbol (2.6) | mitigate | Skip symbol when prior_row is >5 days stale |
| T-06.2-11 | Tampering (config bypass) | gateway._compute_orphan_stop (2.8) | mitigate | Read initial_stop_pct from StrategyConfig (rules.json), remove hardcoded lod*0.99 |
| T-06.2-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit N/A — internal edits only) |
</threat_model>

<verification>
- After each finding task: `python3 -m pytest tests/ -q --tb=short` must report 0 failed (one new green test per finding).
- After all eight findings (Tier 2 complete): `python3 -m pytest tests/ -v` — zero failures.
</verification>

<success_criteria>
- Eight atomic `fix(t2.N)` commits exist, each with its regression test written first.
- `python3 -m pytest tests/ -v` is green, zero failures.
- Cap counts only bot-owned qty>0; rescan protects open positions; trades are recorded; bid/ask failure raises; no duplicate orphans; stale scans skip; exit alerts show meaningful R; orphan stop is rules.json-driven.
</success_criteria>

<artifacts_this_phase_produces>
New symbols introduced by Plan 02 (exclude from cross-AI drift verification — created here):

**New store method**
- bot/state/store.py::insert_trade(self, trade: dict)
- bot/state/store.py::get_open_position_by_code(self, code) — only if the executor adds it (2.5); otherwise get_open_positions is reused

**New gateway surface**
- bot/gateway/gateway.py::MoomooGateway.__init__ new optional parameter `initial_stop_pct` and attribute `self._initial_stop_pct`
- bot/gateway/gateway.py::get_bid_price / get_ask_price now raise GatewayError on failure (behavior change)

**New tests**
- tests/signal/test_signal_engine.py::test_signal_engine_excludes_zero_qty_and_external_positions
- tests/service/test_bot.py::test_intraday_rescan_passes_open_position_codes
- tests/state/test_store.py (insert_trade coverage)
- tests/position/test_manager.py::test_exit_fill_writes_trade_row_to_store
- tests/gateway/test_gateway.py::test_get_bid_price_raises_on_snapshot_failure
- tests/gateway/test_gateway.py::test_adopt_orphan_rejects_duplicate_code
- tests/scanner/test_scanner.py::test_evaluate_symbol_skips_on_stale_prior_row
- tests/position/test_manager.py::test_stop_out_alert_uses_stop_as_exit_proxy
- tests/gateway/test_gateway.py::test_compute_orphan_stop_uses_config_pct

**New log event names**
- orphan_adoption_skipped_duplicate_code (gateway.py)
- scanner_skipped_stale_prior_row (scanner.py)

**Reused (NOT new) — do not flag as drift**
- get_open_positions (store.py:407), get_pending_intent_codes (store.py:539), get_closed_trades/get_daily_trade_stats (store.py:340/364), GatewayError (gateway.py:148), StrategyConfig.initial_stop_pct (loader.py:90), run_intraday_rescan active_codes protection (scanner.py:434), _on_exit_fill (manager.py:499)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06.2-code-review-remediation/06.2-02-SUMMARY.md` when done
</output>
