---
phase: 06-backtester
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backtester/__init__.py
  - tests/backtester/__init__.py
  - tests/backtester/fixtures.py
  - tests/backtester/test_feed.py
  - tests/backtester/test_execution.py
  - tests/backtester/test_harness.py
  - tests/backtester/test_report.py
  - .gitignore
autonomous: true
requirements: [BT-01, BT-02, BT-03, BT-04]
must_haves:
  truths:
    - "python3 -m pytest tests/backtester/ -q collects 4 test modules and reports 0 errors, 0 failures (only skips)"
    - "The synthetic ahead-only 5m dataset fixture is importable and returns a deterministic bar sequence"
    - "backtester/ is an importable package; backtester/cache/ and backtester/runs/ are gitignored"
  artifacts:
    - path: "backtester/__init__.py"
      provides: "backtester package marker"
    - path: "tests/backtester/fixtures.py"
      provides: "make_ahead_only_5m_dataset() + make_trade_log() shared test fixtures"
    - path: "tests/backtester/test_feed.py"
      provides: "BT-04 feed test module (importorskip guarded until 06-02)"
    - path: "tests/backtester/test_execution.py"
      provides: "BT-02 execution test module (importorskip guarded until 06-03)"
    - path: "tests/backtester/test_harness.py"
      provides: "BT-01 harness test module (importorskip guarded until 06-05)"
    - path: "tests/backtester/test_report.py"
      provides: "BT-03 report test module (importorskip guarded until 06-04)"
  key_links:
    - from: "tests/backtester/test_execution.py"
      to: "tests/backtester/fixtures.py"
      via: "import make_ahead_only_5m_dataset"
      pattern: "from tests.backtester.fixtures import"
---

<objective>
Wave-0 foundation for the Phase 6 backtester (Nyquist scaffold). Creates the `backtester/`
package skeleton, the `tests/backtester/` test package, the four failing-but-collectible test
stubs (one per implementation plan), and the shared synthetic-dataset fixture that the
look-ahead proof (BT-02) and the harness integration test (BT-01) depend on.

Every test stub uses `pytest.importorskip("backtester.<module>")` at module top so the suite
stays GREEN (tests skip) until the corresponding implementation plan creates the module —
then the real assertions run RED→GREEN. This is the project's established Wave-0 pattern
(03-01 "failing test stubs", 05-00 scaffold).

Purpose: give every downstream plan (06-02..06-06) a concrete `<automated>` test target that
already exists, and pin the synthetic ahead-only dataset once so feed/execution/harness tests
share one source of truth.
Output: `backtester/` + `tests/backtester/` packages, 4 test stubs, `fixtures.py`, gitignore entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-backtester/06-RESEARCH.md
@.planning/phases/06-backtester/06-PATTERNS.md
@.planning/phases/06-backtester/06-VALIDATION.md
</context>

## Artifacts this phase produces (Plan 06-01 scope)

New symbols/files created by this plan (excluded from source-drift verification):
- `backtester` package (`backtester/__init__.py`)
- `tests.backtester` package (`tests/backtester/__init__.py`)
- `tests/backtester/fixtures.py` — functions `make_ahead_only_5m_dataset()`, `make_trade_log()`
- `tests/backtester/test_feed.py`, `test_execution.py`, `test_harness.py`, `test_report.py`
- `.gitignore` entries: `backtester/cache/`, `backtester/runs/`

<tasks>

<task type="auto">
  <name>Task 1: Create backtester + tests.backtester package skeletons and gitignore entries</name>
  <files>backtester/__init__.py, tests/backtester/__init__.py, .gitignore</files>
  <read_first>
    - .gitignore (current entries — append only, do not rewrite existing lines)
    - bot/__init__.py (empty-package-marker convention to mirror)
    - tests/scanner/__init__.py (test-package marker convention)
  </read_first>
  <action>
    Create `backtester/__init__.py` as an empty package marker (module docstring only,
    matching `bot/__init__.py` style — no exports). Create `tests/backtester/__init__.py`
    as an empty test-package marker matching `tests/scanner/__init__.py`.
    Append two entries to the existing `.gitignore` under a new comment line
    "# Backtester scratch (CSV cache + per-run scratch StateStore DBs)":
    `backtester/cache/` and `backtester/runs/`. Do NOT remove or reorder existing
    `.gitignore` lines. The scratch dirs are created at runtime by later plans; only the
    ignore entries are added here.
  </action>
  <verify>
    <automated>python3 -c "import backtester" && git check-ignore backtester/cache/ backtester/runs/ && python3 -m pytest tests/ -q --collect-only 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import backtester"` exits 0 (package importable)
    - `git check-ignore backtester/cache/` prints `backtester/cache/` (entry active)
    - `git check-ignore backtester/runs/` prints `backtester/runs/` (entry active)
    - `.gitignore` still contains every line it had before (append-only diff)
  </acceptance_criteria>
  <done>backtester/ and tests/backtester/ are importable packages; cache/runs dirs are gitignored; no existing gitignore lines removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared fixtures + four importorskip-guarded failing test stubs</name>
  <files>tests/backtester/fixtures.py, tests/backtester/test_feed.py, tests/backtester/test_execution.py, tests/backtester/test_harness.py, tests/backtester/test_report.py</files>
  <read_first>
    - .planning/phases/06-backtester/06-VALIDATION.md (Per-Task Verification Map, Wave 0 requirements — synthetic ahead-only dataset)
    - .planning/phases/06-backtester/06-RESEARCH.md (Pitfall 1 N+1 fill; Validation Architecture → Test Framework: asyncio.run convention, NOT @pytest.mark.asyncio)
    - bot/signal/events.py (BarEvent field set the fixture bar dicts must mirror: code, time_key, open, high, low, close, volume, hod, lod, cum_volume)
    - tests/scanner/test_fetcher.py (yfinance mocking convention: patch("yfinance.download", ...))
    - tests/test_main_wiring.py (real-construction wiring test pattern the harness stub will follow)
  </read_first>
  <action>
    Create `tests/backtester/fixtures.py` with two pure functions and NO network/import of
    backtester modules (so it is always importable):
      (a) `make_ahead_only_5m_dataset()` — returns a deterministic in-memory 5m bar sequence
          for one code ("US.TEST") on one session, engineered so the ONLY bar whose close
          clears the entry conditions is bar N, and bar N+1's OPEN is materially different
          from bar N's close (e.g. bar N close = 105.00, bar N+1 open = 103.50). This lets the
          06-03 execution test assert the fill uses bar N+1 open (103.50), never
          intent.entry_price (105.00) — the BT-02 look-ahead proof. Include a bar sequence
          long enough to also cover "signal on the LAST bar → no N+1 bar → no fill". Each bar
          is a dict with the full BarEvent key set (code, time_key ISO "YYYY-MM-DD HH:MM:00"
          in ET, open, high, low, close, volume, hod, lod, cum_volume) with hod/lod/cum_volume
          pre-computed as session running max-high / min-low / summed-volume.
      (b) `make_trade_log()` — returns a small list of closed-trade dicts using the phase
          trade-log contract keys (code, entry_price, exit_price, quantity, exit_reason,
          r_multiple, closed_at) with hand-computed expected metrics documented in a comment
          (so the 06-04 report test asserts against known win_rate / avg_r / profit_factor /
          max_drawdown values). Include at least one winner and one loser so win_rate and
          profit_factor are non-trivial, and order rows so max-drawdown is a known non-zero.
    Create the four test stubs. Each stub begins with
    `mod = pytest.importorskip("backtester.<module>")` at MODULE top so uncreated modules
    SKIP (never error/collect-fail). Under the guard, write the REAL intended assertions the
    downstream plan must satisfy (they will be RED once the module exists, GREEN after impl):
      - test_feed.py → asserts SimulatedBarFeed.replay yields chronologically-ordered bars
        with computed hod/lod/cum_volume, next_bar returns the strictly-next bar, and an
        out-of-window start raises BacktestWindowError (BT-04). Mock yfinance via
        patch("yfinance.download", ...).
      - test_execution.py → uses make_ahead_only_5m_dataset(); asserts consume_intent fills at
        bar N+1 open (not intent.entry_price), returns a FillEvent, and a last-bar signal
        yields None (BT-02). Async tests use asyncio.run(coro) directly.
      - test_harness.py → mirrors tests/test_main_wiring.py: drive the REAL BacktestHarness
        construction with only the feed's yfinance seam patched; assert position_manager built
        with gateway=None and a full replay produces at least one captured trade (BT-01).
      - test_report.py → uses make_trade_log(); asserts compute_metrics returns the documented
        win_rate/avg_r_multiple/profit_factor/max_drawdown_usd/total_trades and that a CSV with
        the contract columns is written (BT-03).
    Do NOT implement any backtester source module in this task — stubs only. Every async test
    uses `asyncio.run(...)` inside a plain `def test_*` (project convention — NOT
    @pytest.mark.asyncio), per 06-RESEARCH Validation Architecture.
  </action>
  <verify>
    <automated>python3 -m pytest tests/backtester/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/backtester/ -q` reports 0 errors and 0 failures (all backtester tests SKIP because no backtester.* module exists yet)
    - `python3 -c "from tests.backtester.fixtures import make_ahead_only_5m_dataset, make_trade_log; d=make_ahead_only_5m_dataset(); assert isinstance(d, list) and len(d) >= 3"` exits 0
    - `grep -c 'importorskip' tests/backtester/test_feed.py tests/backtester/test_execution.py tests/backtester/test_harness.py tests/backtester/test_report.py` shows each file has at least one importorskip guard
    - `grep -L 'pytest.mark.asyncio' tests/backtester/test_execution.py` lists the file (i.e. it does NOT use the asyncio marker — asyncio.run convention honored)
  </acceptance_criteria>
  <done>Four importorskip-guarded stubs collect as SKIPs; fixtures.py exports the ahead-only dataset and trade-log with documented expected metrics; full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test scaffold) | This plan creates only package markers and test files; no runtime input, no network, no DB, no broker path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `.gitignore` edit | mitigate | Append-only edit; acceptance criterion asserts all prior lines preserved so existing ignores (data/, __pycache__) are not weakened. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs this phase (06-RESEARCH Package Legitimacy Audit: "No new external packages"). No install task exists; nothing to slopcheck. |
</threat_model>

<verification>
- `python3 -m pytest tests/backtester/ -q` → all skips, 0 errors/failures.
- `python3 -m pytest tests/ -q` → full 590+ suite stays green (no bot/ file touched).
- `import backtester` succeeds; cache/runs gitignored.
</verification>

<success_criteria>
- backtester/ and tests/backtester/ packages exist and import.
- Four test stubs collect as SKIPs (importorskip); each contains the real downstream assertions.
- fixtures.py provides the ahead-only 5m dataset (BT-02 proof) and the trade-log with documented expected metrics (BT-03 proof).
- Full test suite green; no bot/ modifications.
</success_criteria>

<output>
Create `.planning/phases/06-backtester/06-01-SUMMARY.md` when done.
</output>
