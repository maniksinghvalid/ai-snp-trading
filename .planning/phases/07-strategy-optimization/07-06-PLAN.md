---
phase: 07-strategy-optimization
plan: 06
type: execute
wave: 4
depends_on: ["07-04", "07-05"]
files_modified: []
autonomous: false
requirements: [EXIT-MODEL]
must_haves:
  truths:
    - "The exit-model SELECTION (success criterion 2 — chosen from a backtest comparison) is explicitly gated on the Phase 6 backtester, which does not exist yet (RESEARCH Finding C-1)"
    - "This plan does NOT ship an exit-behavior change; it records the dependency, presents the three candidate models, and pauses for an operator decision (defer vs proceed-if-Phase-6-ready)"
    - "The exit-model config seam (07-04) is already in place, so once Phase 6 exists the selection is a bounded follow-up: implement the two variant FSM branches, backtest all three, write the winner to rules.json exit.model, and widen the loader's implemented-models set"
  artifacts: []
  key_links:
    - from: "07-04 exit.model config seam"
      to: "Phase 6 backtester (unbuilt)"
      via: "backtest comparison selects the winning exit.model value"
      pattern: "exit.model"
---

<objective>
Gate the evidence-based exit-model selection (EXIT-MODEL success criterion 2) on the Phase 6 backtester. The backtester has zero code in the repository (RESEARCH Finding C-1), so the shipped exit model CANNOT be chosen from a backtest comparison within Phase 7. This plan is the explicit blocked decision gate: it documents the dependency, presents the three candidate exit models, and pauses for an operator decision rather than silently defaulting or shipping an unvalidated exit variant.

Purpose: Honesty and safety. The exit-model config surface already exists (plan 07-04: exit.model is config-driven, schema-validated, and fails closed for unimplemented variants). What remains — implementing the fixed_2r and full_to_1p5r_trail FSM branches, running the backtest comparison, and selecting the winner — depends on Phase 6 and must not be faked. This gate keeps EXIT-MODEL visible and blocked instead of quietly dropped, and hands the operator the choice to defer.

Output: An operator decision recorded in the plan SUMMARY; no source changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-strategy-optimization/07-CONTEXT.md
@.planning/phases/07-strategy-optimization/07-RESEARCH.md
@.planning/phases/07-strategy-optimization/07-04-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan produces no new source symbols. It records a decision only. (The exit.model config seam and its enum/constants are produced by plan 07-04.)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Exit-model selection is blocked on the Phase 6 backtester — decide defer vs proceed</name>
  <files>none (decision-only checkpoint; no source files modified)</files>
  <decision>How to resolve EXIT-MODEL success criterion 2 (exit model chosen from a backtest comparison) given the Phase 6 backtester does not exist</decision>
  <context>
    Phase 7 success criterion 2 requires the shipped exit model to be selected from a backtest comparison of: (1) the current partial ⅓@0.75R / breakeven@1R / 5m-swing-low-trail model (`partial_be_trail`, the live default), (2) no-scale / fixed-2R (`fixed_2r`), and (3) full-size-to-1.5R then trail (`full_to_1p5r_trail`). RESEARCH Finding C-1 confirms (repo grep) that NO backtester code exists — Phase 6 is unbuilt. The exit-model config seam is already shipped by plan 07-04: `exit.model` is config-driven and schema-validated, defaults to the current `partial_be_trail` (zero behavior change), and the loader FAILS CLOSED (ConfigError) for the two not-yet-implemented variants. Therefore the live bot is safe and correct today; only the evidence-based SELECTION is blocked. This gate must not be auto-approved: shipping an unbacktested exit variant would violate success criterion 2 and CONTEXT.md's "not by default" requirement.
  </context>
  <options>
    <option id="defer">
      <name>Defer exit-model selection to a post-Phase-6 gap-closure (recommended)</name>
      <pros>Honest: criterion 2 genuinely requires Phase 6 evidence. Live bot stays on the validated current model (partial_be_trail). Waves 1–3 (RVOL-TOD, tick-stop, circuit breaker) ship now as pure risk reducers. The follow-up is bounded and pre-scoped (below).</pros>
      <cons>EXIT-MODEL criterion 2 remains open until Phase 6 completes; Phase 7 closes with a documented, tracked dependency rather than a shipped selection.</cons>
    </option>
    <option id="proceed-if-ready">
      <name>Proceed now ONLY if Phase 6 backtester already exists and is validated</name>
      <pros>Closes EXIT-MODEL fully within Phase 7 if the backtester is in fact available.</pros>
      <cons>Contradicts the verified repo state (no backtester code). Would require first building Phase 6, which is out of this phase's scope; selecting without it would be shipping-by-default, which is explicitly disallowed.</cons>
    </option>
  </options>
  <post_phase6_followup>
    When Phase 6 lands, the bounded selection follow-up is: (a) implement the fixed_2r and full_to_1p5r_trail branches in the PositionState FSM dispatched on cfg.exit_model; (b) widen bot/config/loader.py::_IMPLEMENTED_EXIT_MODELS to include the newly-built variants; (c) run the backtester across all three models on the same rules.json + historical 5m data; (d) write the winning model to rules.json exit.model; (e) record the comparison (win rate, avg R, max drawdown, profit factor) as the selection evidence. Note Assumption A3 (15m EMA trail for full_to_1p5r_trail may be derivable from the 5m buffer or need a separate feed) — resolve during that follow-up.
  </post_phase6_followup>
  <action>
    Do NOT modify any source. Verify the block is real, then pause for the operator decision. Concretely: (1) run `find . -path ./node_modules -prune -o -name "*.py" -print | grep -i backtest` and confirm it returns no results (Phase 6 backtester absent); (2) confirm plan 07-04 shipped the exit.model seam (`grep -n "exit_model" bot/config/loader.py` and `grep -n "_IMPLEMENTED_EXIT_MODELS" bot/config/loader.py` match, and the loader fails closed for fixed_2r/full_to_1p5r_trail); (3) present the two options above with the three candidate models and wait for an explicit "defer" or "proceed-if-ready". This gate is blocking and must not be auto-approved regardless of workflow.auto_advance — shipping an exit model without backtest evidence is disallowed by success criterion 2 and CONTEXT.md ("not by default"). On "defer", record the decision and the post-Phase-6 follow-up in the SUMMARY and close the plan without code changes.
  </action>
  <verify>
    <automated>MISSING — decision checkpoint; no automated gate. Guard evidence: `find . -path ./node_modules -prune -o -name "*.py" -print | grep -i backtest` returns empty (Phase 6 absent), confirming the block is real.</automated>
    <human-check>Operator replies "defer" (recommended) or "proceed-if-ready" after confirming the Phase 6 backtester status; the decision and follow-up are recorded in 07-06-SUMMARY.md.</human-check>
  </verify>
  <acceptance_criteria>
    - The backtester-absence guard command returns no results (Phase 6 unbuilt) — the block is verified, not assumed
    - The 07-04 exit.model seam is confirmed present and failing closed for unimplemented variants
    - The operator has recorded an explicit decision (defer / proceed-if-ready); no source file was modified by this plan
  </acceptance_criteria>
  <resume-signal>Reply "defer" to close Phase 7 with EXIT-MODEL selection tracked as a Phase-6-gated follow-up (recommended), or "proceed-if-ready" only if the Phase 6 backtester is confirmed built and validated. Do not auto-approve.</resume-signal>
  <done>The Phase-6 dependency for EXIT-MODEL selection is verified and the operator has decided (defer recommended); the bounded post-Phase-6 follow-up is recorded in the SUMMARY; no exit behavior changed and no source was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator decision → shipped exit model | The selection controls live position exits; it must be backed by backtest evidence, never shipped by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-21 | Tampering | An exit model shipped without backtest evidence (by default or by guess) drives live exits | mitigate | This blocking gate forbids auto-approval; the 07-04 loader fails closed for unimplemented variants; selection requires the Phase 6 comparison |
| T-07-22 | Repudiation | The Phase-6 dependency is silently dropped and criterion 2 forgotten | mitigate | EXIT-MODEL remains an open requirement tracked by this plan's SUMMARY + the ROADMAP follow-up note |
| T-07-SC | Tampering | Supply-chain: dependency installs | accept | No packages installed; no source changes in this plan |
</threat_model>

<verification>
- No source files are modified by this plan (files_modified: []); `git diff --name-only` shows only the SUMMARY on completion.
- The decision (defer / proceed-if-ready) is recorded in 07-06-SUMMARY.md with the post-Phase-6 follow-up steps captured.
- The 07-04 fail-closed guard remains intact (unimplemented exit models still raise ConfigError).
</verification>

<success_criteria>
- EXIT-MODEL criterion 2 is explicitly gated on Phase 6, not shipped by default.
- The operator has decided (defer recommended) and the bounded post-Phase-6 follow-up is documented.
- No unvalidated exit behavior ships; the live model remains partial_be_trail.
</success_criteria>

<output>
Create `.planning/phases/07-strategy-optimization/07-06-SUMMARY.md` recording the operator decision and the post-Phase-6 follow-up plan when done.
</output>
