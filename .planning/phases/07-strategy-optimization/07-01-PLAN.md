---
phase: 07-strategy-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/state/migrations.py
  - bot/state/store.py
  - bot/config/loader.py
  - bot/config/schema.py
  - rules.json
  - tests/state/test_migrations.py
  - tests/state/test_store.py
  - tests/config/test_loader.py
autonomous: true
requirements: [SIG-RVOL-TOD, RISK-CIRCUIT, RISK-TICK-STOP]
must_haves:
  truths:
    - "Migration 0005 creates the tod_baselines table (scan_date, code, time_bucket, cum_vol_mean; PK on the triple) and adds a nullable broker_stop_order_id TEXT column to positions — idempotently (SIG-RVOL-TOD, RISK-TICK-STOP)"
    - "StateStore.get_tod_baseline(scan_date, code, time_bucket) returns the stored mean or 0.0 when absent; upsert_tod_baselines writes a batch of HH:MM→float in one commit (SIG-RVOL-TOD)"
    - "StateStore get/set/clear_circuit_breaker_date persist the ET trip date in the existing meta key-value table and survive a reopen (RISK-CIRCUIT, D-07)"
    - "rules.json carries risk.daily_circuit_breaker_r=2.0, execution.use_broker_stop_orders=true, intraday_filters.I3_rvol_tod_lookback_days=14; the schema validates each and StrategyConfig exposes them (CFG-01, D-02/D-05)"
    - "A malformed value for any new key (wrong type) fails jsonschema validation with ConfigError — no strategy literal is hardcoded in Python (CFG-01)"
  artifacts:
    - path: "bot/state/migrations.py"
      provides: "_migration_0005 (tod_baselines table + broker_stop_order_id column); CURRENT_VERSION bumped to 5"
      contains: "_migration_0005"
    - path: "bot/state/store.py"
      provides: "get_tod_baseline / upsert_tod_baselines / get_circuit_breaker_date / set_circuit_breaker_date / clear_circuit_breaker"
      contains: "def get_tod_baseline"
    - path: "bot/config/loader.py"
      provides: "StrategyConfig fields daily_circuit_breaker_r, use_broker_stop_orders, rvol_tod_lookback_days"
      contains: "daily_circuit_breaker_r"
    - path: "bot/config/schema.py"
      provides: "jsonschema entries for the three new keys"
      contains: "daily_circuit_breaker_r"
    - path: "rules.json"
      provides: "the three new config keys with default values"
      contains: "daily_circuit_breaker_r"
  key_links:
    - from: "bot/state/store.py get/set/clear_circuit_breaker_date"
      to: "meta table"
      via: "SELECT/INSERT ON CONFLICT/DELETE on key='circuit_breaker_tripped_date'"
      pattern: "circuit_breaker_tripped_date"
    - from: "bot/config/loader.py"
      to: "rules.json"
      via: "rk.get('daily_circuit_breaker_r'), ex_cfg.get('use_broker_stop_orders'), inf.get('I3_rvol_tod_lookback_days')"
      pattern: "daily_circuit_breaker_r"
    - from: "bot/state/migrations.py MIGRATIONS list"
      to: "SQLite user_version"
      via: "_migration_0005 appended; CURRENT_VERSION = 5"
      pattern: "CURRENT_VERSION"
---

<objective>
Lay the Phase 7 data + config foundation that every other Phase 7 plan consumes: schema migration 0005 (the `tod_baselines` table for the RVOL-TOD gate and the `broker_stop_order_id` column for the tick-level stop), the StateStore accessor methods for TOD baselines and circuit-breaker persistence, and the three new `rules.json` keys (with schema validation and `StrategyConfig` fields) that keep every new threshold config-driven (CFG-01).

Purpose: This plan owns the shared substrate. Isolating the migration, store methods, and config keys here lets the four feature plans (RVOL-TOD data path, broker-side stop, exit-model seam, signal gates) run without touching migrations/store/loader/schema/rules.json, keeping their file ownership disjoint. No behavior changes ship yet — this is contracts + persistence only.

Output: `_migration_0005`; five new `StateStore` methods; three new `StrategyConfig` fields + schema entries + `rules.json` keys; extended migration/store/loader tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-strategy-optimization/07-CONTEXT.md
@.planning/phases/07-strategy-optimization/07-RESEARCH.md
@.planning/phases/07-strategy-optimization/07-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from source-drift verification):
- Table `tod_baselines` (columns: scan_date, code, time_bucket, cum_vol_mean)
- Column `positions.broker_stop_order_id` (TEXT, nullable)
- Function `bot/state/migrations.py::_migration_0005`; constant `_POSITIONS_0005_COLUMNS`; `CURRENT_VERSION` bumped 4→5
- Methods `StateStore.get_tod_baseline`, `StateStore.upsert_tod_baselines`, `StateStore.get_circuit_breaker_date`, `StateStore.set_circuit_breaker_date`, `StateStore.clear_circuit_breaker`
- meta key string `"circuit_breaker_tripped_date"`
- `StrategyConfig` fields `daily_circuit_breaker_r`, `use_broker_stop_orders`, `rvol_tod_lookback_days`
- rules.json keys `risk.daily_circuit_breaker_r`, `execution.use_broker_stop_orders`, `intraday_filters.I3_rvol_tod_lookback_days`
- Schema entries for the three new keys in `bot/config/schema.py::SCHEMA`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0005 — tod_baselines table + broker_stop_order_id column</name>
  <files>bot/state/migrations.py, tests/state/test_migrations.py</files>
  <read_first>
    - bot/state/migrations.py — read the whole file: the callable-migration pattern of _migration_0002 and _migration_0004 (PRAGMA table_info guard), the MIGRATIONS list, and the CURRENT_VERSION constant (currently 4)
    - .planning/phases/07-strategy-optimization/07-PATTERNS.md section "bot/state/migrations.py — add migration 0005" (the _migration_0005 body and MIGRATIONS/CURRENT_VERSION edits)
    - tests/state/test_migrations.py — existing migration test helpers and how prior migrations assert schema state (PRAGMA table_info / sqlite_master queries)
  </read_first>
  <behavior>
    - Test: after opening a StateStore, PRAGMA user_version == 5.
    - Test: sqlite_master contains a table named tod_baselines with columns scan_date, code, time_bucket, cum_vol_mean and the composite primary key (scan_date, code, time_bucket).
    - Test: PRAGMA table_info(positions) includes a broker_stop_order_id column of type TEXT.
    - Test (idempotency): running the migration runner twice does not raise and does not duplicate the column.
  </behavior>
  <action>
    Add `_migration_0005(conn)` to bot/state/migrations.py following the exact _migration_0004 shape. It must (a) `CREATE TABLE IF NOT EXISTS tod_baselines` with columns scan_date TEXT NOT NULL, code TEXT NOT NULL, time_bucket TEXT NOT NULL, cum_vol_mean REAL NOT NULL and PRIMARY KEY (scan_date, code, time_bucket); and (b) add column broker_stop_order_id TEXT to positions guarded by a `PRAGMA table_info(positions)` existence check (reuse the `_POSITIONS_0005_COLUMNS` tuple pattern). Use `conn.execute(...)` (not executescript) so the version bump commits atomically (WR-03). Append `_migration_0005` to the MIGRATIONS list and set CURRENT_VERSION = 5. Do not alter any earlier migration. Follow the pattern-map excerpt verbatim for names. Extend tests/state/test_migrations.py with the four behavior tests above.
  </action>
  <verify>
    <automated>python3 -m pytest tests/state/test_migrations.py -x -q --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_migration_0005" bot/state/migrations.py` matches the function def AND its entry in the MIGRATIONS list
    - `grep -n "CURRENT_VERSION = 5" bot/state/migrations.py` matches
    - bot/state/migrations.py contains `CREATE TABLE IF NOT EXISTS tod_baselines` and `PRIMARY KEY (scan_date, code, time_bucket)`
    - The broker_stop_order_id ALTER is guarded by a `PRAGMA table_info(positions)` membership check (no unconditional ALTER)
    - `python3 -m pytest tests/state/test_migrations.py -x -q` passes including a user_version==5 assertion and an idempotency (run-twice) assertion
  </acceptance_criteria>
  <done>A fresh or existing DB migrates to version 5 with the tod_baselines table and the nullable broker_stop_order_id column, idempotently, with tests proving schema shape and re-run safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StateStore accessors — TOD baseline CRUD + circuit-breaker meta persistence</name>
  <files>bot/state/store.py, tests/state/test_store.py</files>
  <read_first>
    - bot/state/store.py — get_rvol_baseline (scalar fetch with self._lock), get_daily_trade_stats (lock + fetchone), upsert_position (INSERT ... ON CONFLICT + commit), resolve_pending_intent (guarded write) — copy these lock/commit conventions exactly
    - .planning/phases/07-strategy-optimization/07-PATTERNS.md section "bot/state/store.py — add TOD baseline CRUD + circuit-breaker meta methods" (method bodies for get_tod_baseline, upsert_tod_baselines, get/set/clear_circuit_breaker_date)
    - .planning/phases/07-strategy-optimization/07-RESEARCH.md Findings C-5 (get_daily_trade_stats realized_pnl) and C-6 (meta table key-value)
    - tests/state/test_store.py — existing store-test fixtures (how a temp StateStore is opened/closed)
  </read_first>
  <behavior>
    - Test: upsert_tod_baselines(scan_date, code, {"10:05": 1000.0, "10:10": 2500.0}) then get_tod_baseline(scan_date, code, "10:10") == 2500.0; a missing bucket returns 0.0.
    - Test: upsert_tod_baselines is a full replace for a re-run of the same (scan_date, code) — no duplicate rows, latest values win.
    - Test: get_circuit_breaker_date() is None on a fresh store; after set_circuit_breaker_date("2026-07-03") it returns "2026-07-03"; after clear_circuit_breaker() it returns None.
    - Test (persistence, D-07): set the date, close and reopen the store at the same path, get_circuit_breaker_date() still returns the stored date.
  </behavior>
  <action>
    Add five methods to StateStore, each wrapped in `with self._lock:` and using parameterized SQL (no f-string interpolation of values). get_tod_baseline(scan_date, code, time_bucket) -> float returns cum_vol_mean or 0.0 when no row. upsert_tod_baselines(scan_date, code, baselines: dict) uses executemany INSERT OR REPLACE over tod_baselines for the dict items and commits once. get_circuit_breaker_date() -> Optional[str] reads meta WHERE key='circuit_breaker_tripped_date'. set_circuit_breaker_date(date_str) upserts that meta row via INSERT ... ON CONFLICT(key) DO UPDATE. clear_circuit_breaker() DELETEs that meta row. Match the pattern-map bodies verbatim, including docstrings noting D-07 and the 0.0 fallback contract. Extend tests/state/test_store.py with the four behavior tests, including the close/reopen persistence test.
  </action>
  <verify>
    <automated>python3 -m pytest tests/state/test_store.py -x -q --timeout=60 -k "tod_baseline or circuit_breaker"</automated>
  </verify>
  <acceptance_criteria>
    - bot/state/store.py defines get_tod_baseline, upsert_tod_baselines, get_circuit_breaker_date, set_circuit_breaker_date, clear_circuit_breaker
    - Every new method body opens `with self._lock:` and uses `?` placeholders (no value interpolation into SQL text)
    - get_tod_baseline returns 0.0 for an absent (scan_date, code, time_bucket)
    - A close→reopen test proves the circuit-breaker date persists (D-07)
    - `python3 -m pytest tests/state/test_store.py -x -q -k "tod_baseline or circuit_breaker"` passes
  </acceptance_criteria>
  <done>The store can read/write TOD baselines and durably persist/clear the circuit-breaker trip date via the existing meta table, all under the store lock with parameterized SQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Config keys — daily_circuit_breaker_r, use_broker_stop_orders, I3_rvol_tod_lookback_days</name>
  <files>rules.json, bot/config/schema.py, bot/config/loader.py, tests/config/test_loader.py</files>
  <read_first>
    - rules.json — the risk, execution, and intraday_filters blocks (current keys/values)
    - bot/config/schema.py — the SCHEMA dict structure; how risk/execution/intraday_filters properties and required lists are declared
    - bot/config/loader.py — the StrategyConfig dataclass grouped fields (intraday, exit, risk, execution) and the load_strategy_config mapping block (rk/ex_cfg/inf/svc_cfg dict reads); note sizing_equity_usd is an Optional with a default (the pattern to copy)
    - .planning/phases/07-strategy-optimization/07-PATTERNS.md sections "bot/config/loader.py", "rules.json" (exact field names, mapping lines, defaults)
    - tests/config/test_loader.py — how config-load tests assert field values and how a malformed config asserts ConfigError
  </read_first>
  <behavior>
    - Test: load_strategy_config on the real rules.json yields cfg.daily_circuit_breaker_r == 2.0, cfg.use_broker_stop_orders is True, cfg.rvol_tod_lookback_days == 14.
    - Test: a config dict with risk.daily_circuit_breaker_r set to a string (wrong type) raises ConfigError (jsonschema type=number).
    - Test: a config dict with execution.use_broker_stop_orders set to a non-boolean raises ConfigError.
    - Test: the three keys default correctly if absent from a minimal config (loader .get defaults: 2.0 / true / 14) — asserting no KeyError.
  </behavior>
  <action>
    In rules.json add risk.daily_circuit_breaker_r = 2.0, execution.use_broker_stop_orders = true, intraday_filters.I3_rvol_tod_lookback_days = 14 (place each alongside its existing siblings). In bot/config/schema.py add typed properties: risk.daily_circuit_breaker_r as {"type":"number"}, execution.use_broker_stop_orders as {"type":"boolean"}, intraday_filters.I3_rvol_tod_lookback_days as {"type":"integer"} (or "number" to match the file's existing numeric convention); do not add them to the required lists (keep them optional-with-default so older configs still validate). In bot/config/loader.py add three StrategyConfig fields — daily_circuit_breaker_r: float = 2.0, use_broker_stop_orders: bool = True, rvol_tod_lookback_days: int = 14 — under commented "Phase 7 additions" groups, and add mapping lines daily_circuit_breaker_r=float(rk.get("daily_circuit_breaker_r", 2.0)), use_broker_stop_orders=bool(ex_cfg.get("use_broker_stop_orders", True)), rvol_tod_lookback_days=int(inf.get("I3_rvol_tod_lookback_days", 14)). Do NOT hardcode 2.0 / 14 anywhere outside the loader default (CFG-01: consumers read cfg.*). Extend tests/config/test_loader.py with the four behavior tests.
  </action>
  <verify>
    <automated>python3 -m pytest tests/config/test_loader.py -x -q --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "daily_circuit_breaker_r" rules.json bot/config/schema.py bot/config/loader.py` matches in all three files
    - `grep -n "use_broker_stop_orders" rules.json bot/config/schema.py bot/config/loader.py` matches in all three files
    - `grep -n "I3_rvol_tod_lookback_days" rules.json bot/config/schema.py` and `grep -n "rvol_tod_lookback_days" bot/config/loader.py` match
    - A wrong-type value for daily_circuit_breaker_r or use_broker_stop_orders raises ConfigError in the test suite
    - `python3 -m pytest tests/config/test_loader.py -x -q` passes with the default-value and malformed-value assertions
  </acceptance_criteria>
  <done>The three Phase 7 thresholds live in rules.json, are schema-validated, and are exposed as typed StrategyConfig fields with safe defaults — no strategy literal is hardcoded in feature code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rules.json (operator-authored file) → loader | Config values cross into runtime; malformed input must fail closed |
| SQLite DB file → StateStore | Persisted state (baselines, breaker date) is read back and drives trading gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Malformed `daily_circuit_breaker_r` / `use_broker_stop_orders` in rules.json | mitigate | jsonschema `type` validation in schema.py (number/boolean); loader raises ConfigError; bot/main.py hard-exits (V5 Input Validation) |
| T-07-02 | Tampering | SQL injection via scan_date/code/time_bucket into store methods | mitigate | All new store methods use `?` parameterized placeholders exclusively — no value interpolation |
| T-07-03 | Spoofing | Stale circuit-breaker date from a prior session read as current | mitigate | Stored value is a plain ET date string; date comparison (owned by 07-05) auto-resets; persistence here is write/read/delete only |
| T-07-04 | Information Disclosure | broker_stop_order_id column leaks broker internals | accept | Local single-user SQLite on the operator's machine; no external surface (V4 not applicable) |
| T-07-SC | Tampering | Supply-chain: dependency installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); reuses moomoo-api/sqlite3/pytest already vetted |
</threat_model>

<verification>
- Full migration path: open a fresh StateStore → PRAGMA user_version == 5, tod_baselines present, positions.broker_stop_order_id present.
- Round-trip: upsert_tod_baselines → get_tod_baseline returns stored value; set_circuit_breaker_date → reopen → still set → clear → None.
- Config: `python3 -m pytest tests/config/test_loader.py tests/state/test_store.py tests/state/test_migrations.py -q` all green.
- Regression: `python3 -m pytest tests/ -q --tb=short` reports no new failures (baseline 514 passing on develop).
</verification>

<success_criteria>
- Migration 0005 lands (version 5) with tod_baselines + broker_stop_order_id, idempotent.
- Five StateStore methods exist, lock-guarded and parameterized; circuit-breaker date persists across reopen.
- Three new keys in rules.json + schema + StrategyConfig with safe defaults; malformed values fail closed (CFG-01).
- No feature code consumes hardcoded 2.0/14 — the values are read from cfg.* by downstream plans.
</success_criteria>

<output>
Create `.planning/phases/07-strategy-optimization/07-01-SUMMARY.md` when done.
</output>
