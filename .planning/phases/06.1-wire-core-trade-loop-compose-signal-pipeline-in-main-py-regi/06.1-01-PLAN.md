---
phase: 06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/service/test_trade_loop_integration.py
  - tests/position/test_manager.py
  - tests/gateway/test_gateway.py
autonomous: true
requirements: [SIG-01, SVC-02, SAFE-03, ALERT-02, EXEC-05, POS-03]
must_haves:
  truths:
    - "An integration test file exists that drives synthetic K_5M bar data through the composition seams and FAILS today (nothing is wired)"
    - "Each blocker/warning from the v1.0 audit has a named failing test that turns green only when its seam is wired"
    - "Running the Wave-0 test commands produces RED (collection/assertion failures), proving the wiring is genuinely absent"
  artifacts:
    - path: "tests/service/test_trade_loop_integration.py"
      provides: "Integration tests for BLOCKER-01/02/03 and WARNING-02 (push handler, entries gate, reconcile task, bar_buffer late-bind)"
      contains: "test_entries_disabled_blocks_entry"
      min_lines: 120
    - path: "tests/position/test_manager.py"
      provides: "test_exit_alert_fires_after_manage_exit covering WARNING-01 (D-01/D-02)"
      contains: "test_exit_alert_fires_after_manage_exit"
    - path: "tests/gateway/test_gateway.py"
      provides: "test_set_handler_delegates_to_quote_ctx + test_reconcile_once_externally_closed covering BLOCKER-01 set_handler and SAFE-03 drift"
      contains: "test_reconcile_once_externally_closed"
  key_links:
    - from: "tests/service/test_trade_loop_integration.py"
      to: "bot.service.bot.TradingBot._on_bar_closed"
      via: "_make_minimal_bot helper + await bot._on_bar_closed(bar_data)"
      pattern: "_on_bar_closed"
    - from: "tests/gateway/test_gateway.py"
      to: "MoomooGateway.set_handler / reconcile_once"
      via: "_make_gateway_with_mocks helper"
      pattern: "set_handler|reconcile_once"
---

<objective>
Create the Wave-0 failing test infrastructure (TDD RED phase) that proves the core trade loop is NOT wired today and will turn green only as each composition seam lands in Waves 1–3.

This phase is a wiring/composition phase: 405 existing unit tests cover leaf components in isolation, but ZERO tests cover the composition seams (the exact blind spot the v1.0 milestone audit caught). The integration test file built here is the Nyquist measurement point that proves the wiring actually exists.

Per `.planning/phases/.../06.1-VALIDATION.md`, these tests cover SIG-01/BLOCKER-01 (push handler registered), SVC-02/BLOCKER-02/D-06 (entries gate), SAFE-03/BLOCKER-03/D-07/D-08 (reconcile task + drift), ALERT-02/EXEC-05/WARNING-01/D-01/D-02 (exit-fill→alert), POS-03/WARNING-02/D-05 (bar_buffer late-bind).

Purpose: lock the acceptance contract before implementation so the executor in later waves has a concrete, runnable definition of "done" for each seam.
Output: one new test file plus added test functions in two existing test files, all RED (failing) at commit time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-CONTEXT.md
@.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-RESEARCH.md
@.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-PATTERNS.md
@.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/service/test_trade_loop_integration.py with failing seam tests</name>
  <files>tests/service/test_trade_loop_integration.py</files>
  <read_first>
    - tests/service/test_bot.py (analog: `_make_bot_with_mocks` helper at lines 23–65, `TradingBot(...)` construction at line 54, async test patterns)
    - tests/conftest.py (analog: `make_bar_event` factory, `minimal_rules` fixture)
    - 06.1-PATTERNS.md → section "tests/service/test_trade_loop_integration.py (NEW file)" (copy-ready `_make_minimal_bot`, `_make_synthetic_bar_data`, `test_entries_disabled_blocks_entry`)
    - 06.1-RESEARCH.md → "Integration Test Seam" code block (synthetic bar push via `agg._handle_row`) and "Phase Requirements → Test Map"
    - 06.1-VALIDATION.md → "Per-Task Verification Map" (exact test names + commands)
    - 06.1-CONTEXT.md → D-05/D-06/D-08 (the behaviors these tests assert)
  </read_first>
  <behavior>
    - test_push_handler_registered: TradingBot.run() (or the documented run() seam) calls gateway.set_handler with a BarAggregator before any subscribe — assert set_handler invoked with a CurKlineHandlerBase instance. RED today: run() never calls set_handler.
    - test_bar_push_fires_on_bar_closed: pushing two synthetic K_5M rows (advancing time_key) through a real BarAggregator constructed with a fake `_on_bar_closed` callback fires the callback exactly once with the closed bar's data. RED today only if asserting through TradingBot._on_bar_closed (method does not exist).
    - test_entries_disabled_blocks_entry: bot._entries_enabled=False → bot._on_bar_closed(bar_data) calls position_manager.on_bar exactly once (management) but NEVER calls signal_engine.on_bar (entry branch blocked). RED today: _on_bar_closed does not exist.
    - test_entries_enabled_allows_entry: bot._entries_enabled=True → bot._on_bar_closed runs signal→risk→consume_intent; assert all three async mocks called once. RED today: _on_bar_closed does not exist.
    - test_reconcile_task_started: after the run()-startup seam, bot has a non-None reconciliation task (asyncio.Task) referencing gateway.reconciliation_loop. RED today: no reconcile task is created.
    - test_bar_buffer_late_bound: after the run()-startup seam, position_manager._bar_buffer is the same object as bar_aggregator._bar_buffer (identity, `is`). RED today: PositionManager constructed with bar_buffer=None and never late-bound.
  </behavior>
  <action>
    Create tests/service/test_trade_loop_integration.py. Copy the file header docstring, the `_make_minimal_bot(...)` helper, and the `_make_synthetic_bar_data(...)` helper verbatim from 06.1-PATTERNS.md "tests/service/test_trade_loop_integration.py (NEW file)". The helper builds a real `bot.service.bot.TradingBot` with mocked deps and the TWO NEW constructor params `signal_engine` and `risk_engine` (these params do not exist yet — that is the intended RED for the constructor-level tests; do not pre-add the params to TradingBot).
    Write the six test functions named exactly per 06.1-VALIDATION.md: test_push_handler_registered, test_bar_push_fires_on_bar_closed, test_entries_disabled_blocks_entry, test_entries_enabled_allows_entry, test_reconcile_task_started, test_bar_buffer_late_bound. Use `@pytest.mark.asyncio` on all async tests. For the gate tests use `MagicMock` deps with `AsyncMock` on `.on_bar`, `.on_signal`, `.consume_intent` per the PATTERNS excerpt; assert with `.assert_called_once()` / `.assert_not_called()`.
    For test_bar_push_fires_on_bar_closed, drive a real `BarAggregator(loop, fake_on_bar_closed)` and push `row_a` then `row_b` (advancing time_key) via `agg._handle_row(...)`, then `await asyncio.sleep(0)` to drain the run_coroutine_threadsafe-scheduled coroutine, asserting the callback fired once with the closed bar (the 06.1-RESEARCH.md "Integration Test Seam" block is copy-ready).
    For test_reconcile_task_started and test_bar_buffer_late_bound, exercise the run()-startup seam minimally: these MAY be written to call a small helper that performs only the post-readiness-gate startup block, OR be marked to assert on bot attributes set by run() (`bot._reconcile_task`, `bot._position_manager._bar_buffer`). Choose whichever keeps the test deterministic without starting the scheduler or the kill-switch loop — document the chosen seam in a test docstring. These tests MUST be RED now (the attributes/methods do not exist).
    Do NOT add `xfail`/`skip` markers — these must fail loudly as collection or assertion errors so the RED state is unambiguous. Do NOT modify any bot/ source file in this task.
  </action>
  <verify>
    <automated>cd /Users/acdc/Documents/AI/ai-snp-trading-claude && python -m pytest tests/service/test_trade_loop_integration.py -x 2>&1 | tail -20; test ${PIPESTATUS[0]} -ne 0 &amp;&amp; echo "RED_CONFIRMED" || echo "ERROR_TESTS_PASSED_BEFORE_WIRING"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/service/test_trade_loop_integration.py exists and is collectable by pytest (no syntax/import errors that prevent collection of the file itself — failures must be assertion/AttributeError on the unwired seams, not file-level SyntaxError).
    - `python -m pytest tests/service/test_trade_loop_integration.py -x` exits non-zero (RED) — printing `RED_CONFIRMED`. A passing run here means the tests do not actually exercise the missing wiring and must be rewritten.
    - All six test function names from 06.1-VALIDATION.md are present (grep: `grep -c 'def test_' tests/service/test_trade_loop_integration.py` ≥ 6).
  </acceptance_criteria>
  <done>The integration test file exists with six named failing tests covering BLOCKER-01/02/03 and WARNING-02; the suite is RED, proving the seams are unwired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing exit-alert and gateway-seam tests to existing test files</name>
  <files>tests/position/test_manager.py, tests/gateway/test_gateway.py</files>
  <read_first>
    - tests/position/test_manager.py (analog: `test_higher_swing_low_raises_trail_stop` block at lines 528–567, the bar_buffer fixture wiring at 534–551, the `asyncio.run(mgr.on_bar(bar))` call at line 564)
    - tests/gateway/test_gateway.py (analog: `_make_gateway_with_mocks` helper at lines 50–65; `import pandas as pd` already present at line 19; async test + AsyncMock patterns)
    - 06.1-PATTERNS.md → "tests/position/test_manager.py — add exit-alert test" and "tests/gateway/test_gateway.py — add set_handler + reconcile_once tests" (both copy-ready)
    - 06.1-RESEARCH.md → Pattern 7 (exit-fill fix) and Pattern 6 (reconcile_once drift) — the behaviors being asserted
    - 06.1-CONTEXT.md → D-01/D-02 (exit-fill→alert) and D-07 (reconcile drift cases)
  </read_first>
  <behavior>
    - test_exit_alert_fires_after_manage_exit (tests/position/test_manager.py): with engine.manage_exit mocked to return filled_qty=10, triggering a stop-out (or calling the exit path that calls _place_exit_order) causes the on_exit_alert callback to be invoked once with (code, reason, r_multiple). RED today: _place_exit_order returns None, so no alert fires from the return value.
    - test_set_handler_delegates_to_quote_ctx (tests/gateway/test_gateway.py): gw.set_handler(handler) calls gw._quote_ctx.set_handler(handler) exactly once. RED today: MoomooGateway has no set_handler method (AttributeError).
    - test_reconcile_once_externally_closed (tests/gateway/test_gateway.py): broker returns empty positions while manager._positions has one ACTIVE code not in manager._exiting → reconcile_once marks it CLOSED (DB update issued), removes it from _positions, fires alerter.send, and returns the code in result["closed"]. RED today: reconcile_once is a no-op returning {"drift": {}} and its signature takes no store/manager/alerter args (TypeError).
  </behavior>
  <action>
    In tests/position/test_manager.py add `test_exit_alert_fires_after_manage_exit`: build a PositionManager with a mock engine whose `manage_exit` is an AsyncMock returning `10`, pass an `on_exit_alert` MagicMock callback, set up an ACTIVE position whose FSM triggers a stop-out on a synthetic bar (or call `_trigger_stop_out`/`_place_exit_order` directly per the PATTERNS analog), drive via `asyncio.run(...)`, and assert the `on_exit_alert` callback was called once. The test asserts the D-01/D-02 contract (alert driven by the manage_exit return value, not exit_order_id). It must be RED now because `_place_exit_order` returns None and the call sites set `pos.exit_order_id` instead of applying filled_qty.
    In tests/gateway/test_gateway.py add `test_set_handler_delegates_to_quote_ctx` and `test_reconcile_once_externally_closed`, both copy-ready from 06.1-PATTERNS.md "tests/gateway/test_gateway.py — add set_handler + reconcile_once tests". Reuse `_make_gateway_with_mocks()`; for the reconcile test, set `gw.get_positions = AsyncMock(return_value=(0, pd.DataFrame()))`, a `mock_manager._positions={"US.AAPL": mock_pos}`, `mock_manager._exiting=set()`, a `mock_store.conn` MagicMock, and `mock_alerter.send=AsyncMock()`; assert `"US.AAPL" in result["closed"]` and `mock_store.conn.execute.assert_called()`.
    Do NOT modify any bot/ source file. Do NOT add skip/xfail markers — these must fail as AttributeError/TypeError/assertion to prove the seams are unwired.
  </action>
  <verify>
    <automated>cd /Users/acdc/Documents/AI/ai-snp-trading-claude && python -m pytest tests/position/test_manager.py::test_exit_alert_fires_after_manage_exit tests/gateway/test_gateway.py::test_set_handler_delegates_to_quote_ctx tests/gateway/test_gateway.py::test_reconcile_once_externally_closed 2>&1 | tail -20; test ${PIPESTATUS[0]} -ne 0 &amp;&amp; echo "RED_CONFIRMED" || echo "ERROR_TESTS_PASSED_BEFORE_WIRING"</automated>
  </verify>
  <acceptance_criteria>
    - The three new test functions exist (grep each name returns 1 match in its file).
    - `python -m pytest tests/position/test_manager.py::test_exit_alert_fires_after_manage_exit tests/gateway/test_gateway.py::test_set_handler_delegates_to_quote_ctx tests/gateway/test_gateway.py::test_reconcile_once_externally_closed` exits non-zero (RED), printing `RED_CONFIRMED`.
    - The rest of the existing test_manager.py / test_gateway.py suites still collect (no file-level syntax errors introduced): `python -m pytest tests/position/test_manager.py tests/gateway/test_gateway.py --collect-only -q` succeeds.
  </acceptance_criteria>
  <done>Three failing tests added to existing files covering WARNING-01 (exit-fill→alert), BLOCKER-01 (set_handler), and SAFE-03 (reconcile drift); all RED, existing suites still collect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → production code | Tests exercise unwired seams; the only "input" crossing is synthetic bar_data dicts and mocked broker responses. No live broker or OpenD. |

## STRIDE Threat Register (logic-integrity, per security_enforcement)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-W0-TDD | Tampering | Wave-0 RED gate | mitigate | Tests MUST be RED at commit (verify asserts non-zero exit). A green Wave-0 run means the tests do not exercise the missing wiring and silently bless a broken loop — the `ERROR_TESTS_PASSED_BEFORE_WIRING` branch flags this. |
| T-06.1-W0-COLLECT | Denial of Service | pytest collection | accept | A file-level SyntaxError would mask the intended RED. Acceptance criteria require `--collect-only` to succeed, so failures are assertion/Attribute/Type errors, not collection breakage. |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (06.1-RESEARCH.md: "installs no new external packages"). N/A — no install task to gate. |
</threat_model>

<verification>
- `python -m pytest tests/service/test_trade_loop_integration.py -x` → RED (non-zero exit).
- `python -m pytest tests/position/test_manager.py::test_exit_alert_fires_after_manage_exit tests/gateway/test_gateway.py::test_set_handler_delegates_to_quote_ctx tests/gateway/test_gateway.py::test_reconcile_once_externally_closed` → RED.
- `python -m pytest tests/ --collect-only -q` → all files collect (no syntax errors introduced).
</verification>

<success_criteria>
- One new integration test file + three new test functions in existing files, all named exactly per 06.1-VALIDATION.md.
- Every named test is RED today (the wiring it asserts does not exist).
- No bot/ source files modified in this plan.
- Existing suites still collect.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (new symbols introduced by THIS plan)

- `tests/service/test_trade_loop_integration.py` (NEW FILE) with: `_make_minimal_bot`, `_make_synthetic_bar_data`, `test_push_handler_registered`, `test_bar_push_fires_on_bar_closed`, `test_entries_disabled_blocks_entry`, `test_entries_enabled_allows_entry`, `test_reconcile_task_started`, `test_bar_buffer_late_bound`
- `tests/position/test_manager.py::test_exit_alert_fires_after_manage_exit` (NEW test)
- `tests/gateway/test_gateway.py::test_set_handler_delegates_to_quote_ctx` (NEW test)
- `tests/gateway/test_gateway.py::test_reconcile_once_externally_closed` (NEW test)
</artifacts_produced>

<output>
Create `.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-01-SUMMARY.md` when done.
</output>
