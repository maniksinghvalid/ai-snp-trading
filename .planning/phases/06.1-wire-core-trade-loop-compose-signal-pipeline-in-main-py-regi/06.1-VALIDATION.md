---
phase: 06.1
slug: wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-24
validated: 2026-07-07
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 06.1-RESEARCH.md "## Validation Architecture". The 405 existing unit tests cover leaf
> components in isolation; this phase's risk surface is the **composition seams**, which have ZERO
> coverage today. The integration test file is the Nyquist measurement point proving the trade loop
> is actually wired.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest >= 8.0 + pytest-asyncio >= 0.24 |
| **Config file** | none (inline `@pytest.mark.asyncio` markers / pyproject.toml) |
| **Quick run command** | `pytest tests/service/test_trade_loop_integration.py tests/position/test_manager.py -x` |
| **Full suite command** | `pytest tests/ -x` |
| **Estimated runtime** | ~30 seconds (quick) / ~90 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/service/test_trade_loop_integration.py tests/position/test_manager.py -x`
- **After every plan wave:** Run `pytest tests/ -x`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| W0 | — | 0 | SIG-01 / BLOCKER-01 | — | Push handler registered before subscribe; no unconsumed bars | integration | `pytest tests/service/test_trade_loop_integration.py::test_push_handler_registered -x` | ✅ | ✅ green |
| W0 | — | 0 | SIG-01 | — | K_5M push reaches BarAggregator → fires `_on_bar_closed` | integration | `pytest tests/service/test_trade_loop_integration.py::test_bar_push_fires_on_bar_closed -x` | ✅ | ✅ green |
| W0 | — | 0 | SVC-02 / BLOCKER-02 / D-06 | T-06.1-ENTRY-GATE | `_entries_enabled=False` blocks `consume_intent`; management still runs | integration | `pytest tests/service/test_trade_loop_integration.py::test_entries_disabled_blocks_entry -x` | ✅ | ✅ green |
| W0 | — | 0 | SVC-02 / BLOCKER-02 | T-06.1-ENTRY-GATE | `_entries_enabled=True` allows full signal→risk→consume_intent | integration | `pytest tests/service/test_trade_loop_integration.py::test_entries_enabled_allows_entry -x` | ✅ | ✅ green |
| W0 | — | 0 | SAFE-03 / BLOCKER-03 / D-08 | — | `reconciliation_loop` task started in `TradingBot.run()` | unit | `pytest tests/service/test_trade_loop_integration.py::test_reconcile_task_started -x` | ✅ | ✅ green |
| W0 | — | 0 | SAFE-03 / D-07 / D-09 | T-06.1-RECONCILE | `reconcile_once()` detects externally-closed position → mark CLOSED + alert | integration | `pytest tests/gateway/test_gateway.py::test_reconcile_once_externally_closed -x` | ✅ | ✅ green |
| W0 | — | 0 | EXEC-05 / ALERT-02 / WARNING-01 / D-01 / D-02 | — | Exit alert fires after `manage_exit()` returns filled_qty > 0 | integration | `pytest tests/position/test_manager.py::test_exit_alert_fires_after_manage_exit -x` | ✅ | ✅ green |
| W0 | — | 0 | POS-03 / WARNING-02 / D-05 | — | `PositionManager._bar_buffer` is not None after `TradingBot.run()` late-binds it | unit | `pytest tests/service/test_trade_loop_integration.py::test_bar_buffer_late_bound -x` | ✅ | ✅ green |
| W0 | — | 0 | BLOCKER-01 (set_handler) | — | `MoomooGateway.set_handler()` delegates to `quote_ctx.set_handler` | unit | `pytest tests/gateway/test_gateway.py::test_set_handler_delegates_to_quote_ctx -x` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `tests/service/test_trade_loop_integration.py` — new integration file covering BLOCKER-01, BLOCKER-02, BLOCKER-03, WARNING-02 (SIG-01, SAFE-03, SVC-02, POS-03). Uses fake gateway + fake broker + synthetic K_5M bar pushes (no OpenD).
- [x] `tests/position/test_manager.py::test_exit_alert_fires_after_manage_exit` — covers WARNING-01 (D-01/D-02, ALERT-02, EXEC-05): `_place_exit_order` returns filled_qty and the manager applies exit + fires `on_exit_alert`.
- [x] `tests/gateway/test_gateway.py::test_set_handler_delegates_to_quote_ctx` — covers the new `set_handler()` wrapper on MoomooGateway.
- [x] `tests/gateway/test_gateway.py::test_reconcile_once_externally_closed` — covers SAFE-03 drift logic (externally-closed detection, orphan adopt, skip-active-exit).

*pytest + pytest-asyncio already in use across the suite — no framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end trade loop against live OpenD/SIMULATE | (audit step 2 — DEFERRED) | Requires OpenD GUI running + live 5m subscription; out of scope for this phase | Deferred to the separate Live SIMULATE UAT remediation step; NOT a gate for 06.1. |

*All in-scope (wiring) behaviors have automated integration coverage via fake gateway/broker. Live-broker confirmation is explicitly deferred per CONTEXT.md `<deferred>`.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter
- [x] `wave_0_complete: true` — all 9 Wave-0 tests exist and run green

**Approval:** approved 2026-06-24 (pre-execution contract); validated 2026-07-07 (post-execution audit — all Wave-0 tests green).

---

## Validation Audit 2026-07-07

Phase docs (incl. this VALIDATION.md and VERIFICATION.md) were stranded on branch
`plan/phase-05-service-orchestration` and never merged to `develop`; the 06.1 *implementation*
(composed trade loop in `main.py`, `SignalEngine`/`PositionManager`/scanner wiring, `MoomooGateway.set_handler`,
`reconcile_once`) is on `develop` and was built on by phases 06.2/07. This audit recovered the docs and
re-ran the full Per-Task Map against the live `develop` tree.

All 9 Wave-0 tests exist and pass green (`9 passed in 0.23s`):
`test_push_handler_registered`, `test_bar_push_fires_on_bar_closed`, `test_entries_disabled_blocks_entry`,
`test_entries_enabled_allows_entry`, `test_reconcile_task_started`, `test_bar_buffer_late_bound`,
`test_reconcile_once_externally_closed`, `test_set_handler_delegates_to_quote_ctx`,
`test_exit_alert_fires_after_manage_exit`.

| Metric | Count |
|--------|-------|
| Requirements in map | 9 |
| COVERED (green) | 9 |
| PARTIAL | 0 |
| MISSING | 0 |
| Gaps found | 0 |
| Resolved | 0 |
| Escalated (manual-only) | 1 (live OpenD/SIMULATE E2E — pre-existing deferral, not a 06.1 gate) |

**Result:** Phase 06.1 is Nyquist-compliant. No test generation or auditor spawn needed.
