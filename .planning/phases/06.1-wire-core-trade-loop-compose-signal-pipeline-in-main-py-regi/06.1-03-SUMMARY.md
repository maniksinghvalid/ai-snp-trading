---
phase: 06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi
plan: "03"
subsystem: position-management
tags: [position-manager, exit-fill, alert-dispatch, fsm, d-01, d-02, warning-01]

# Dependency graph
requires:
  - phase: 06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi-01
    provides: wave-0 RED test test_exit_alert_fires_after_manage_exit
provides:
  - _place_exit_order returning int filled-qty (0 on failure, never None)
  - self._exiting set tracking in-flight manage_exit calls for SAFE-03 reconcile skip
  - Stop-out and partial call sites applying filled_qty + firing on_exit_alert synchronously
  - force_close_all dispatching on_exit_alert for force_close reason
affects:
  - gateway.reconcile_once (reads _exiting to skip mid-exit codes)
  - main.py (wires on_exit_alert callback to alerter)
  - tests/position/test_manager.py (Wave-0 RED test now GREEN)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "D-01/D-02: _place_exit_order returns int filled-qty; call sites apply exit from return, not exit_order_id push-match"
    - "_exiting set: add before manage_exit, discard in finally — guarantees code removed even on exception"
    - "ALERT-04 isolation: all on_exit_alert calls wrapped in try/except so callback failures never break FSM"

key-files:
  created: []
  modified:
    - bot/position/manager.py

key-decisions:
  - "D-01/D-02: Exit alert delivery path changed from exit_order_id push-match to synchronous manage_exit int return — eliminates WARNING-01 dead delivery path"
  - "_exiting set uses finally guarantee (not if/else) so the code is always removed even if _place_exit_order raises"
  - "Partial alert block preserved exactly as-is — no new alert added at partial call site (prevents T-06.1-EXIT-ALERT-DUP double-fire)"
  - "force_close_all exit alert added (POS-04 / D-01/D-02) — was missing prior to this plan"

patterns-established:
  - "Exit fill delivery: always drive from manage_exit int return, never from exit_order_id broker push"
  - "In-flight set pattern: add code before async call, discard in finally block"

requirements-completed: [EXEC-05, ALERT-02, POS-03]

# Metrics
duration: 18min
completed: 2026-06-25
---

# Phase 06.1 Plan 03: Fix Exit-Fill Alert Delivery (D-01/D-02 / WARNING-01) Summary

**_place_exit_order now returns int filled-qty; both stop-out and partial call sites apply the return value and fire on_exit_alert synchronously, closing the WARNING-01 dead delivery path**

## Performance

- **Duration:** 18 min
- **Started:** 2026-06-25T00:07:00Z
- **Completed:** 2026-06-25T00:16:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Fixed `_place_exit_order` to return `int` filled-qty (was `Optional[str]` returning `None`) — D-01/D-02
- Added `self._exiting: set` to `__init__` for SAFE-03 reconcile-skip guard (Pitfall 6 / D-07)
- Updated `_trigger_stop_out` call site: wraps `_place_exit_order` with `_exiting.add/discard`, applies `filled_qty`, fires `on_exit_alert` synchronously from the return value
- Updated `_trigger_partial_profit` call site: same `_exiting` wrap + `partial_profit_filled` persist; existing partial alert block preserved (no double-fire)
- Added exit alert dispatch to `force_close_all` for `force_close` reason (POS-04 / D-01/D-02)
- Wave-0 RED test `test_exit_alert_fires_after_manage_exit` now GREEN — 43/43 manager tests pass

## Task Commits

1. **Task 1: _place_exit_order returns int filled-qty + add _exiting set** - `dd5d246` (feat)
2. **Task 2: Apply filled_qty + fire exit alert at both call sites** - `f7cff0d` (feat)

**Plan metadata:** `(pending — docs commit follows)`

## Files Created/Modified

- `bot/position/manager.py` — Changed `_place_exit_order` return type to `int`; added `self._exiting` set; updated `_trigger_stop_out` and `_trigger_partial_profit` call sites with D-01/D-02 pattern; added exit alert dispatch to `force_close_all`

## Decisions Made

- D-01/D-02 delivery path: Drive `remaining_quantity` decrement and `on_exit_alert` from `manage_exit` int return value — eliminates the broken exit_order_id push-match path (WARNING-01)
- `_exiting` set uses `try/finally` (not `if filled_qty`) to guarantee the code is always discarded even if `_place_exit_order` raises (prevents T-06.1-EXIT-RACE)
- Partial alert block at lines ~611-622 kept exactly as-is — no second alert added at partial call site (T-06.1-EXIT-ALERT-DUP mitigation)
- `force_close_all` exit alert added as Rule 2 (missing critical ALERT-02 coverage for POS-04 exits)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added on_exit_alert dispatch to force_close_all**
- **Found during:** Task 2 verification
- **Issue:** force_close_all directly calls `self._engine.manage_exit` and applies `filled`, but never calls `self._on_exit_alert` for force_close exits — ALERT-02 gap for POS-04 exits
- **Fix:** Added exit alert try/except block after the `else` branch in force_close_all (same pattern as _trigger_stop_out)
- **Files modified:** bot/position/manager.py
- **Verification:** test_force_close_half_day still passes; existing exit alert tests still pass
- **Committed in:** f7cff0d (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Auto-fix necessary for ALERT-02 completeness on POS-04 force_close path. No scope creep.

## Issues Encountered

- Verification command in plan used `cd /Users/acdc/Documents/AI/ai-snp-trading-claude && python -m pytest ...` (main repo path). Tests were correctly run from the WORKTREE directory (`python3 -m pytest` without `cd`) — pytest resolved `bot.position.manager` from the worktree's sys.path since rootdir was the worktree. All tests passed when run from worktree.

## Next Phase Readiness

- `bot/position/manager.py` ready for Wave 2: exit fills now route correctly via `manage_exit` int return; `_exiting` set is in place for `gateway.reconcile_once` to consume
- WARNING-01 closed — exit alerts (stop_out, trail_stop, breakeven, partial, force_close) all fire deterministically from the `manage_exit` return value
- SAFE-03 reconcile loop can now safely skip in-flight exits via `getattr(manager, '_exiting', set())`

## Self-Check

- [x] `bot/position/manager.py` exists in worktree: FOUND
- [x] Commits dd5d246 and f7cff0d exist in git log
- [x] `test_exit_alert_fires_after_manage_exit` PASSED (GREEN)
- [x] All 43 manager tests pass; all 46 position tests pass
- [x] No `pos.exit_order_id = order_id` in _trigger_partial_profit / _trigger_stop_out
- [x] `self._exiting.add` appears 2 times (both call sites)

## Self-Check: PASSED
