---
status: partial
phase: 06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi
source: [06.1-01-SUMMARY.md, 06.1-02-SUMMARY.md, 06.1-03-SUMMARY.md, 06.1-04-SUMMARY.md, 06.1-05-SUMMARY.md, 06.1-06-SUMMARY.md, 06.1-07-SUMMARY.md]
started: 2026-06-25T08:39:13Z
updated: 2026-06-25T18:05:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing paused — 4 items outstanding: Tests 4-7 blocked on live trigger conditions (entries-disabled path, stop-out fill, partial scale-out, broker orphan). Their shared live infra (management-every-bar + reconcile loop) is confirmed operational; each is covered by named passing tests in the suite. Operator opted (2026-06-25) to NOT mutate the paper account to manufacture triggers — true live trigger validation deferred to a natural-conditions SIMULATE session.]

## Tests

### 1. Cold Start Smoke Test
expected: From a clean state, `python3 -m bot` (or `python3 -c "import bot.main"`) boots/imports with the full scan→bar→signal→size→order→manage→alert composition wired and no traceback. Missing OpenD produces a clean, explained connectivity failure, not a wiring crash.
result: pass

### 2. Full Test Suite Green
expected: Running `python3 -m pytest -q` from the repo root passes the full suite — 428 passed, 1 skipped — with no failures or collection errors. This is the regression gate proving all 6 composition seams and the 3 safety gap-closures are wired.
result: pass

### 3. Trade Loop Composed — Pipeline Runs on Each Closed 5m Bar (SIG-01)
expected: With OpenD running and the market open, the bot subscribes the capped watchlist, the BarAggregator push handler is registered, and on each closed 5-minute bar the bot runs the signal→risk→exec pipeline. Position management runs every bar; entries are evaluated and (when enabled) sized to 1% risk and placed.
result: pass
note: |
  RE-TESTED LIVE 2026-06-25 ~14:00 ET (SIMULATE acc 1727266) after gap-closure
  plans 06.1-08 (RLock + check_same_thread=False) and 06.1-09 (StateStore
  encapsulation). Three live confirmations:
  1. Boot/wiring: readiness_gate_passed; startup_reconcile_done broker=1 state=1;
     position_reconstructed US.NVDY; push_handler_registered handler=BarAggregator;
     jobs_registered (all 5) + Scheduler started + bot_started.
  2. Original blocker FIXED: faithful repro of bot.py:544 executor path ran
     run_intraday_rescan on worker thread 6170144768 against the store opened on
     main thread 8271240832 (same main-thread id as the original crash) →
     rescan_complete watchlist_count=20; persisted + read back cross-thread;
     NO sqlite3.ProgrammingError.
  3. on_bar pipeline fired 20× on the real 14:00:00 closed bar (one per subscribed
     code); position_manager.on_bar (management) ran every bar; entry branch
     reachable (entries_enabled=True); zero errors. Reconcile loop ran 5× (75s
     cadence): adopted=[] closed=[] reprotected=[] (correct no-op, no drift).

### 4. Entries Gate — Management Continues When Entries Paused (SVC-02)
expected: When a readiness/startup failure disables entries (`_entries_enabled = False`), the bot stops placing NEW entry orders but continues managing existing positions (stops, breakeven, trail, exits still fire every bar). After readiness passes, entries re-enable.
result: blocked
blocked_by: server
reason: "Entries-ENABLED path confirmed live 2026-06-25 ~14:00 ET: 20 closed bars ran _process_bar → position_manager.on_bar (management) every bar with entry branch reachable (entries_enabled=True), zero errors. The entries-DISABLED branch (the test's core: management continues while entries paused) requires toggling the gate through the live order path; operator opted not to mutate the paper account. Covered by test suite (test_entries_disabled_blocks_entry / test_entries_enabled_allows_entry)."

### 5. Safe Exit Path — Alert Fires, No Unmanaged Position (SAFE-03 / CR-01 / ALERT-02)
expected: On a full stop-out fill, the position closes and a Telegram exit alert fires. On a zero/short exit fill, the position is re-protected (phase reverts to managed, retried next bar) and is NEVER left live-but-unmanaged. The reconcile loop additionally re-arms any CLOSED-but-held / qty-drift position to broker truth.
result: blocked
blocked_by: server
reason: "Requires a live stop-out exit fill — manufacturing it would CLOSE the operator's real NVDY paper position; operator opted not to mutate the account. Shared infra confirmed live 2026-06-25: reconcile_once ran 5× (75s cadence) against broker truth with correct no-op (closed=[] reprotected=[]). Covered by test suite (test_zero_fill_stop_out_reprotects / test_full_fill_stop_out_closes / test_reconcile_once_reprotects_closed_but_held)."

### 6. Quantity Tracks Broker Truth After Partial Scale-Out (EXEC-05 / CR-02)
expected: After a partial scale-out, remaining_quantity equals broker truth with no double-decrement. Example: a 300-share position scaling out 99 leaves remaining_quantity = 201 (not 102). Short partial fills are credited, not discarded; a partial that drives remaining to 0 marks the position CLOSED.
result: blocked
blocked_by: server
reason: "Requires a live partial scale-out fill (places sell orders on the paper account); operator opted not to mutate the account. Covered by test suite (test_partial_fill_keeps_remaining_at_broker_truth / test_partial_short_fill_restores_shortfall)."

### 7. Mid-Session Orphan Adopted and Protected (SAFE-03 / CR-03)
expected: A position present at the broker but absent from in-memory state (orphan) is adopted by `reconcile_once`: registered as a managed PositionState, persisted DB-first, subscribed to its 5m feed, and managed by on_bar the SAME cycle — it immediately receives stop/breakeven/trail/exit. A triggering bar after adoption fires a STOP_OUT.
result: blocked
blocked_by: server
reason: "Requires a broker-side orphan (position at broker absent from state) — manufacturing it places a speculative paper buy; operator opted not to mutate the account. Live reconcile_once ran 5× 2026-06-25 with broker=1/state=1 matched (adopted=[]) — the adoption branch had nothing to adopt. Covered by test suite (test_adopt_orphan_position_is_managed_by_on_bar / test_reconcile_once_orphan_registers_in_memory)."

## Summary

total: 7
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 4

## Gaps

- truth: "The live bot subscribes the watchlist and runs its scheduled jobs (premarket scan, market-open subscribe, intraday rescan, EOD report) without error"
  status: resolved
  resolution: "Fixed by gap-closure plans 06.1-08 (RLock + check_same_thread=False on the StateStore connection) and 06.1-09 (StateStore method encapsulation so all DB access serializes through self._lock). RE-VERIFIED LIVE 2026-06-25 ~14:00 ET: the exact bot.py:544 executor path (run_intraday_rescan on a worker thread against the main-thread store) completed clean with no sqlite3.ProgrammingError, persisted watchlist_count=20, and read back cross-thread. Regression test (TestStressRowFactoryRace) gives WR-01 deterministic teeth in the suite."
  reason: "User reported: Live run: intraday_rescan_error — sqlite3.ProgrammingError: SQLite objects created in a thread can only be used in that same thread (thread 8271240832 created, used in 6192738304)"
  severity: blocker
  test: 3
  root_cause: "bot/state/store.py:195 opens the SQLite connection with sqlite3.connect(self._db_path) using the default check_same_thread=True, binding the connection to the main thread. Four scheduled jobs in bot/service/bot.py pass self._store into loop.run_in_executor(None, ...) ThreadPoolExecutor worker threads, where conn.execute/read runs on a different thread → ProgrammingError. Surfaced live for the first time because Phase 06.1 wired the loop so the scheduler jobs actually run; unit tests call these paths synchronously (same thread) so never caught it."
  artifacts:
    - path: "bot/state/store.py"
      issue: "line 195 sqlite3.connect(self._db_path) uses default check_same_thread=True; no thread-safety strategy for executor-thread access"
    - path: "bot/service/bot.py"
      issue: "_job_premarket_scan (447), _job_market_open_subscribe (484), _job_intraday_rescan (526), _job_eod_report (595) all touch self._store inside run_in_executor worker threads"
    - path: "bot/scanner/scanner.py"
      issue: "_persist_watchlist (203) calls conn.execute on the passed store.conn from the executor thread"
  missing:
    - "A thread-safe store-access strategy for run_in_executor jobs (e.g. check_same_thread=False with serialized access, a per-thread/connection-per-call pattern, or running store writes back on the event-loop thread). Decision belongs in planning."
    - "Regression test that exercises a store write/read through run_in_executor (cross-thread) so the crash is caught by the suite, not only live."
    - "Verify the fix covers all four executor jobs, not just intraday rescan."
  debug_session: ""
