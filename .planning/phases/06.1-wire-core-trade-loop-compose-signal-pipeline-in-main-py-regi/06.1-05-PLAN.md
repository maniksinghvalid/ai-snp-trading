---
phase: 06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi
plan: 05
type: execute
wave: 3
depends_on: ["06.1-01", "06.1-04"]
files_modified:
  - bot/main.py
autonomous: true
requirements: [SIG-01, RISK-01]
must_haves:
  truths:
    - "main.py constructs the loop-independent SignalEngine and RiskEngine leaves and injects them into TradingBot, keeping main.py pure DI (D-04)"
    - "RiskEngine receives the SignalEngine reference so note_intent_emitted() drives the D-09 daily-cap burst guard (RISK-05)"
    - "The composed bot, run end-to-end, has every pipeline component instantiated and reachable — no leaf is orphaned in composition"
  artifacts:
    - path: "bot/main.py"
      provides: "SignalEngine + RiskEngine construction and injection into TradingBot"
      contains: "SignalEngine("
      exports: ["main"]
  key_links:
    - from: "bot/main.py"
      to: "TradingBot(signal_engine=..., risk_engine=...)"
      via: "two new constructor kwargs"
      pattern: "signal_engine=signal_engine|risk_engine=risk_engine"
    - from: "bot/main.py::RiskEngine"
      to: "signal_engine reference"
      via: "RiskEngine(..., signal_engine=signal_engine)"
      pattern: "signal_engine=signal_engine"
---

<objective>
Complete the composition by adding the loop-independent leaf builds in `bot/main.py` (D-04): construct `SignalEngine` and `RiskEngine` and inject them into the `TradingBot` constructor (which gained these params in Plan 04).

main.py stays pure DI — it builds leaf components and injects; the loop-dependent objects (BarAggregator) are still built inside `TradingBot.run()` per D-03. SignalEngine and RiskEngine are loop-independent (their constructors take only cfg/gateway/store), so they belong in main.py. RiskEngine must receive the SignalEngine reference so `note_intent_emitted()` feeds the D-09 daily-cap burst guard (Pitfall 7 / RISK-05).

Purpose: the final wiring step — once these two leaves are constructed and injected, the entire scan->bar->signal->size->order->manage->alert pipeline is reachable from a single `python -m bot` launch. Depends on Plan 04 (TradingBot constructor must already accept signal_engine/risk_engine).
Output: a main.py that composes a fully-wired bot.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-CONTEXT.md
@.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-RESEARCH.md
@.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-PATTERNS.md
@bot/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct SignalEngine + RiskEngine and inject into TradingBot</name>
  <files>bot/main.py</files>
  <read_first>
    - bot/main.py (the file being modified: import block at lines 25-35; Step 5a leaf builds at lines 74-75 (strategy, engine); PositionManager construction at lines 86-97; TradingBot(...) call at lines 103-113; the watchdog injected after construction)
    - bot/signal/signal_engine.py (contract: SignalEngine(cfg, gateway, store) — loop-independent)
    - bot/risk/risk_engine.py (contract: RiskEngine(cfg, gateway, store, signal_engine=None) — optional signal_engine for the D-09 burst guard)
    - bot/service/bot.py (Plan 04 output: TradingBot.__init__ now accepts signal_engine and risk_engine kwargs)
    - 06.1-PATTERNS.md section "bot/main.py — pure DI composition" (copy-ready: the two new leaf builds and the two new TradingBot kwargs)
    - 06.1-RESEARCH.md Pattern 5 (main.py gains SignalEngine/RiskEngine, loop-independent), Pitfall 7 (RiskEngine needs signal_engine for D-09 burst guard)
    - 06.1-CONTEXT.md D-03 (BarAggregator stays in TradingBot, NOT main.py), D-04 (main.py builds leaf SignalEngine/RiskEngine and injects)
  </read_first>
  <behavior>
    - SignalEngine is constructed once with cfg/gateway/store; RiskEngine is constructed once with cfg/gateway/store and signal_engine=<the SignalEngine just built>.
    - Both are passed to TradingBot via signal_engine= and risk_engine= kwargs.
    - main.py builds NO loop-dependent object (no BarAggregator, no get_running_loop) — that remains in TradingBot.run() (D-03).
    - `python -c "import bot.main"` imports cleanly; the composition wiring is syntactically and structurally valid.
  </behavior>
  <action>
    Add imports `from bot.signal.signal_engine import SignalEngine` and `from bot.risk.risk_engine import RiskEngine` to the main.py import block (alongside the existing bot.* imports). After Step 5a (the `strategy`/`engine` builds at lines 74-75), construct `signal_engine = SignalEngine(cfg=cfg, gateway=gateway, store=store)` then `risk_engine = RiskEngine(cfg=cfg, gateway=gateway, store=store, signal_engine=signal_engine)` — order matters (signal_engine must exist before RiskEngine references it; Pitfall 7). Add the two new kwargs `signal_engine=signal_engine` and `risk_engine=risk_engine` to the existing `TradingBot(...)` call (lines 103-113). Do NOT construct BarAggregator or call get_running_loop in main.py (D-03 — that is TradingBot.run()'s job). Keep the construction-order docstring comment accurate by noting the new leaf builds.
  </action>
  <verify>
    <automated>cd /Users/acdc/Documents/AI/ai-snp-trading-claude && python -c "import bot.main" && python -m pytest tests/service/test_trade_loop_integration.py tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import bot.main"` exits 0 (composition imports cleanly).
    - Source assertion: `grep -c 'SignalEngine(' bot/main.py` >= 1 and `grep -c 'RiskEngine(' bot/main.py` >= 1; the RiskEngine build passes `signal_engine=signal_engine` (grep matches); the TradingBot(...) call passes both `signal_engine=signal_engine` and `risk_engine=risk_engine`.
    - Full suite green: `pytest tests/ -q` (the phase gate — all composition seams wired, no regressions).
    - Source assertion: no `BarAggregator` or `get_running_loop` reference exists in bot/main.py (`grep -c 'BarAggregator\|get_running_loop' bot/main.py` == 0) — D-03 preserved.
  </acceptance_criteria>
  <done>main.py constructs and injects SignalEngine + RiskEngine (with the burst-guard reference), the bot imports cleanly, and the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.py DI -> TradingBot | A miswired or omitted leaf leaves the pipeline silently broken (the exact v1.0 audit failure mode); the composition must be complete and verified by the full suite. |
| RiskEngine -> SignalEngine (burst guard) | Without the signal_engine reference, the D-09 daily-cap tally never increments, allowing more entries than max_trades_per_day. |

## STRIDE Threat Register (logic-integrity, per security_enforcement)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-DI-ORPHAN | Tampering | a leaf component left unconstructed/unwired in composition | mitigate | The full `pytest tests/ -q` phase gate plus the integration test file (which constructs a real TradingBot with both new params) prove no leaf is orphaned; `import bot.main` proves the kwargs match the Plan-04 constructor signature. |
| T-06.1-BURST-GUARD | Elevation of Privilege | entries exceeding max_trades_per_day because note_intent_emitted never fires | mitigate | RiskEngine is constructed with signal_engine=signal_engine (Pitfall 7), wiring the D-09 burst guard; the build order ensures signal_engine exists first. |
| T-06.1-LOOP-DEP | Denial of Service | a loop-dependent object built in main.py before asyncio.run() crashing startup | mitigate | Acceptance criterion forbids BarAggregator/get_running_loop in main.py (D-03); only the loop-independent SignalEngine/RiskEngine are built here. |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH: no new external packages). N/A. |
</threat_model>

<verification>
- `python -c "import bot.main"` → exits 0.
- `pytest tests/ -q` → full suite green (phase gate).
- Source: SignalEngine + RiskEngine built and injected; RiskEngine gets the signal_engine ref; no BarAggregator/get_running_loop in main.py.
</verification>

<success_criteria>
- main.py builds the loop-independent SignalEngine + RiskEngine and injects both into TradingBot (D-04).
- RiskEngine receives the SignalEngine reference (RISK-05 burst guard wired).
- Bot imports cleanly; full suite green; the entire core trade loop is composed and reachable.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (new/changed symbols introduced by THIS plan)

- `bot/main.py` SignalEngine instantiation — NEW leaf build (`signal_engine = SignalEngine(cfg=..., gateway=..., store=...)`)
- `bot/main.py` RiskEngine instantiation — NEW leaf build (`risk_engine = RiskEngine(cfg=..., gateway=..., store=..., signal_engine=signal_engine)`)
- `bot/main.py` TradingBot(...) call — gains `signal_engine=signal_engine` and `risk_engine=risk_engine` kwargs (matching the Plan-04 constructor)
</artifacts_produced>

<output>
Create `.planning/phases/06.1-wire-core-trade-loop-compose-signal-pipeline-in-main-py-regi/06.1-05-SUMMARY.md` when done.
</output>
